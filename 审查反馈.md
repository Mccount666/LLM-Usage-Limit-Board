# 审查反馈 — 第三至六轮修复 + 全量回归复核

日期：2026-09-11
回复对象：`审查报告.md`（第一部分首轮 21 项 + 第二部分复审 + 《待办清单》 + 第三部分第三/四轮复核 9 条 + **第四部分第五轮复核 3 条 P1 + 5 条 P3**）
被改文件：`src/main.js`、`src/preload.js`、`src/renderer/renderer.js`、`src/renderer/index.html`、`src/renderer/styles.css`、`README.md`

> 按报告方要求，每条都给出「文件:行号 + 实际代码片段 + 实测输出」，不接受只写「已修复」，也不接受「已按建议改写」。
>
> **结构**：第三轮处理《待办清单》9 项 + 核验中新发现的崩溃（第一、二节）；第四轮把标为「保留」的 P3-2 / P3-3 / P3-5 / P3-6 落地（第三节）；第七节是对整份报告的全量回归复核；**第八节是对报告《第三部分》9 条待办的修复**（P1-A / P1-B / P2-A / P2-B / P2-C / P3-A / P3-B / P3-C / P3-D），含对第六节 4 个问题的回答与同类排查清单。**第三节推翻了本文件上一版第三节的表述。**
>
> **怎么复跑**：测试已进仓库，`npm test` 与 `npm run test:dom` 各一条命令即可复现（各自打印实际条数）；下文所有「实测输出」都来自这些套件。第八节起每节开头给出该轮交付后的源码哈希。
>
> **报告的哪个版本**：第九节基线 = md5 `b4b05711b7919427b3a6c01dd740fb35`、61925 字节、1228 行、mtime `2026-09-11 23:43:28`（含《第四部分》）；第八节基线 = `6940b0c2…`（941 行）；第七节基线 = `1788743832…`（575 行）。引用报告内容时请先核对哈希。

---

## 零、一个必须先说清的时间差

报告落盘时间 19:34；第三/四轮**交付时实测** `src/` 最新 mtime 为 `21:18:25`（`index.html`）— **源码比报告新**。所以《待办清单》9 项在报告写作时确实成立，但其中 6 项在报告落盘后、本轮开始前已被改掉。本轮的工作是：逐条回源码核验这 9 项，并修掉核验中新暴露的问题。

> P3-D 更正：本节此前把 `src/` 的最后修改时间写成了 `20:30`（会话内近似值，非实测），复审方实测为 `21:18:25`。源码时间戳每轮都会变，**引用时一律以实测 `mtime` 或文件哈希为准**；本文件顶部给出的报告 md5/字节数/mtime 基线同理。

因此下面的表分两类：**「核验已闭合」**（报告写完后已改，本轮给证据）与**「本轮新修」**。

---

## 一、《待办清单》9 项逐条核验

| # | 待办 | 状态 | 证据（文件:行号） |
| --- | --- | --- | --- |
| 1 | escapeHtml 映射改回实体 + 自测 | ✅ 已闭合 | `renderer.js:512`、`424-441` |
| 2 | 协议校验补到 `usage:fetch` 读取侧 | ✅ 已闭合 | `main.js:332` |
| 3 | saveProviders 失败反馈到 UI | ✅ 已闭合 | `renderer.js:197` |
| 4 | 删除 `hasKey` 兜底循环 | ✅ 已闭合 | `grep -n "hasKey === undefined" src/` 无匹配 |
| 5 | 先归一化再 `toFixed` | ✅ 已闭合 | `renderer.js:420` |
| 6 | error 状态阈值重绘语义明确 | ✅ 已闭合 | `renderer.js:160` |
| 7 | `providerCache` 移到文件顶部 | ✅ 已闭合 | `main.js:29` |
| 8 | http:// 明文传输告警 | ✅ 已闭合 | `index.html:83-85`、`renderer.js:107` |
| 9 | 实机运行验证 | ✅ 本轮补做 | 见第四节 |

### 1. escapeHtml（对应复审第一节 P0-2）

```js
// renderer.js:512
function escapeHtml(s) {
  // Use \u escapes so no transport/tooling can accidentally decode entities.
  const AMP = '\u0026amp;';   // &
  const LT  = '\u0026lt;';    // <
  const GT  = '\u0026gt;';    // >
  const QT  = '\u0026quot;';  // "
  const AP  = '&#39;';        // '
  return String(s).replace(/[&<>"']/g, (c) => {
    if (c === '&') return AMP;
    if (c === '<') return LT;
    if (c === '>') return GT;
    if (c === '"') return QT;
    return AP;
  });
}
```

`'\u0026amp;'` 求值为 `&amp;`，五项映射全部是实体，**不再是恒等映射**。自测已按报告要求补上（`renderer.js:528`）：断言 `&` → `&amp;`、`<` → `&lt;`、`"><img>` → `&quot;&gt;&lt;img&gt;`，期望值用字符串拼接构造（`'&' + 'amp;'`）而不是写字面实体，避免被任何一层工具再次解码洗掉。实测跑启动时无 `[security] escapeHtml self-test FAILED` 输出。

### 2. 读取侧协议校验（对应复审第二节 P2-3）

```js
// main.js:332
// P2-3 read-side: even if the file was hand-edited or migrated from an
// older version, we must not pass a non-HTTP(S) URL to `fetch`.
const rawBaseUrl = String(stored.baseUrl || '');
try {
  const u = new URL(rawBaseUrl);
  if (u.protocol !== 'http:' && u.protocol !== 'https:') {
    return { ok: false, error: `Base URL 协议不支持: ${u.protocol}（仅允许 http/https）` };
  }
} catch {
  return { ok: false, error: `Base URL 格式不合法: ${rawBaseUrl}` };
}
```

写入侧（`main.js:280`）与读取侧现在**都有**校验，手工编辑 `providers.json` 塞 `file://` 也拦得住。

### 3. 保存失败反馈（对应复审第二节第 (2) 点）

```js
// renderer.js:197
const ok = await persistAll({ id, apiKey: apiKey || undefined });
if (!ok.ok) {
  // P2-3 UI feedback: show error and STOP. Do not reset form / re-render.
  els.saveError.textContent = ok.error || '保存失败，请检查 Base URL 格式';
  els.saveError.classList.remove('hidden');
  return;
}
els.saveError.classList.add('hidden');
resetForm();
renderAll();
renderHttpWarning();
pollOne(id);
```

失败即 `return`，不再 `resetForm()` / `renderAll()`，不会演出「保存成功」的假象。

### 5 / 6. `toFixed` 归一化 与 error 态重绘语义

```js
// renderer.js:420
const fiveHourPct = Number.isFinite(usage.fiveHourPct) ? usage.fiveHourPct : 0;
const weeklyPct    = Number.isFinite(usage.weeklyPct)    ? usage.weeklyPct    : 0;
setBar(fills[0], pcts[0], fiveHourPct, `${fiveHourPct.toFixed(1)}%`);
setBar(fills[1], pcts[1], weeklyPct,    `${weeklyPct.toFixed(1)}%`);
```

```js
// renderer.js:160
// N-2: only re-render providers that have a successful cached usage;
// skip error-state ones (re-writing the same error message is pointless
// and the user already sees it).
for (const p of state.providers) {
  const last = state.lastUsage.get(p.id);
  if (!last || !last.usage) continue;
  applyUsageRow(p.id, last.usage, null);
}
```

N-2 的语义被统一为「阈值变化只重绘有成功数据的行」，error 行保持原样不动 —— 明确了「重绘什么」。

### 7 / 8. 缓存位置 与 http 告警

```js
// main.js:29
let providerCache = null;
```

```html
<!-- index.html:83-85 -->
<div id="httpWarning" class="http-warning hidden">
  ⚠️ 检测到使用 HTTP（非加密）协议的 Base URL，API Key 将以明文传输。建议改用 HTTPS。
</div>
```

---

## 二、本轮新发现并修复（报告未覆盖）

### N-5【启动级崩溃，最高优先级】`els.minimizeBtn` 未定义 → `bindUi()` 抛错 → 整个界面不工作

复审时还没有最小化按钮；之后加的窗口按钮只改了 HTML 和事件绑定，**漏了 `els` 映射表**。

原代码（本轮修改前）：

```js
// renderer.js:98（修改前）
els.minimizeBtn.addEventListener('click', () => window.api.minimizeWindow());
```

而 `els` 对象里只有 `toggleSettings / refreshBtn / hideBtn`，**没有 `minimizeBtn`**。`els.minimizeBtn` 为 `undefined`，`.addEventListener` 抛 `TypeError`。该行在 `init()` 里位置很靠前：

```js
// renderer.js:65（节选）
(async function init() {
  state.providers = (await window.api.loadProviders()) || [];
  state.security = await window.api.getSecurityStatus().catch(...);
  bindUi();                 // ← 这里抛错
  renderSecurityBanner();
  renderHttpWarning();
  renderAll();              // ← 永远到不了
  await pollAll();          // ← 永远到不了
  setInterval(pollAll, POLL_INTERVAL_MS);
})();
```

后果不是「最小化按钮不灵」，而是 **`renderAll()` / `pollAll()` 全都不执行**——看板永远空白、设置打不开、刷新无反应。这解释了「应用能启动但看起来是坏的」。

修复（`renderer.js:45`，现第 45 行）：

```js
  minimizeBtn: document.getElementById('minimizeBtn'),
```

### N-6【死代码，P1-2 同类】`pickPct()` 零引用

```js
// main.js:463-470（已删除）
function pickPct(list) {
  for (const v of list) {
    if (v == null) continue;
    if (typeof v === 'number') return v <= 1 ? v * 100 : v;
    if (typeof v === 'string' && v.endsWith('%')) return parseFloat(v);
  }
  return null;
}
```

`grep -rn "pickPct" src/` 仅命中定义行本身，功能已被 `detectLimitPct` 取代。**已删除**。这与首轮 P1-2（`clamp()`）是同一类问题，说明上一轮只清了报告点到的那一个，没做同类排查。

### N-7【可用性缺陷】最小化 / 隐藏后无法恢复

窗口是 `frame: false` + `skipTaskbar: true`，既没有任务栏按钮，也没有托盘。点 `_` 或 `×` 之后窗口就**没有任何系统入口能调回来**（`window-all-closed` 也不会触发，因为窗口只是不可见而非关闭），只能重启程序。

修复：新增惰性托盘 + 恢复链路。

```js
// main.js:122-179（节选）
// 16x16 green dot, inlined so packaging needs no external icon asset.
const TRAY_ICON_DATA_URL = 'data:image/png;base64,iVBOR...';
let tray = null;

function showWidget() {
  if (!widgetWindow || widgetWindow.isDestroyed()) return;
  if (widgetWindow.isMinimized()) widgetWindow.restore();
  widgetWindow.show();
  widgetWindow.focus();
}

function ensureTray() {
  if (tray) return;
  try {
    tray = new Tray(nativeImage.createFromDataURL(TRAY_ICON_DATA_URL));
    tray.setToolTip('LLM 用量看板');
    tray.setContextMenu(Menu.buildFromTemplate([
      { label: '显示看板', click: showWidget },
      { type: 'separator' },
      { label: '退出', click: () => app.quit() },
    ]));
    tray.on('click', showWidget);
  } catch (err) {
    // A tray failure must never take the app down.
    console.error('tray creation failed', err);
    tray = null;
  }
}

ipcMain.handle('window:minimize', () => { ...; widgetWindow.minimize(); ensureTray(); });
ipcMain.handle('window:hide',     () => { ...; widgetWindow.hide();     ensureTray(); });
ipcMain.handle('window:show', () => showWidget());
app.on('before-quit', () => { if (tray) { tray.destroy(); tray = null; } });
```

配套 IPC 暴露（`preload.js:12`）：

```js
  showWindow: () => ipcRenderer.invoke('window:show'),
```

托盘图标用 16×16 PNG 内联 data URL（`main.js:130`），**不需要新增二进制资源**，打包 `files: ["src/**/*"]` 不受影响。`Tray` / `Menu` / `nativeImage` 已加入 `main.js:13` 的 require。托盘创建失败会 `catch` 并降级，不会连带主进程崩溃。

> 说明：这属于「报告没提、但不修就等于发一个坏程序」的项（多用户分发场景尤其致命）。若认为超出本轮范围，可回退，但保留 `els.minimizeBtn` 那一条是必须的。

### N-8【安全加固】渲染进程缺 CSP

Electron 启动时报 `Electron Security Warning (Insecure Content-Security-Policy)`。`buildRowHtml` 仍是拼 `innerHTML`（虽然 name 已转义），加一层 CSP 可以保证即使转义再被写错，注入的脚本也执行不了。

```html
<!-- index.html:5-15 -->
<meta
  http-equiv="Content-Security-Policy"
  content="default-src 'none'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'none'"
/>
```

`connect-src 'none'` 让渲染进程连 `fetch`/`XHR` 都发不出去 —— 把「唯一外发入口在主进程」这条隐私契约在渲染层再锁一道。`style-src` 保留 `'unsafe-inline'` 的原因见第三节「一次失败的收紧」（`setBar` 的 `el.style.width` 会被判为内联样式）；`script-src` 锁 `'self'`，页面无内联 `<script>`。加上后重跑，CSP 警告消失且无 `Refused to` 报错。

### N-9【文档失准】README 仍引用已删除的 `normalizeOneAPI`

README 原文（修改前）：

```
我帮你改 `normalizeOneAPI / normalizeBalance`
```

`normalizeOneAPI` 正是首轮 P0-1 认定**全项目不存在**的标识符，README 还在提。已改为 `detectLimitPct / normalizeBalance`，并同步更新了隐私段落（原文写死「发起一次 HTTPS 请求」，与实际允许 http + 告警、且 `redirect: 'error'` 不符）与托盘用法说明。

---

## 三、第四轮：报告 P3 保留项落地 + 同类漏网点

上一版反馈把 P3-2 / P3-3 / P3-5 / P3-6 标为「保留」。本节把它们**全部落地**，并补上同类缺陷链上发现的漏网点。**这里推翻了本文件上一版第三节「没有改动 P3-2/P3-3/P3-5/P3-6」的表述**，特此显式更正。

### P3-2 冗余防御 → 不变量说明

`renderer.js:445`：

```js
function balanceVisualPct(amount, warn, danger) {
  // Map: 0 → 0%, danger → 25%, warn → 75%, 2×warn → 100% (clamped).
  // Invariant: danger < warn, guaranteed by loadThresholds() and
  // onThresholdChange(); neither value reaches here unvalidated, so the
  // divisions below cannot be by zero and need no epsilon padding (P3-2).
  const hi = Math.max(warn * 2, warn + 1); // keeps hi > warn even when warn <= 0
  if (amount <= danger) return 0;
  if (amount >= hi) return 100;
  if (amount <= warn) {
    // danger..warn → 25..75%
    return 25 + ((amount - danger) / (warn - danger)) * 50;
  }
  // warn..hi → 75..100%
  return 75 + ((amount - warn) / (hi - warn)) * 25;
}
```

去掉了 `Math.max(0, danger)` 与两处 `Math.max(…, 0.0001)`。`Math.max(warn*2, warn+1)` **保留**——它不是「不信任上游」的防御，而是保证 `hi > warn` 的构造式（否则 `warn <= 0` 时除法会炸）。实机断言：`balanceVisualPct(0)=0`、`(10)=0`、`(50)=75`、`(200)=100`。

### P3-3 内联 width 归入 CSS

`buildRowHtml()` 中三处 `style="width:0%"` 已删除，初值改由样式表提供（`styles.css:346-351`）：

```css
.bar-fill {
  height: 100%;
  width: 0%; /* P3-3: initial width lives here, not as an inline style */
  border-radius: 3px;
  transition: width 0.6s ease;
}
```

验证：`grep -rn 'style="width:0%"' src/` 无匹配；渲染后宽度仍由 `setBar` 写入（见第五节断言 `widths`）。

### P3-5 平台差异显式分支

`main.js:84-92`：

```js
  // P3-5: `floating` is honoured on macOS and Windows; Linux WMs generally
  // ignore the level argument but still apply the "always on top" hint.
  widgetWindow.setAlwaysOnTop(true, 'floating');
  // P3-5: "visible on all workspaces" is a macOS/Linux concept. On Windows the
  // window is already global, so the call is a no-op there and is skipped to
  // avoid platform-specific quirks.
  if (process.platform !== 'win32') {
    widgetWindow.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true });
  }
```

### P3-6 直接消除黑窗风险类别（**本轮唯一视觉变化**）

报告给的处置是条件式的（「若实测出现黑窗，先移除」）。但这是要**分发到大量不可控机器**的程序，GPU/驱动差异无法替用户复现，因此本轮直接移除 `backdrop-filter`，改为近乎不透明面板（`styles.css:19-34`）：

```css
.widget {
  width: 100%;
  height: 100%;
  /* P3-6: `backdrop-filter` on a `transparent: true` window is known to
     composite the whole window black on some Windows GPU/driver combos, and
     this build ships to many machines we cannot test. The acrylic blur is
     therefore dropped for a near-opaque panel; corners stay rounded because
     the window itself is still transparent. To restore the blur, re-add
     `backdrop-filter: blur(18px);` here and lower the alpha back to ~0.78. */
  background: rgba(20, 22, 28, 0.96);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 14px;
  ...
}
```

**代价是失去毛玻璃效果**（深色面板变实）。若认为视觉优先于这个风险类别，回退方式已写在注释里（重新加 `backdrop-filter` + alpha 降回 0.78），请直接指示。

### N-10【P3-7 同类漏网点】`formatBalance` 未归一化

上一轮按 N-3 只归一化了 plan 分支的 `toFixed`，余额分支同样脆弱：`formatBalance(usage)` 内直接 `usage.amount.toFixed(...)`，`amount` 为 `undefined` 时 `TypeError`。改为在 `applyUsageRow` 统一归一化再传入（`renderer.js:411`）：

```js
  if (usage.mode === 'balance') {
    const { warn, danger } = state.thresholds;
    // Same P3-7 discipline as the plan branch below: normalize before the value
    // reaches comparisons / toFixed, so a malformed amount cannot throw.
    const amount = Number.isFinite(usage.amount) ? usage.amount : 0;
    const level = balanceLevel(amount, warn, danger);
    const visualPct = balanceVisualPct(amount, warn, danger);
    setBar(fills[0], pcts[0], visualPct, formatBalance(amount, usage.currency), level);
    return;
  }
```

```js
// renderer.js:461
function formatBalance(amount, currency) {
  // `amount` is already normalized by the caller, but keep this function total
  // so it can never throw on a bad cache entry.
  const n = Number.isFinite(amount) ? amount : 0;
  const cur = currency || '';
  const formatted = n >= 1 ? n.toFixed(2) : n.toFixed(4);
  return cur ? `${cur} ${formatted}` : formatted;
}
```

实机断言 `formatBalance(undefined, undefined)` → `"0.0000"`，不抛错。

### N-11【P1-4 同类】整表重绘会把已有读数清空

`renderUsage()` 每次 `innerHTML = ''` 重建，而 `buildRowHtml` 的初值是 `--`。于是**新增或删除一个订阅，会让其它所有订阅的读数一起退回 `--`**，直到下一轮轮询（最长 60s）。这与 P1-4「用已有数据重绘」的意图相悖。修法是重建后回填缓存（`renderer.js:329`）：

```js
  // A full rebuild starts every row at "--". Re-apply the cache so adding or
  // deleting one provider does not blank the readings of all the others.
  for (const p of state.providers) {
    const last = state.lastUsage.get(p.id);
    if (!last) continue;
    if (last.usage) applyUsageRow(p.id, last.usage, null);
    else if (last.error) applyUsageRow(p.id, null, last.error);
  }
```

注意：这里对 `error` 状态**也**回填，和 N-2 的阈值重绘不同——因为重绘后行是空的 `--`，错误信息必须补回去；而阈值变化不重建 DOM，所以那处跳过 error 是对的。两者语义不冲突。

同时 `deleteProvider` 删掉对应缓存项，避免 map 随增删无限增长（`renderer.js:259`）：

```js
  state.lastUsage.delete(id); // drop the cache entry with the provider
```

实机断言：`renderAll()` 前后 `bar-pct` 文本完全一致（`["CNY 12.35","42.5%","88.0%"]` → 同值）。

### N-12 二次启动应唤回已隐藏的窗口

`window:hide` 存在后，`requestSingleInstanceLock` 失败即 `app.quit()`，用户再次双击图标什么也不会发生。补上 `second-instance`（`main.js:112-120`）：

```js
const gotLock = app.requestSingleInstanceLock();
if (!gotLock) {
  app.quit();
} else {
  // Launching the app again while it is minimized/hidden should reveal the
  // existing widget instead of silently doing nothing.
  app.on('second-instance', () => showWidget());
}
```

### 一次失败的收紧（如实记录）

P3-3 去掉内联 style 后，我尝试把 CSP 从 `style-src 'self' 'unsafe-inline'` 收紧为 `style-src 'self'`。**实测失败**：`setBar` 的 `el.style.width = …` 被 Chromium 判为内联样式，控制台出现 4 条 `Refused to apply inline style`。虽然 `el.style.width` 读数与 `getComputedStyle` 宽度看起来仍然生效，但行为不可依赖、且会带着 CSP 违规日志发布，故**已回退**。`index.html:5-15` 保留了说明注释：`script-src` 仍为 `'self'`（无内联 `<script>`）、`connect-src` 仍为 `'none'`，只有**样式**保留 `'unsafe-inline'`。

---

## 四、未做的事（避免歧义）

- **P3-6 的视觉效果未肉眼确认**：只验证了「不再有 `backdrop-filter` 黑窗风险类别」，面板变实的观感需人眼过一遍。
- **没有新增**任何 `fetch` 调用。全项目 `grep -n "fetch(" src/` 仍只有 `main.js` 一处真实调用（另一处是注释）。
- **没有实现**托盘之外的第二入口（全局快捷键等），保持最小改动。
- **没有做** HTTP 状态码透出：目前 API Key 错误（401/403）会显示成「找不到字段」，定位成本高。属报告范围外，列为建议。

---

## 五、验证方式与结果

| 项目 | 命令 / 方法 | 结果 |
| --- | --- | --- |
| 语法 | `node --check src/main.js` / `preload.js` / `renderer.js` | 3/3 通过 |
| `els` 键完整性 | 脚本提取 `els` 映射表 key 与 `els.*` 引用做差集 | `missing: (none)`（这正是 N-5 的成因，现已闭合） |
| DOM id 覆盖 | 提取 HTML `id=` 与 `getElementById('…')` 做差集 | 仅 `securityBanner` 不在 HTML 中——它是 `createElement` 动态创建，符合预期 |
| **渲染进程断言（新增）** | 临时 Electron 壳 + stub preload，喂 2 个 provider（包含 XSS 名与超长小数余额），读真实 DOM | 见下方 JSON，全部符合预期，`CONSOLE_ERRORS []` |
| 实机启动 | `electron . --enable-logging`，9s 后回收日志 | 无 `Uncaught` / `TypeError` / `ReferenceError`，无 `escapeHtml self-test FAILED` |
| CSP 生效 | 同上，过滤 `Content Security` / `Refused to` | 警告消失，无拒绝加载 |
| 死引用 | `grep -rn "pickPct\|module.exports\|openExternal\|normalizeOneAPI" src/ README.md` | 全部无匹配 |
| 内联样式清零 | `grep -rn 'style="width:0%"' src/` | 无匹配（P3-3） |

**渲染进程断言原始输出**（stub：`p1` 名称为 `"><img src=x onerror="window.__xss=1">` 且余额 `12.3456 CNY`；`p2` 为 `42.5% / 88%` 的 plan）：

```json
{
  "itemCount": 2,
  "injectedImg": 0,
  "xss": "not-executed",
  "pcts": ["CNY 12.35", "42.5%", "88.0%"],
  "widths": ["27.9%", "42.5%", "88%"],
  "classes": ["bar-fill warn", "bar-fill ok", "bar-fill danger"],
  "nameText": "\"><img src=x onerror=\"window.__xss=1\">",
  "escapeTest": "&quot;&gt;&lt;img&gt;",
  "balanceVisual": [0, 0, 75, 100],
  "formatBadInput": "0.0000",
  "pctsAfterRerender": ["CNY 12.35", "42.5%", "88.0%"]
}
CONSOLE_ERRORS []
```

逐项含义：名称里的 `<img>` **没有被解析成元素**（`injectedImg: 0`、`xss: not-executed`），而是作为文本原样显示；`escapeTest` 证明转义表是实体映射；余额 `12.3456` 被格式化为 `CNY 12.35` 且阈值分级为 `warn`；plan 的 `88` 分级为 `danger`；`formatBadInput` 证明空值不再抛错；`pctsAfterRerender` 与重绘前一致，证明 N-11 的回填生效。

**未验证 / 需实机确认**：

1. **托盘图标实际外观**：内联 PNG 已用脚本按 PNG 规范（IHDR/IDAT/IEND + CRC32）生成，但未在真实托盘区肉眼确认渲染效果与 DPI 缩放表现。
2. **最小化→托盘恢复的交互**：`skipTaskbar: true` 的窗口 `minimize()` 后经 `restore()+show()` 能否稳定回到前台，需实机点一次确认（Windows 上一般可行，但未实测）。
3. **Kimi / 各服务商的真实字段映射**：仍是启发式，需要真实返回 JSON 才能定论（沿用上一轮结论）。
4. **P3-6 的视觉观感**：黑窗风险类别已从设计上移除，但「深色实面板」好不好看需要人眼确认。

---

## 六、请求复审方重点核验的点

1. `renderer.js:45` 的 `els.minimizeBtn` 是否真的消除了 `bindUi()` 崩溃路径（建议对照 `els` 映射表与 `bindUi` 全部引用）。
2. `main.js:122-179` 托盘逻辑是否引入新的生命周期问题（如 `app.quit()` 与 `before-quit` 中 `tray.destroy()` 的顺序、`second-instance` 与 `window:show` 的幂等性）。
3. CSP 是否过紧或过松：`script-src 'self'` 下列表渲染 / 事件绑定是否全部正常；`style-src` 保留 `'unsafe-inline'` 是否为可接受取舍（我实测收紧会被 `el.style.width` 触发违规，见第三节）。
4. N-7（托盘）与 N-12（second-instance）是否属于本轮合理范围——若判定越界，请指出应回退到哪一步。
5. **P3-6 的处置是否过激**：我以「分发到不可控机器」为由直接移除了毛玻璃。若复审方认为应保留视觉、仅加文档说明，请明确，我改回。
6. N-10 / N-11 是否真正覆盖了 P3-7 与 P1-4 的全部崩溃/失真路径，还是仍有同类漏网点（这是连续两轮都出现的问题类型）。

---

## 七、全量回归复核（首轮 21 项 + 复审 + 待办清单 + 备注）

**起因**：2026-09-11 稍晚，用户告知「审查报告已更新」。核对后 `审查报告.md` 与本文件开工时**逐字节一致**（md5 `1788743832adc05e4ca1743bf2fcef9e`，24305 字节，mtime 仍为 `19:34:37`），文末仍是《复审方法论教训》，没有任何针对第三、四轮改动的复核内容；工作区其它位置也没有副本（`.workbuddy/memory/` 的审查方笔记停在 19:35）。**结论：没有新的审查意见可执行。** 按用户选择，改为对现有报告做一次全量回归复核，确认第三、四轮的改动没有让任何一条首轮条目回退。

**方法**：写一次性脚本（跑完即删）把报告里每一条要求翻译成可判定的断言，逐条对当前源码求值，不靠人工「看着像」。共 40 条，**PASS 40 / FAIL 0**。

| 编号 | 报告要求 | 结果 | 证据 |
| --- | --- | --- | --- |
| P0-1 | 删除引用不存在函数的 `module.exports` | PASS | `grep module.exports\|normalizeOneAPI src/` 无匹配 |
| P0-2 | `escapeHtml` 真实体映射（非恒等） | PASS | `renderer.js:514`，五项均为实体 |
| P0-2b | 补最小自测 | PASS | `renderer.js:540` |
| P0-3 | 删除未注册的 `shell:openExternal` | PASS | preload/main 均无；`shell` 未 require |
| P0-3b | preload 暴露的每个通道都有 handler | PASS | channels=8, handlers=8, missing=无 |
| P1-1 | 不再依赖 `undefined` 隐式哨兵 | PASS | `renderer.js:216` `persistAll({ id, apiKey: apiKey \|\| undefined })` |
| P1-2 | 删 `clamp()` 死代码 | PASS | 全项目无 `clamp(`；`clampPct` 定义于 `src/lib/limits.js:67` 且被调用 6 处 |
| P1-3 | 轮询重入保护 | PASS | `renderer.js:472` |
| P1-4 | 阈值变更走缓存、不发请求 | PASS | `renderer.js:148` 区间内无 `pollAll` / `pollOne` |
| P1-5 | provider 内存缓存 | PASS | `main.js:29`，写操作失效缓存 |
| P1-6 | 移除 apiKey 的 `required` | PASS | `index.html:55`；新增强制在 `renderer.js:183` |
| P2-1 | `finally` 清理超时 timer | PASS | `main.js:536` |
| P2-2 | `redirect: 'error'` | PASS | `main.js:529` |
| P2-3 | 协议校验写入侧 + 读取侧 | PASS | `main.js:280` / `main.js:332` |
| P2-3b | 保存失败反馈到 UI | PASS | `renderer.js:197` |
| P2-3c | http:// 明文告警 | PASS | `index.html:83` / `renderer.js:107` |
| P2-4 | `normalizeBalance` 按键名 + 跳过空值 | PASS | `src/lib/limits.js:152` |
| P2-5 | safeStorage 降级告警全链路 | PASS | `main.js:236` / `renderer.js:94` / `styles.css:206` |
| P3-1 | `pollOne` try/catch | PASS | `renderer.js:486` |
| P3-2 | 简化 `balanceVisualPct` 冗余防御 | PASS | `renderer.js:445`，无 `Math.max(0, danger)`、无 `0.0001` |
| P3-3 | 内联 width 归入 CSS | PASS | `styles.css:353`；`grep 'style="width:0%"'` 无 |
| P3-4 | crash reporter 开关合法 | PASS | `main.js:49` `disable-crash-reporter` |
| P3-5 | 平台差异分支 | PASS | `main.js:90` |
| P3-6 | 移除 `backdrop-filter` | PASS | `styles.css:22` 仅注释，无生效声明 |
| P3-7 | `setBar` 兜底 + 调用点归一化 | PASS | `renderer.js:411` / `renderer.js:420` / `setBar` 守卫 |
| N-1 | 删 `hasKey` 兜底死循环 | PASS | `grep hasKey === undefined` 无 |
| N-2 | 阈值重绘语义明确 | PASS | `renderer.js:160` |
| N-3 | 调用点先归一化再 `toFixed` | PASS | `renderer.js:414`；无 `usage.fiveHourPct.toFixed` |
| N-4 | `providerCache` 移到文件顶部 | PASS | `main.js:29` < `usage:fetch` |
| N-5 | `els.minimizeBtn` 定义 | PASS | `renderer.js:45` |
| N-6 | 删 `pickPct` 死代码 | PASS | `grep pickPct` 无 |
| N-7 | 托盘恢复链路 | PASS | `main.js:142` + `preload.js:12` + `window:show` |
| N-8 | CSP 锁定 script/connect | PASS | `index.html:13` |
| N-9 | README 无 `normalizeOneAPI` | PASS | README 核验 |
| N-10 | `formatBalance` 归一化 | PASS | `renderer.js:461` |
| N-11 | 重绘后回填缓存 + 删缓存项 | PASS | `renderer.js:282` / `renderer.js:259` |
| N-12 | `second-instance` 唤回窗口 | PASS | `main.js:119` |
| 备注-1 | README 与 redirect 行为一致 | PASS | 不再写死「发起一次 HTTPS 请求」 |
| 备注-2 | `asar: true` 且 files 覆盖 src | PASS | `package.json` 的 build 段 |
| 备注-3 | 全项目 `fetch(` 真实调用仅 1 处 | PASS | 脚本计数 = 1（`main.js:529`） |

### 回归结论

第三、四轮的改动（`renderUsage` 回填缓存、CSS 类化、移除 `backdrop-filter`、新增 CSP、托盘与 `second-instance`）**没有让任何一条首轮条目回退**。特别复核了两类历史上出过问题的项：

- **「只改一处、漏别处」型**：P1-2（`clamp`）与 N-6（`pickPct`）两处死代码都已清掉；N-3 与 N-10 的归一化在 plan / balance **两个分支都有**；P2-3 的协议校验写入侧与读取侧都在。
- **「修表层、漏崩溃点」型**：P3-7 的 `toFixed` 崩溃点在两处调用点之前都做了 `Number.isFinite` 归一化，`formatBalance` 自身也做成了 total function。

### 复核过程中的一次自我纠错

首轮跑核验脚本时报 `P1-2 FAIL`，追查后是**脚本自身写错**——它去 `renderer.js` 里找 `clampPct(`，而该函数定义在 `main.js`。修正断言后为 PASS。记录在此，因为「核验工具本身可能是错的」正是报告《复审方法论教训》指出的那类风险，不应被静默吞掉。

---

## 八、第五轮：对报告《第三部分》9 条待办的修复

**复核基线**：本轮针对的 `审查报告.md` 为 md5 `6940b0c21b35fd92669d3c73a0e0764f`、43420 字节、941 行、mtime `2026-09-11 23:04:22`（新增《第三部分》）。本轮交付后源码哈希：`main.js` `90d2dd6f…`、`lib/limits.js` `81197c1e…`、`renderer.js` `f7c99b20…`、`index.html` `29a2a4f9…`、`styles.css` `ebee6a81…`、`preload.js` `8dfb1996…`；测试文件 `report-items.test.js` `2af1d2d3…`、`limits.test.js` `1090d3d4…`、`ipc.test.js` `96de26d6…`、`dom/main.js` `f1a9ee95…`。

### 结论速览

| 报告条目 | 状态 | 位置 | 实测 |
| --- | --- | --- | --- |
| P1-A `detectLimitPct` 绝对量当百分数 | ✅ 已修 | `src/lib/limits.js:18-145` | 22 条断言，报告表格 6 例全绿 |
| P1-B `p.id` DOM 注入 + 刷新停摆 | ✅ 已修 | `renderer.js:282-310`、`:342`、`:453-476` | DOM 断言：注入元素 2→**0**，`lastUpdated` `—`→**更新于 …** |
| P2-A http 告警大小写绕过 | ✅ 已修 | `renderer.js:107-118` | 判别性断言：`HTTP://` 现在触发明文告警 |
| P2-B `loadProviders()` 无兜底 | ✅ 已修 | `renderer.js:68-78` + `index.html:94` | DOM 5 条：横幅可见、`init` 未中断 |
| P2-C catch 内二次抛出 | ✅ 已修 | `renderer.js:486-509` | 单一受保护 DOM 调用点，控制台零错误 |
| P3-A 行形状双来源 | ✅ 已修 | `renderer.js:312-317`、`:361-366` | plan provider 收到 balance 数据 → 重建为 1 条 bar |
| P3-B `id` 类型/唯一性未校验 | ✅ 已修 | `main.js:264-276` | 5 条 IPC 断言 |
| P3-C 余额最坏串行 7 次请求 | ✅ 已修（本轮补完 user-info 4 候选） | `main.js:429`（`probeCandidates`） | 首次探测并发；稳态仅 1 个请求 |
| P3-D 反馈时间戳不实 | ✅ 已更正 | 本文件第一节 + 顶部基线 | 改为实测值 |

### P1-A `detectLimitPct`：拆成三档

报告表格里 6 例有 4 例是错的（`used/total` 返回 100 而非 30 等），根因是 hint 顺序倒置 + 对绝对量做 `<=1 ? *100 : 原值` + 分母字段单独当分子。已按三档重写（`src/lib/limits.js:18-145`）：

```js
// src/lib/limits.js:121-145
function detectLimitPct(root, spec) {
  // ① paired used/total — divide, never treat the numerator as a percentage
  for (const { used, total } of spec.pairs) {
    const u = numOf(getPath(root, used));
    const t = numOf(getPath(root, total));
    if (u == null || t == null || t <= 0) continue;
    return clampPct((u / t) * 100);
  }
  // ② fields whose name states they are a percentage
  for (const path of spec.pctPaths) {
    const parsed = parseLimit(getPath(root, path));
    if (parsed == null) continue;
    return clampPct(asPct(parsed));
  }
  // ③ ambiguous scalars — only two readings are trustworthy: an explicit "42%"
  // string, or a value <= 1 (an unambiguous ratio). A bare 300 is an absolute
  // amount whose denominator we do NOT know, so abstain rather than guess.
  for (const path of spec.ambiguous) {
    const parsed = parseLimit(getPath(root, path));
    if (parsed == null) continue;
    if (!parsed.explicit && parsed.value > 1) continue;
    return clampPct(parsed.explicit ? parsed.value : parsed.value * 100);
  }
  return null;
}
```

**一处偏离报告建议、需要复审方注意**：报告给的 `ambiguous` 档只写了「≤1 当比例，>1 弃权」。这样会把 `{'5h_used': '42%'}` 也一起弃权掉——但报告自己的表格里这一格期望是 **42**。所以我把「带 `%` 后缀」实现为独立的 `explicit` 信号：**显式 `%` 字符串直接采信，裸数字才受 `>1 弃权` 约束**。`parseLimit`/`asPct` 见 `src/lib/limits.js:90-106`。

实测输出（`node tmp-review-test/unit.js`，函数从真实源码抽取后求值，非副本）：

```
== detectLimitPct / FIVE_HOUR_SPEC ==
  PASS  审查报告表格 1: {five_hour:{used_percentage:30}}  =>  30
  PASS  审查报告表格 2: {five_hour:{used:300,total:1000}}  =>  30
  PASS  审查报告表格 3: {five_hour:{used:3,total:1000}}  =>  0.3
  PASS  审查报告表格 4: {quota_5h_used:300,quota_5h_total:1000}  =>  30
  PASS  审查报告表格 5: {five_hour_limit:1000}  (分母单独出现→无值)  =>  null
  PASS  审查报告表格 6: {5h_used:"42%"}  =>  42
  PASS  used 无 total → 弃权  =>  null
  PASS  裸标量 300（分母未知）→ 弃权  =>  null
  PASS  裸标量 0.42（比例）→ 42
  PASS  used_percentage:0.3（比例）→ 30
  PASS  total=0 → 不除零，弃权  =>  null
  PASS  超额 used>total → 夹到 100  =>  100
  PASS  five_hour.used + five_hour.limit 配对  =>  25
  PASS  pairs 优先级高于 ambiguous  =>  30
  PASS  空对象 → null / null → null / "abc" → 弃权
== detectLimitPct / WEEKLY_SPEC ==  (5 条，含 seven_day used/total → 60)
==== unit: PASS 29, FAIL 0 ====
```

端到端（真实 `main.js` 走 `usage:fetch` handler，stub `fetch`）：

```
  PASS  used/total 300/1000 → 30（报告表格第 2 例）  [30]
  PASS  Authorization 头带解密后的 key  [["Bearer sk-plan"]]
  PASS  裸 300 不被当成 300%  [{"ok":false,"error":"服务商返回中找不到 5h/周 限额字段…"}]
```

顺带清掉一个新产生的死代码：改写后 `pctOf()` 无引用，已删（`grep pctOf src/` 零命中）。这正是报告《方法论教训》第 2 类「只改点名处、不做同类排查」，所以我在同一轮里自己扫了一遍。

### P1-B `p.id`：不再拼 HTML，选择器不再带插值

`renderProviderList` 改为 DOM API（`renderer.js:282-310`），彻底不存在"转义漏没漏"的问题：

```js
// renderer.js:291-308
const label = document.createElement('span');
label.appendChild(document.createTextNode(p.name));
const tag = document.createElement('small');
tag.className = 'mode-tag';
tag.textContent = `· ${p.mode === 'balance' ? '余额' : 'Plan'}`;
label.appendChild(tag);

const actions = document.createElement('span');
const editBtn = document.createElement('button');
editBtn.textContent = '编辑';
editBtn.addEventListener('click', () => startEdit(p.id));
...
li.append(label, actions);
```

`applyUsageRow` 的查找改走 dataset 遍历（`renderer.js:375`）：

```js
const row = [...els.list.querySelectorAll('.usage-item')].find((el) => el.dataset.id === id);
```

实测复现（`electron tmp-review-test/stub-main.js`，provider id 为 `inj"><img src=x onerror="window.__xssId=1`）：

```
DOM {"imgInProviderList":0,"imgInUsageList":0,"xssIdFlag":"undefined",
     "lastUpdated":"更新于 23:13:39",
     "rowIds":["inj\"><img src=x onerror=\"window.__xssId=1","p2","p3","p4"]}
  PASS  provider 列表无注入 <img>（报告实测为 2）  [0]
  PASS  注入脚本未执行  [undefined]
  PASS  自动刷新链路存活（lastUpdated 已更新，报告实测为 "—"）  ["更新于 23:13:39"]
  PASS  恶意 id 原样保留在 dataset 中
  PASS  渲染进程无 console 错误（无 querySelector SyntaxError）  [[]]
==== dom(injection): PASS 12, FAIL 0 ====
```

### P2-A / P2-B / P2-C

```js
// renderer.js:107-118 —— 用解析后的协议判断，而不是大小写敏感的 startsWith
const hasHttp = state.providers.some((p) => {
  try {
    return new URL(String(p.baseUrl || '')).protocol === 'http:';
  } catch {
    return false;
  }
});
```

判别性证据（旧实现必然漏判）：

```
new URL('HTTP://plain.example.com').protocol   = http:
'HTTP://plain.example.com'.startsWith('http://') = false   <- 旧实现据此判断，会漏
DOM: PASS  P2-A: HTTP:// 大写协议也触发明文告警  [hidden=false]
```

```js
// renderer.js:68-78 —— 失败兜底 + 可见提示 + 首次抓取不再阻断定时器
let loadError = null;
state.providers = await window.api.loadProviders().catch((err) => {
  loadError = err?.message || String(err);
  return [];
});
...
renderLoadError(loadError);
renderAll();
setInterval(pollAll, POLL_INTERVAL_MS);   // ← 已提到 await pollAll() 之前
await pollAll();
```

`pollOne` 重构为「网络段」+「渲染段」两段，DOM 写入只保留一个调用点且整体包在 try/catch 内（`renderer.js:486-509`）；`pollAll` 也补了 catch，避免未处理 rejection。

实测（preload 里 `loadProviders` 直接 throw）：

```
DOM {"exists":true,"hidden":false,
     "text":"⚠️ 读取本地订阅失败：IPC channel closed。看板可能不完整，可在设置里重新添加订阅。"}
  PASS  loadError 可见（非 hidden）  [false]
  PASS  init 未中断，renderAll 仍执行（空态可见）
==== dom(loadfail): PASS 5, FAIL 0 ====
```

### P3-A / P3-B / P3-C

```js
// renderer.js:312-317 —— 行的形状以数据为准
function rowMode(p) {
  const last = state.lastUsage.get(p.id);
  if (last && last.usage && last.usage.mode) return last.usage.mode === 'balance' ? 'balance' : 'plan';
  return p.mode === 'balance' ? 'balance' : 'plan';
}
// renderer.js:394-399 —— 数据模式与已渲染形状不符时重建该行
const mode = usage.mode === 'balance' ? 'balance' : 'plan';
if (row.dataset.mode !== mode) {
  const p = state.providers.find((x) => x.id === id);
  row.dataset.mode = mode;
  row.innerHTML = buildRowHtml(p, mode);
}
```

```
DOM: PASS  P3-A 形状随数据：plan provider 收到 balance → 重建为 1 条 bar  [["CNY 9.50"]]
```

`main.js:269-276` 加 id 校验与去重；`main.js:442-450` 余额候选改并发：

```
  PASS  非字符串 id 被拒  [{"ok":false,"error":"订阅 id 不合法（必须是非空字符串）"}]
  PASS  重复 id 被拒  [{"ok":false,"error":"订阅 id 重复: dup"}]
  PASS  三个余额候选并发（<350ms 而非串行 ~450ms）  [155ms, ok=true]
  PASS  三个候选都被发出  [3 次]
```

### P3-C 收口：并发探测 + 记住可用路径（本轮补完）

上一版留了个尾巴：只并发了余额的 3 个候选，`fetchOneAPIUserInfo` 的 4 个候选仍是串行，最坏 4 × 10s = 40s —— 叠加 60s 的轮询间隔，一次慢响应会直接吃掉下一整轮。本轮补完，并把这条路径统一到同一个机制里。

```js
// main.js:429-455
async function probeCandidates(provider, kind, paths, headers, accept) {
  const base = provider.baseUrl.replace(/\/+$/, '');
  const key = `${provider.id}:${kind}`;

  const remembered = candidatePathCache.get(key);
  if (remembered && paths.includes(remembered)) {
    const res = await tryFetchJson(`${base}${remembered}`, provider, headers);
    if (res.ok && accept(res)) return res;
    candidatePathCache.delete(key); // stale — rediscover below
  }

  const probed = await Promise.all(
    paths.map(async (path) => ({ path, res: await tryFetchJson(`${base}${path}`, provider, headers) })),
  );
  for (const { path, res } of probed) {
    if (res.ok && accept(res)) {
      candidatePathCache.set(key, path);
      return res;
    }
  }
  return null;
}
```

**没有简单地"全部并发"是有意的**：那几个候选是同一个 host 上同一语义查询的不同拼写，每次轮询都全发会稳定产生 3 个必然 404 的请求。所以做成「记住上次成功的路径 → 稳态只发 1 个；记住的失效就整体重探」。结果是拿到并发的延迟上界（最坏 10s 而非 40s），又不放大稳态请求数。缓存随 `providers:save` / `providers:delete` 清空（`main.js:42-45`），换服务商不会被旧路径拖住。

实测（`npm test` → `test/ipc.test.js`）：

```
  ok   首次探测并发（4 候选各 150ms → <350ms）
  ok   稳态只发 1 个请求（复用记住的路径）
  ok   记住的路径失效 → 重新探测并恢复
  ok   余额候选并发探测（清缓存后 3 个同时发出）
```

### 界面与图标：把"请人眼确认"改成实测

上一版把「托盘图标外观」「最小化→恢复交互」「P3-6 观感」都挂成"需人眼确认"。本轮能自动化的都自动化了：

1. **托盘图标真的能解码**（`test/dom/main.js` 的 `visual` 场景）：从 `main.js` 取出内联 data URL，用 `nativeImage.createFromDataURL` 真正解码 —— `empty=false`、尺寸 `16x16`、PNG 296 字节。此前只验证过"字符串存在"，那不够：解码失败就是一个空图标，托盘区什么也看不到。
2. **窗口控制 + 托盘链路**（`test/ipc.test.js`，把 Electron stub 改成真正执行 `createWidgetWindow()`）：托盘惰性创建且只创建一次；`window:show` 对已最小化的窗口执行 `restore → show → focus`、未最小化时 `show → focus`；托盘菜单含「显示看板」与「退出」。
3. **实际渲染截图**：`capturePage()` 存成 PNG 后人工查看 —— 面板是深色实底、**没有黑窗**，四行读数与颜色分级（含 88% 红、42.5% 绿、余额单条、错误行）都正确。同时把"感觉 bar 长度不太对"的疑虑改成**测量**：`bar-fill` 实测占轨道宽度 `[42.5, 88]`、余额中段 `[50]`、低用量 `[10, 20]`、错误态 `[0, 0]`，与输入严格一致 —— 我原先的肉眼判断是错的，所以这条以测量为准。


### 回答《六、要求下一轮反馈回答的问题》

**1. `used/total` 改实现还是改 README？—— 改实现，README 原样保留（现在承诺为真）。** 证据即 `limits.test: 37 passed`（300/1000 → 30）；`README.md:45` 的「支持 `used/total`、`used_percentage`、`"42%"` 三种形态」现在逐项成立。

**2. 为什么第七节 40 条里没有一条覆盖 `detectLimitPct`？—— 接受批评，并写成硬规则。** 原因很直白：我的回归清单是**按报告条目**组织的，`detectLimitPct` 是上一轮我自己替换出来的新函数，报告里没有对应条目，清单就没长出来 —— 于是"PASS 40 / FAIL 0"与"新函数 6 例中 4 例错"同时成立。**本轮起执行：交付前先列出「本轮新增或语义变更的函数」，每个至少 3 条断言**。本轮清单：

| 本轮新增/变更的函数 | 断言数 | 载体（已进仓库） |
| --- | --- | --- |
| `detectLimitPct`（语义重写） | 22 | `test/limits.test.js` |
| `parseLimit` / `asPct` / `getPath` / `numOf` / `clampPct`（新增/改） | 10 | `test/limits.test.js` |
| `normalizeBalance`（随模块搬迁） | 6 | `test/limits.test.js` |
| `renderProviderList`（重写为 DOM API） | 3 | `test/dom/main.js` |
| `rowMode`（新增） | 2 | `test/dom/main.js` |
| `applyUsageRow`（查找与形状重写） | 4 | `test/dom/main.js` |
| `pollOne` / `pollAll`（重构） | 2 | `test/dom/main.js` |
| `renderHttpWarning`（改判断方式） | 1（判别性） | `test/dom/main.js` |
| `renderLoadError`（新增） | 3 | `test/dom/main.js`（loadfail 场景） |
| `providers:save` id 校验（改） | 5 | `test/ipc.test.js` |
| `fetchBalanceUsage` 并发（改） | 2 | `test/ipc.test.js` |

### 为了让测试真的长出来：纯函数抽成 `src/lib/limits.js`

问题 2 的根因不只是"清单没跟着长"，还有**结构上长不出来**：`detectLimitPct` / `normalizeBalance` 原本内联在 `main.js` 里，而 `main.js` 是 Electron 入口（`require('electron')`、`app.whenReady()`、`ipcMain.handle()`），在没有窗口的环境里根本 `require` 不进来——所以上一轮"想补测试"也只能靠从我这儿临时抽取源码文本。

本轮把这些**纯函数**（无 Electron、无 I/O）抽成 `src/lib/limits.js`，`main.js` 顶部 `require('./lib/limits')` 使用（`main.js:16-22`）。这样 `test/limits.test.js` 直接 `require('../src/lib/limits')` 就能跑，**不需要任何抽取技巧**。副作用是 `main.js` 从 22729 字节降到 16732 字节，职责更清楚。

另外新增 `test/report-items.test.js`（58 条）：把报告里每一条要求编码成对源码的断言。这个文件的存在意义是——**以后任何一轮改动，只要碰坏了报告点过的任何一条，`npm test` 立刻红**，不依赖我或复审方记得去查。

**3. 同类全量排查：所有「拼进 `innerHTML` 或选择器」的变量 —— 完整清单。**

| 位置 | 变量 | 上下文 | 处置 |
| --- | --- | --- | --- |
| `renderer.js:306-330` `buildRowHtml` | `name` | innerHTML 的**文本**位置 + `title="…"` **属性**位置 | 唯一进入 HTML 上下文的变量，经 `escapeHtml`；DOM 断言注入未成型 |
| `renderer.js:282-310` `renderProviderList` | `p.name` / `p.id` | 原为 innerHTML 属性位置 | **已改为 `createElement` + `dataset` 属性赋值**，不再经 HTML 解析 |
| `renderer.js:375` `applyUsageRow` | `id` | 原为 `querySelector` 模板 | **已改为 dataset 遍历匹配**，无插值 |
| `renderer.js:365` `row.innerHTML` | 无变量 | —— | 只调 `buildRowHtml`，受上面同一条约束 |
| `renderer.js:88-90` `renderLoadError` | `msg` | textContent | 天然安全 |
| `renderer.js:262` 模式标签 | 字面量 | textContent | 天然安全 |
| `renderer.js:389-396` `setBar` | `pct` | `style.width` / textContent | `Number.isFinite` 归一化 + `toFixed`，纯数字串 |
| 其余 textContent 写入（错误信息、更新时间） | `msg` / 时间 | textContent | 天然安全 |

结论：**当前唯一进入 HTML 上下文的变量只有 `name`，且已转义；选择器侧零插值。** 可以用 `grep -n 'querySelector.*\${' src/renderer/renderer.js`（零命中）与 `grep -n '\${' src/renderer/renderer.js` 复核。

**4. `setInterval` 提到 `await pollAll()` 之前 —— 已做**（`renderer.js:84` 在 `:85` 之前），并额外给 `pollAll` 加了 catch。理由如报告所述：自动刷新与首次抓取解耦，首次 reject 不再让定时器永不注册。

### 测试总量（本轮）

| 套件 | 条数 | 结果 | 命令 |
| --- | --- | --- | --- |
| 语法 | 4 | 4/4 | `node --check src/main.js src/preload.js src/renderer/renderer.js src/lib/limits.js` |
| 报告条目回归 | 58 | 58/58 | `npm test`（`test/report-items.test.js`） |
| 纯函数单元 | 37 | 37/37 | `npm test`（`test/limits.test.js`） |
| IPC 端到端（stub electron + stub fetch + 窗口/托盘） | 23 | 23/23 | `npm test`（`test/ipc.test.js`） |
| 实机 DOM（真实 Electron，含截图场景） | 16 + 5 + 6 | 27/27 | `npm run test:dom` |
| **合计** | **145** | **145/145** | `npm test && npm run test:dom` |

**这些测试已进仓库**（`test/`，不随 `electron-builder` 打包——`files` 只含 `src/**/*`），不再是"跑完即删"。报告要求的「把实测表那 6 行写成断言进回归清单」现在落在 `test/limits.test.js`；「新增/替换的函数必须进回归清单」落在上面那张表 + `test/report-items.test.js`。

### 本轮的自我纠错（核验脚本连续第三次误报）

`audit.js` 首轮报 3 条 FAIL，逐条追查**全部是脚本自身的问题**：① 去 `renderer.js` 找定义在 `main.js` 的 `clampPct`；② 把清空用的 `innerHTML = ''` 误判为"拼接 innerHTML"；③ 命中了注释里的 `data-id="${p.id}"`，以及我在更正说明里**引用的旧措辞**。断言已修正并加了去注释处理。这与报告《方法论教训》里「核验工具本身可能是错的」是同一件事，我把它固化成习惯：**断言失败时先怀疑断言，再怀疑代码**，并把每次误报写进反馈。

### 未做 / 未验证（明确列出）

- **真实桌面点击托盘**仍未做：handler 序列（惰性建托盘 / `restore → show → focus` / 菜单项）已由 `test/ipc.test.js` 覆盖，但"在真实 Windows 通知区左键点一下能把窗口调回来"依赖 Shell 行为，自动测试无法替代，需人在真机上确认一次。
- **真实服务商 JSON 仍缺**：字段映射是启发式，`detectLimitPct` 的三档覆盖了报告表格与常见变体，但没见过真实 Kimi/NewAPI 返回，无法保证穷尽。

---

## 九、第六轮：对报告《第四部分》的修复

**复核基线**：本轮针对的 `审查报告.md` 为 md5 `b4b05711b7919427b3a6c01dd740fb35`、61925 字节、1228 行、mtime `2026-09-11 23:43:28`（新增《第四部分 · 第五轮复核》）。本轮交付后源码哈希：`main.js` `79e3eb31…`、`lib/limits.js` `81197c1e…`（未变）、`renderer.js` `e32a924c…`、`index.html` `29a2a4f9…`（未变）、`styles.css` `ebee6a81…`（未变）、`preload.js` `8dfb1996…`（未变）、`README.md` `2ac7285a…`；测试 `report-items.test.js` `91996cdf…`、`limits.test.js` `1090d3d4…`（未变）、`ipc.test.js` `1dcc9317…`、`dom/main.js` `24c94f8b…`。

### 结论速览

| 报告条目 | 状态 | 位置 | 实测 |
| --- | --- | --- | --- |
| P1-A `probeCandidates` 等最慢候选 | ✅ 已修 | `main.js:429-466` | 首选 0ms + 另一候选 3000ms → **实测不等待**（新判别性断言 `<300ms`） |
| P1-B `accept` 太松导致缓存卡死 | ✅ 已修 | `main.js:493-501`、`:503`、`:385`、`:479` | 200+{} 不被接受，有效候选胜出且被记住 |
| P1-C 7 个 IPC 调用点只修了 1 个 | ✅ 已修（全部 7 个） | 见下方清单 | 4 个新场景实测（savefail / ipcfail / behavior / loadfail） |
| P3-E README 测试数字过时 | ✅ 已修（改为不写数字，根治） | `README.md:41-53` | 并加断言禁止再写死 |
| P3-F README 解析逻辑路径失准 | ✅ 已修 | `README.md:57` | 改为 `src/lib/limits.js` |
| P3-G 关键条目升级为行为断言 | ✅ 已做 | `test/dom/main.js`（`behavior` 场景） | 重叠轮询只发一轮、阈值变更 0 请求 |
| P3-H 时间断言易 flake | ✅ 已修 | `test/ipc.test.js` | 350ms → 1000ms，并注明计数断言才是权威 |
| P3-I `normalizeBalance` 算两次 | ✅ 已修 | `main.js:471-482` | 赢家解析结果由 accept 捕获，只算一次 |

### 一、P1-A：改成「先到先用」

报告是对的，而且这一条是我上一轮**新引入的退化**。我当时只盯着「最坏 40s → 10s」，没意识到 `Promise.all` 会把**常见情况**变差：正确端点在 10ms 就返回，却要等一个不相干的慢候选 8s。这是拿典型路径去换最坏路径，方向错了。

```js
// main.js:446-465 —— 先到先用：任一候选被接受即返回，不等慢候选
  function probeAll() {
    if (paths.length === 0) return Promise.resolve(null);
    return new Promise((resolve) => {
      let remaining = paths.length;
      let done = false;
      for (const path of paths) {
        tryFetchJson(`${base}${path}`, provider, headers)
          .then((res) => {
            if (done) return;
            if (res.ok && accept(res)) {
              done = true;
              candidatePathCache.set(key, path);
              resolve(res);
              return;
            }
            if (--remaining === 0) resolve(null);
          })
          .catch(() => {
            if (!done && --remaining === 0) resolve(null);
          });
      }
    });
  }
```

**新增判别性测试**（报告要求的那条）：首选 0ms、另一个候选 3000ms。

```
  ok   P1-A 先到先用：首选 0ms + 一个候选 3000ms → <300ms
```

这条在旧实现下必然失败（会等满 3000ms），是真正的判别性用例，不是"看着像好了"。

**关于测试设计**：报告问「你的测试给 4 个候选设了相同 delay，为什么？」——**因为我在照着实现写测试，而不是照着反例写测试**。相同 delay 只能证明"并发确实发生了"，无法区分"先到先用"和"等最慢"。这个错误比"漏了一个用例"更值得记：**测试如果和实现同构，就只是在复述实现**。本轮的新机制我一律先写反例（慢候选、空对象、reject）。

### 二、P1-B：`accept` 改为「真的解析出来了才算数」

报告又对：`fetchOneAPIUserInfo` 的 accept 只要求"是个对象"，于是 `HTTP 200 + {success:false,message:"无权进行此操作"}` 会被当成有效响应写进缓存，**其余候选永远不再被尝试**。而 `fetchBalanceUsage` 的 accept 是严谨的——同一次重构里两套标准，**松的那一处是笔误**。

```js
// main.js:493-501 —— plan 模式的严格判据
function hasPlanLimits(root) {
  if (!root || typeof root !== 'object') return false;
  return detectLimitPct(root, FIVE_HOUR_SPEC) != null || detectLimitPct(root, WEEKLY_SPEC) != null;
}

// main.js:385 —— 调用方各自传判据
const data = await fetchOneAPIUserInfo(provider, (res) => hasPlanLimits(res.data?.data ?? res.data));

// main.js:483 —— 余额 fallback 沿用本来就严谨的那套
const data = await fetchOneAPIUserInfo(provider, (res) => Boolean(normalizeBalance(res.data)));
```

顺带删掉一处**因严格 accept 而变成不可达**的分支：原来 `if (fiveHourPct == null && weeklyPct == null) return {找不到…}` —— accept 已经保证至少有一个非空，这个检查永远不会命中。但那条错误文案是有用的（它请用户把 JSON 发来适配），所以把文案移到了「无候选被接受」这条真正会走到的分支上，没有丢。

实测（报告要求的判别性用例）：

```
  ok   P1-B 200+{} 不被接受，有效候选胜出并被记住
       → /api/user/self 返 200+{success:false}，/api/user/status 返有效数据
       → 解析出 30；下一轮稳态只请求 /api/user/status（1 次）
  ok   P1-B 全是 200+{} 时明确报错（不静默当 0）
  ok   裸 300 不被当成 300%          ← 旧的宽松 accept 也在这条上被覆盖
```

### 三、P1-C：IPC 调用点兜底清单（报告要的那张表）

报告问得好：「上一轮 P2-B 我指的是**这一整类**，不是这一行，你为什么只改了 `loadProviders`？」——**因为我把同类排查用在了"变量"上，没用在"调用点"上。** 上一轮那张 innerHTML/选择器清单做得完整，是因为报告直接点了"变量"这个词；而 P2-B 我理解成了"这一行没有 catch"，于是只补了那一行。这是同一个毛病换了个对象。

完整清单（`renderer.js`，7 个调用点，`grep -n "window\.api\." src/renderer/renderer.js` 可复现）：

| 行 | 调用 | 兜底 | 失败时的用户可见行为 | 覆盖它的测试 |
| --- | --- | --- | --- | --- |
| `:70` | `loadProviders()` | `.catch` + `#loadError` 横幅 | 横幅显示原因，UI 仍可用 | `dom` `loadfail` |
| `:74` | `getSecurityStatus()` | `.catch` 回落为"加密可用" | 不误报警 | 静态断言 |
| `:130` | `minimizeWindow()` | `.catch(() => {})` **（新增）** | 无操作，不留未处理 rejection | `dom` `ipcfail` |
| `:133` | `hideWindow()` | `.catch(() => {})` **（新增）** | 同上 | `dom` `ipcfail` |
| `:225` | `saveProviders()` | `persistAll` 内 try/catch **（新增）** | `#saveError` 显示原因；**不渲染未落盘的行**；表单保留 | `dom` `savefail` |
| `:262` | `deleteProvider()` | try/catch + **按原位置回滚** **（新增）** | 行不消失，`#saveError` 显示"删除失败" | `dom` `ipcfail` |
| `:493` | `fetchUsage()` | `pollOne` 内 try/catch（既有） | 行内显示错误 | `dom` `injection` |

实测：

```
  ok   保存失败横幅可见  [hidden=false]
  ok   横幅含 IPC rejection 原因  ["保存失败：EACCES: providers.json is read-only"]
  ok   未渲染"假保存成功"的行  [0]
  ok   表单内容保留（用户不用重输）  [Unsaveable]
  ok   删除失败后行回滚（UI 不显示未落盘的删除）
  ok   删除失败有可见提示  ["删除失败：EACCES: providers.json is read-only"]
  ok   窗口按钮 reject 不产生未处理 rejection
```

「未渲染假保存成功」这条是关键：`state.providers` 在 `persistAll` **之前**就被 push/assign 过，所以只要失败后不调 `renderAll()`，用户就不会看到一条重启后消失的订阅。

### 四、P3 级

**P3-E（README 数字）**：我没有把 14 改成 23 —— 而是**把条数从 README 里删掉**。理由是这类数字每轮都变，写死了就是等着漂；报告说"数字错了比不写更糟"，那就不写。改成「各自打印实际条数」+ 一句说明。并加了断言禁止复发：

```js
t('README 不重复测试条数（否则必然与实测漂移）', () => {
  notMatches(README, /报告条目回归 \d+/);
  notMatches(README, /IPC 端到端 \d+/);
  notMatches(README, /渲染断言 \d+/);
});
```

**P3-F**：`README.md:57` 的「`src/main.js` 里的解析逻辑」已改为「`src/lib/limits.js` 里的解析逻辑」。

**P3-G（断言强度）**：报告说 `report-items.test.js` 大多是源码文本匹配，"能防删掉、防不住逻辑变了"。同意，而且它举的例子很准：P1-3 的两处正则各自命中，但**不保证在同一函数里**。本轮把两条关键项升级成行为断言（`test/dom/main.js` 的 `behavior` 场景，用 stub 暴露的 `getFetchCount()` 数真实调用次数）：

```
DOM {"providerCount":4,"requestsForTwoPolls":4,"requestsAfterThresholdChange":0,"pctsUnchanged":true}
  ok   两次重叠轮询只发一轮请求（4 个 provider → 4 次）
  ok   阈值变更不发任何请求
  ok   阈值变更后读数由缓存重绘（未清空）
```

这比正则强得多：它真的调了两次 `pollAll()`，然后数 IPC 调用次数。文本断言保留作廉价护栏，但**关键条目的权威判据是行为断言**。

**P3-H**：两处 `elapsed < 350` 放宽到 `< 1000`，并注明计数断言（`assert.strictEqual(probed, 3)`）才是权威；新的先到先用用例保留 `< 300`，因为它有 10 倍余量（期望值 ~0ms vs 退化值 3000ms），不是靠裕度侥幸。

**P3-I**：`normalizeBalance` 现在只算一次 —— accept 里算出结果存进闭包变量，命中即用：

```js
// main.js:471-480
  let matched = null;
  const direct = await probeCandidates(provider, 'balance', balanceCandidates, {}, (res) => {
    matched = normalizeBalance(res.data);
    return matched != null;
  });
  if (direct && matched) return { ok: true, usage: { mode: 'balance', ...matched } };
```

安全性依赖一条实现约束：`probeCandidates` 在 `done = true` 之后**不再调用 accept**（`if (done) return` 在 accept 之前），所以 `matched` 不会被后来的慢候选覆盖。这条约束写进了注释，并被「稳态只发 1 个请求」和余额相关断言间接覆盖。

### 五、回答《四、要求下一轮反馈回答的问题》

**1. `Promise.all` 等最慢候选，是有意取舍还是没考虑到？** —— **没考虑到，而且我把取舍的方向搞反了。** 我有意用并发把最坏情况从 40s 压到 10s，但没想清楚并发 + `Promise.all` 的组合会把**典型情况**从「首个成功即返回」变成「等最慢」。报告给的实测（10ms → 8035ms）成立。已改为先到先用。「测试为什么给相同 delay」：见第一节——我在照着实现写测试，不是在找反例。

**2. 两套 accept 标准，哪个是笔误？** —— **`fetchOneAPIUserInfo` 那个宽松的是笔误。** `fetchBalanceUsage` 的「真解析出余额才算数」才是应有的标准。已统一为「调用方传入严格判据」，plan 用 `hasPlanLimits`，balance 用 `normalizeBalance != null`。

**3. 为什么只改了 `loadProviders`？请给调用点兜底清单** —— 清单见第三节（7 个调用点全列，含新增的 4 个）。原因写在那一节开头：我的同类排查只覆盖了"变量"，没覆盖"调用点"，因为报告 P2-B 我是当成"这一行缺 catch"读的。报告把「变量 / 调用点 / 新函数」三类并列，这个框架我接受，本轮三类各出了一张表。

**4. README 的数字以哪个为准？** —— **以命令输出为准，README 里不再写条数**（见 P3-E）。现在 README 只说明每个命令跑什么、会打印条数，避免二次漂移。

### 六、本轮新增 / 变更的函数（按规则，含反例测试）

| 函数 | 变更 | 断言数 | 反例（而不只是正例） |
| --- | --- | --- | --- |
| `probeCandidates` / `probeAll` | 重写为先到先用 | 5 静态 + 5 行为 | 首选 0ms + 慢候选 3000ms；记住路径失效后重探；空候选数组返回 null |
| `hasPlanLimits` | 新增 | 2 静态 + 3 行为 | 200+{} 不被接受；全是 200+{} 时明确报错；裸 300 不被当 300% |
| `fetchOneAPIUserInfo(provider, accept)` | 签名变更 | 2 静态 | 严格判据缺失时不得通过（`typeof root === 'object'` 宽松式已断言不存在） |
| `fetchBalanceUsage` | accept + 解析一次 | 2 行为 | 余额候选全 404 → 落到 user-info；`matched` 不被慢候选覆盖 |
| `persistAll` | 加 try/catch | 2 静态 + 3 行为 | saveProviders reject → 横幅可见、不渲染假行、表单保留 |
| `deleteProvider` | 加 try/catch + 回滚 | 1 静态 + 2 行为 | deleteProvider reject → 行回滚、有提示 |
| 窗口按钮两个 handler | 加 `.catch` | 2 静态 + 1 行为 | reject 不产生未处理 rejection |

### 七、测试总量与一处测试自身缺陷

| 套件 | 条数 | 结果 | 命令 |
| --- | --- | --- | --- |
| 语法 | 4 | 4/4 | `node --check` |
| 报告条目回归 | 61 | 61/61 | `npm test` |
| 纯函数单元 | 37 | 37/37 | `npm test` |
| IPC 端到端 | 26 | 26/26 | `npm test` |
| 实机 DOM（5 个场景） | 16 + 3 + 5 + 5 + 3 + 6 | 38/38 | `npm run test:dom` |
| **合计** | **166** | **166/166** | `npm test && npm run test:dom` |

**加 `behavior` 场景时暴露了一个测试自身的缺陷**，一并说明：该场景会把余额阈值改成 80，而阈值存在 `localStorage` 里 —— 于是后跑的 `injection` 场景读到被污染的阈值，`bar 宽度成正比` 断言从 50 变成 39.3 而失败。修法是每个 DOM 场景用独立的 `userData` 目录（`app.setPath('userData', <tmp>/llmb-dom-<pid>)`），跑完清理。**这类"测试之间通过持久状态互相污染"如果不修，会在将来变成随机失败**，比单条断言错更麻烦。

### 八、未做 / 未验证

- **真实桌面点击托盘**：handler 序列已覆盖，但真实 Shell 通知区行为需人在真机点一次（沿用上轮结论）。
- **真实服务商 JSON 仍缺**：字段映射是启发式。特别是严格 accept 之后，若某服务商的限额字段名不在 `FIVE_HOUR_SPEC` / `WEEKLY_SPEC` 里，会被判为"无候选被接受"并报「找不到 5h/周 限额字段」——行为是诚实的（不猜），但**适配新服务商仍需真实 JSON**。
- **`report-items.test.js` 仍以源码文本断言为主**：按 P3-G 只把两条关键项升级为行为断言，其余保留文本匹配作廉价护栏。若复审方认为某几条也该升级，请点名。
