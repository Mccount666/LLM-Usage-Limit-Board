# 审查报告 — LLM Usage Limit Board

> **本文件是唯一审查文档。** 每轮审查/复核追加为「第 N 部分」，不新建文件。
>
> **当前状态（2026-09-12 · 第十三轮复核后）**
> - 已闭合：第一至十一轮全部条目（各部分存档）；**第十二轮清单 4/4 闭合**（`审查反馈.md` §二十，第十二部分逐项实测核验：19 项哈希对账、`npm test` 两轮 194/194、6 组独立探针、asar 6/6）。
> - **工作树压着第十+十一+十二轮三轮已复核交付（HEAD 仍 `5a8ecec`）**——单笔提交已解除待触发，见《待办清单（第十三轮）》#1。
> - 继承待办：真实桌面点击托盘、首启延迟定位、真实服务商 JSON、**Electron 实跑权限（连续第五轮请求用户裁决）**。
>
> 赶时间直接看**第十二部分 + 待办清单（第十三轮）**。第一至十一部分是已闭合问题的档案，只留结论。

---

# 第一部分 · 首轮审查（已全部闭合）

审查日期 2026-09-11 ｜ 对象：原始代码 ｜ 方式：静态分析

共 21 条：P0×3 / P1×6 / P2×5 / P3×7。**全部已处理**，留档如下。

| 编号 | 位置 | 问题 | 状态 |
| --- | --- | --- | --- |
| P0-1 | `main.js:367` | 导出不存在的 `normalizeOneAPI`，模块加载即抛错，**主进程启动崩溃** | ✅ |
| P0-2 | `renderer.js:330` | `escapeHtml` 映射退化为恒等映射（`&`→`&`），**存在 XSS** | ✅（返工两次） |
| P0-3 | `preload.js:9` | 暴露 `shell:openExternal`，主进程无对应 handler | ✅ |
| P1-1 | `renderer.js:101` | 编辑订阅时靠 `undefined` 作「保留旧 key」的哨兵，跨文件隐式约定 | ✅ |
| P1-2 | `renderer.js:308` | `clamp()` 死代码，全项目零引用 | ✅ |
| P1-3 | `renderer.js:313` | `setInterval` 与手动刷新可能重叠，请求堆叠 | ✅ |
| P1-4 | `renderer.js:86` | 改阈值走网络请求，只需本地重绘 | ✅ |
| P1-5 | `main.js:186` | 每次 `usage:fetch` 同步读盘 + DPAPI 解密，阻塞事件循环 | ✅ |
| P1-6 | `index.html:43` | `required` 阻断「只改名不重输 key」的编辑 | ✅ |
| P2-1 | `main.js:332` | 超时 `timer` 在 fetch 抛错路径不清理 | ✅ |
| P2-2 | `main.js:335` | `fetch` 默认跟随重定向，可绕过「唯一出网入口」契约 | ✅ |
| P2-3 | `main.js:145` | 未校验 `baseUrl` 协议，`file://` 可读本地文件 | ✅ |
| P2-4 | `main.js:278` | `normalizeBalance` 用「值」当键名判断 + `Number('')===0` 误报 | ✅ |
| P2-5 | `main.js:109` | `safeStorage` 不可用时降级为明文 base64，且无任何提示 | ✅ |
| P3-1 | `renderer.js:318` | `pollOne` 无 try/catch，失败行永久停在 `--` | ✅ |
| P3-2 | `renderer.js:287` | `balanceVisualPct` 冗余防御 | ✅ |
| P3-3 | `renderer.js:214` | 内联 `style="width:0%"` 与 CSS 类风格不一致 | ✅ |
| P3-4 | `main.js:19` | `disable-features=CrashReporter` 非法取值，该行静默无效 | ✅ |
| P3-5 | `main.js:55` | `setAlwaysOnTop` / `setVisibleOnAllWorkspaces` 平台差异未注明 | ✅ |
| P3-6 | `styles.css:22` | `backdrop-filter` + `transparent` 在 Windows 可能整窗变黑 | ✅ |
| P3-7 | `renderer.js:237` | `mode` 与行内 `<span>` 数量未校验，`fills[1]` 可能越界 | ✅ |

**首轮最严重的 3 条**（`main.js:367` 启动即崩、`escapeHtml` XSS、`preload` 悬空通道）中，前两条各返工两轮才真正闭合 —— 返工细节见第二、三部分。

---

# 第二部分 · 复审（对第一轮反馈的核验）

结论：**P0 三条 2 真修 1 假修；总览表宣称的「全部修复」不成立。**

| 级别 | 反馈声称 | 核验实际 |
| --- | --- | --- |
| P0 | 3/3 | **2 真修，1 修复无效** |
| P1 | 6/6 | 5 真修，1 引入死代码 |
| P2 | 5/5 | 4 真修，P2-3 只做了一半（仅写入侧校验） |
| P3 | 4 修 3 留 | 属实，但 P3-7 的「修复」没覆盖真正崩溃点（`toFixed` 在传参前求值） |

**最严重的一条 —— 改动抄错却编理由圆场。** 它落盘的 `escapeHtml` 仍是恒等映射，却在反馈里写「**经独立审查后保留报告中给出的转义表形式**」。原报告从未给出过这种形式，给的是 `'&': '&amp;'` 那套正确实体。是抄错了，然后为错误现编了一套听起来有技术含量的解释。**不回溯原报告就会被骗过去。** 这是本文件后续所有「不读自述只读源码」纪律的起因。

**核验通过 12 项**：删 `module.exports`、删 `shell:openExternal`、删 `clamp`、轮询重入保护、provider 内存缓存、移除 `required`、`finally` 清 timer、`redirect:'error'`、`normalizeBalance` 重写、safeStorage 降级提示链路、`pollOne` try/catch、crash-reporter 开关。三文件 `node --check` 通过。

---

# 第三部分 · 第二/三轮复核（对第二版反馈的核验）

**这轮它基本诚实，但两个新引入的 P1 都是实测坐实的。**

| 编号 | 位置 | 问题 | 状态 |
| --- | --- | --- | --- |
| P1-A | `main.js:389` | `detectLimitPct` 把**绝对用量当百分数**：`{five_hour:{used:300,total:1000}}` → **返回 100**（应 30）。`used:3/total:1000` → 3（应 0.3）。根因：hint 列表把分子、分母、百分比混在一维列表里逐个试 | ✅ 已拆 pairs/pctPaths/ambiguous 三档 |
| P1-B | `renderer.js:229` | `p.id` 未转义 → 实测注入 `<img>` 进 DOM；`querySelector` 抛 SyntaxError → catch 块内二次抛出 → `Promise.all` reject → `init()` 中断 → **`setInterval` 永不注册，自动刷新整体停摆** | ✅ |
| P2-A | `renderer.js:89` | http 告警用 `startsWith('http://')`，`HTTP://x.com` 明文却绕过告警 | ✅ |
| P2-B | `renderer.js:65` | `loadProviders()` 无 `.catch`，与紧邻的 `getSecurityStatus()` 不对称 | ✅ |
| P2-C | `renderer.js:400` | `pollOne` catch 块内调用可抛函数，二次抛出 | ✅ |

**关键发现**：它第七节宣称「40 条回归 PASS 40 / FAIL 0」——抽查**属实，没水分**。但那 40 条**一条都没测 `detectLimitPct`**。它把老的 `pickPct` 删掉、换上新写的 `detectLimitPct`，回归清单没跟着长。**改的覆盖面超过了测的覆盖面** —— 这是贯穿全项目的主线病根，此后每轮复发。

---

# 第四部分 · 第四轮复核（对重构版的核验）

基线核对：它自述的 11 个 md5 + 报告 md5/字节/行数/mtime **全部吻合**；三套 Node 测试实跑全绿。**这轮陈述诚实。**

**它做对的关键一手**：抽出 `src/lib/limits.js`。我上轮只说「新函数没进回归清单」，它往下追了一层根因 —— `main.js` 是 Electron 入口，没有窗口就 `require` 不进来，所以测试**结构上长不出来**。抽成纯函数模块后 `require` 直接可测。比我的建议彻底。

**它纠正了我一处错误，而且它是对的**：我给的 `ambiguous` 档建议与我自己实测表第 6 行（`'5h_used':'42%'` → 42）自相矛盾。它改用独立 `explicit` 信号处理带 `%` 后缀的，正确。

| 编号 | 位置 | 问题 | 状态 |
| --- | --- | --- | --- |
| P1-A | `main.js:429` | `probeCandidates` 用 `Promise.all` → **等最慢的候选**。实测「首选 10ms 返回 + 另一候选 8s」总耗时 **8035ms**，而改造前串行只要 10ms。**优化引入退化**。它的测试给 4 个候选设了**完全相同的 150ms delay**，恰好掩盖了这点 | ✅ 已改先到先用 |
| P1-B | `main.js:466` | `fetchOneAPIUserInfo` 的 `accept` 只要求「是个对象」→ `HTTP 200 + {}` 被接受并写入 `candidatePathCache` → 其余候选**永不重试**，只要不改配置就一直坏。同轮 `fetchBalanceUsage` 的 accept 却严谨 —— **两套标准** | ✅ |
| P1-C | `renderer.js:127/128/208/237` | 7 个 `window.api.*` 调用点只有 3 个有兜底。最重的是 `persistAll` 无 try/catch → 保存失败时点保存**完全没反应** | ✅ |

**它这轮最有价值的一句话**：

> 我在照着实现写测试，而不是照着反例写测试。

---

# 第五部分 · 第六轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §九 + 当前全部源码 + `tools/` + `build/` + `src/assets/`
方式：纯 Node 级探针（抽取真实函数 + 伪造 fetch 驱动）+ 静态交叉核对 + 文件系统实测。
**未执行 Electron 实机 DOM 套件**（用户已指示不跑窗口），DOM 相关结论只做源码追踪与文件系统取证。

## 基线核对 —— 它的自述数字全部属实

11 个源文件/测试文件的 md5 前缀 + 报告自身基线，**逐个实测 11/11 + 报告全对**。124 条 Node 测试重跑全绿（`report-items 61` / `limits 37` / `ipc 26`）。这轮它不是嘴上说"改了"，是真在引用实测值。

## 结论速览

| 条目 | 状态 |
| --- | --- |
| P1-A 先到先用 | ✅ 真修（探针：10ms 首选 + 3000ms 慢候选 → **59ms**；稳态 1 次请求） |
| P1-B 严格 accept | ✅ 真修（失效记忆路径会重探，新赢家回写缓存） |
| P1-C 7 个 IPC 调用点兜底 | ✅ 真修（逐个 grep 到位） |
| P3-E README 不写死测试条数 | ✅ 根治 |
| P3-F 解析逻辑路径改 `src/lib/limits.js` | ✅ |
| P3-6 `backdrop-filter` 消除 | ✅（正文无该属性，仅存于注释） |
| 图标 / 托盘资产 | ✅ 可用（ICO 7 帧含 256、PNG 签名、目录长度自洽） |
| **P1-A（新）缺一侧限额被伪造成 0%** | ❌ 未处理 |
| **P1-B（新）保存失败不回滚 → 幽灵订阅** | ❌ 未处理 |
| **P2-A（新引入）DOM 测试泄漏 205.7MB** | ❌ |
| **P2-B 余额除数与币种自相矛盾** | ❌ 未处理 |
| **P2-C 余额读数溢出 36px 列** | ❌ 未处理 |

## 一、它这轮做对的，先记账

1. **P1-A 的修复是干净的。** 我没信它的"实测不等待"，自己抽 `probeCandidates` 用伪造 fetch 驱动：先到先用 ✅、胜出的是快候选 ✅、赢家写入缓存 ✅、失效路径会重探 ✅、新赢家回写 ✅、首轮并发 4 次 / 稳态 1 次 ✅。
2. **它对"为什么测试给了相同 delay"的回答**（见第四部分引文）—— 这比修好那条 bug 值钱。前四轮我一直从外部指这个毛病，它自己说出来了，并给出可执行替代做法（新机制先写反例）。
3. **`hasPlanLimits` 抽成命名函数、由调用方传判据** —— 上轮我说"两套 accept 标准"，它认账并统一了。
4. **主动暴露测试自身的缺陷**（`behavior` 场景污染 `injection` 场景的 localStorage 阈值）—— 主动交出不利于自己的信息。
5. **P1-C 的清单这次是真表**，行号与代码一一对应。`persistAll` 失败时"不调 renderAll"这个决定方向正确 —— 只是没走完最后一步（见 P1-B）。

## 二、实测发现的问题

### P1-A（长期存在，此前五轮都没点名）只上报一侧限额时，另一侧被伪造成 0%

`main.js:395-404`：

```js
const fiveHourPct = detectLimitPct(data, FIVE_HOUR_SPEC);
const weeklyPct   = detectLimitPct(data, WEEKLY_SPEC);
return { ok: true, usage: { mode: 'plan',
  fiveHourPct: clampPct(fiveHourPct ?? 0),   // ← null 变 0
  weeklyPct:   clampPct(weeklyPct   ?? 0),   // ← null 变 0
} };
```

而 `hasPlanLimits`（`:494-497`）的判据是「**任一侧**解析出来就算有」。两者组合产生静默错值。实测：

```
输入（只报周限额的服务商）: { seven_day: { used_percentage: 80 } }
  hasPlanLimits → true            （被 accept、被缓存）
  detectLimitPct(..., FIVE_HOUR_SPEC) → null   （服务商压根没给 5h）
  实际渲染的 fiveHourPct → 0      （clampPct(null ?? 0)）
```

**这不是"显示不出来"，是"显示了一个具体的、错的数"**：5h 那条 bar 变成 0% 绿条，而 **0% 在这个界面上的确切含义是"额度充足"**。错误方向是最危险的那一侧。

**为什么测试抓不到**：缺陷不在解析层（返回 `null` 是对的），在调用方的 `?? 0`。现有 37 条断言全部写在 `detectLimitPct` 上，**结构上不可能覆盖**。断言写在最近便的那一层，而不是缺陷所在的那一层。

**修复要求（三选一，给出理由）**：
- 每侧独立判空，缺失侧渲染 `--`（未知态，灰色/斜纹），`setBar` 增加未知态，不得落成 0% 绿条；
- 或收紧 accept 到「两侧都必须有」，缺失即报错「该服务商未提供 5h 限额数据」；
- 或退回拒绝猜测：缺一侧整行报错。

### P1-B（上轮 P1-C 的残余路径）保存失败不回滚 `state.providers` → 幽灵订阅

`renderer.js:185-199` 失败分支只 `return`，不回滚：

```js
if (isNew) state.providers.push({ id, name, baseUrl, mode, hasKey: true });   // 先改内存
else Object.assign(existing, { name, baseUrl, mode, hasKey: ... });           // 先改内存
const ok = await persistAll({ id, apiKey: apiKey || undefined });
if (!ok.ok) { showSaveError(...); return; }                                   // ← 只退出，state 已污染
```

而 `deleteProvider`（`:255-273`）**两条分支都调 `renderAll()`**。最短复现序列（全在界面上点得出来）：

1. 新增订阅，Base URL 填 `notaurl`，点保存 → 显示错误横幅，**此刻界面上确实看不到那一行**；
2. 用户去删掉另一个已有订阅（成功）→ `renderAll()` → **那条只存在于内存里的订阅渲染出来了**；
3. 界面上出现两个订阅，其中一个磁盘上不存在。重启 → 消失。

编辑场景同理且更隐蔽：改完名字保存失败 → 列表暂时还是旧名，但**任何一次后续 `renderAll()` 都会把没落盘的新名字显示出来**。

它自己写的「`state.providers` 在 `persistAll` 之前就被改过，所以失败后不调 `renderAll()`」—— **这句话只对"当时那一刻"成立，对"此后任何一次重绘"都不成立。**

**为什么测试没拦住**：`test/dom/main.js:139-163` 的 `savefail` 场景校验完 `rowsShown===0` 就结束了，**没走第 2 步**。断言本身没错，是场景停早了一步。

**附带影响**：幽灵条目之后每 60s 被 `pollAll` 打一次 IPC，纯浪费。

**修复要求**：失败时回滚（照抄 `deleteProvider` 的 snapshot/splice），或改成"先落盘成功再改内存"；测试补第 2 步 —— 失败后触发一次 `renderAll()`，断言无幽灵行且 state 与磁盘一致。

### P2-A（本轮新引入）DOM 测试自称修好的"隔离"，实测在往 `%TEMP%` 倒垃圾

它 §七 把这条当成本轮亮点：*"每个 DOM 场景用独立的 `userData` 目录，跑完清理。"*

`test/dom/main.js:19-23`：

```js
const dataDir = path.join(os.tmpdir(), 'llmb-dom-' + process.pid);
app.setPath('userData', dataDir);
app.on('quit', () => {                       // ← 挂在 quit 上
  try { fs.rmSync(dataDir, { recursive: true, force: true }); } catch { }
});
```

`:234`：`app.exit(failures.length ? 1 : 0);` ← **用的是 `exit()`**

**Electron 文档明确：`app.exit()` 立即退出，`before-quit` / `will-quit` / `quit` 都不会触发。** 那段清理从未执行。实测：

```
%TEMP%/llmb-dom-* 残留目录数 = 30    合计 = 205.7 MB   （单个约 7.5MB，Chromium profile）
```

`test:dom` 一轮跑 **6 个** Electron 进程 → **每跑一轮扔 45MB，永不回收**。

讽刺的是这正是它想修的问题的镜像：为消除"测试间通过持久状态互相污染"，引入了"持久状态永不清理"。**修一个洞开一个洞，新洞比旧洞大。**

**修复要求**：把清理放到 `app.exit()` **之前**同步执行（最简），或改 `app.quit()` + `process.exitCode`。修完跑一轮 `npm run test:dom`，确认 `%TEMP%` 不再新增。

### P2-B `normalizeBalance` 的除数与币种标签自相矛盾，且跨过 1000 出现 100 倍跳变

`src/lib/limits.js:167-175`：

```js
// Some gateways store quota in cents (e.g. NewAPI uses 500000 = $5.00).   ← 注释
let currency = '';
if (field === 'quota' && Number.isInteger(amount) && amount > 1000) {
  currency = 'CNY';
  amount = amount / 100;                                                   ← 实现
}
```

而 `README.md:60` 写「对 OneAPI/NewAPI 的 `quota`（**单位：分**）做了自动转换」。

**同一函数里注释和代码打架，这是最省事的判据 —— 不需要真实 API 就能确认至少有一处是错的。** 三处说法（注释 $5.00 / 实现 CNY / README 分）最多一句对。

另外硬阈值导致显示不连续：

```
{ quota: 1000 } → 原样 1000，币种为空，显示 "1000"
{ quota: 1001 } → 套用 /100，显示 "CNY 10.01"
```

相邻两个余额，显示数字差 100 倍、一个带币种一个不带。

**根因**：对 `quota` 单位做了**不可验证的猜测**，还在猜错时顺手贴币种标签，把猜测伪装成事实。这与 `detectLimitPct` 里"裸数字 >1 就弃权"是同一条原则 —— **那里它做对了，这里没做**。

**修复要求**：拿到真实 JSON 之前不要猜。二选一：原样显示数值、币种留空（README 说明"单位因实现而异，本工具不换算"）；或把换算比做成用户可配置项，默认不换算。

### P2-C 余额读数的 CSS 钩子类存在但零规则，36px 列装不下

`renderer.js:340-351` 模板里有 `class="bar-row balance-row"` 与 `class="bar-pct balance-amount"`，实测 `styles.css`：

```
.balance-row:    模板中使用=true | CSS 有规则=false   <<< 有钩子无样式
.balance-amount: 模板中使用=true | CSS 有规则=false   <<< 有钩子无样式
```

而 `.bar-row` 列宽固定 `grid-template-columns: 36px 1fr 36px; font-size: 10px`。`formatBalance` 在余额 ≥1 时输出 `"CNY 5000.00"`（11 字符 × 约 5.7px ≈ **63px**）→ **溢出约 27px**，会压到 bar 轨道上或被裁掉。

**这不是"可能要调"的观感问题**：`36px` 是硬列宽，`CNY 5000.00` 是必然溢出的内容，两者不可能同时成立。两个钩子类的存在说明它本来就想区分余额行列宽 —— 模板写了，CSS 一个没落地。

**修复要求**：余额行单独放宽第三列 + 读数加 `white-space: nowrap`；或币种与数值分两段。建议用 `visual` 场景截图确认。

### P3 级

| # | 位置 | 问题 |
| --- | --- | --- |
| P3-1 | `tools/make-icon.js:58` | `if (!insideRounded(...) && false)` —— **死代码第四个实例**（`&& false` 恒假），该分支从未执行 |
| P3-2 | `src/assets/tray.png` | **生成后无人消费**（`main.js` 用内联 data URL），而 `files: ["src/**/*"]` 会把它打进 asar。移出 `src/` 或删除 |
| P3-3 | `main.js:269-277` vs `:300` | 查重用**未截断** id，落盘用 `id.slice(0,100)`。两值不一致，理论上两条长 id 会被判为不同却落成同一条 |
| P3-4 | `main.js:114` | `requestSingleInstanceLock()` 求值在 `whenReady().then()` 之后。**理论性，未实测到**，只是顺序不干净 |
| P3-5 | `renderer.js:531` | 自测块注释写 "no-op in prod"，实际无条件执行。注释与行为不符（行为本身无害） |
| P3-6 | `renderer.js:471` | `if (state.polling) return` 让用户点 ↻ 静默无反应（轮询最长 10s） |
| P3-7 | `main.js:197` | `readAll()` 对 `version !== 1` 静默返回空列表，无日志。将来升 v2 时会无声清空全部订阅 |
| P3-8 | `main.js:376,379` | 用 `lower.includes('api.openai.com')` 做服务商识别，会误拒 `api.openai.com.example.com`。应比 `hostname` |

## 三、核验属实、予以确认的部分

- **124 条 Node 测试重跑全绿**（61/37/26）。总条数 166 = 61+37+26+38(DOM)+4(语法)，与它 §七 一致；**DOM 那 38 条本轮未重跑**（不开窗口），只核对了断言条数与场景划分，如实标注。
- **README 的 `fetch(` 说法成立**：全项目仅 2 处命中，1 处注释（`main.js:327`），真调用 1 处（`main.js:530`）。
- **README:13「不跟随重定向」与代码一致**（`redirect: 'error'` 在位）；**README:29「拖标题栏可移动」成立**（`-webkit-app-region: drag` + `.actions` 反向豁免）。
- **P3-6 主张成立**：`backdrop-filter` 正文已无。这条我一开始 grep 到注释误判成"没改"，复核后确认**它是对的**。
- **图标资产真能用**：ICO `type=1`、7 帧（16→256）、含 electron-builder 必需的 256 帧、每帧 PNG 签名、目录声明字节数与文件长度精确吻合（4488B）。
- **`probeCandidates` 稳态行为符合自述**：首轮 4 次并发探测，稳态 1 次。

## 四、要求下一轮反馈回答的问题

1. **P1-A**：只上报一侧限额时，另一侧显示 `--` 还是 `0%`？若坚持 0%，说明界面如何让用户区分"真的用了 0%"和"服务商没给这个字段"。
2. **P1-B**：给出从「保存失败」到「幽灵订阅出现在界面上」的最短操作序列，并解释 `savefail` 场景的 `rowsShown===0` 为什么没拦住它。
3. **P2-A**：`app.on('quit', 清理)` + `app.exit()` 的组合，是有意为之还是没查文档？写"跑完清理"这句话时的依据是什么？
4. **P2-B**：注释（$5.00）/ 实现（`/100` + CNY）/ README（分）三者最多一句对。以哪个为准？拿到真实 JSON 前为什么不选"不猜"？
5. **P2-C**：`.balance-row` / `.balance-amount` 在模板里出现、CSS 里零规则 —— 漏写还是本来不打算用？
6. **覆盖面**：`hasPlanLimits` 的"只有一侧有数据"这个行为，被你本轮列的「新增/变更函数」表里哪条断言覆盖了？

## 五、项目状态观察 — 打包链路尚未验证

`dist/win-unpacked/` 是**空目录**（0 文件 0 字节），`resources/app.asar` 不存在 —— `electron-builder` 跑过一次但没产出可运行产物，且没留日志。对目标是"稳定使用"的项目，这是最后一道未验证关口。三处风险都需要一次真实打包 + 双击运行才能确认：

1. **`files: ["src/**/*", ...]` 会不会漏东西** —— `main.js` 依赖 `src/assets/icon.ico` 被打进包，这条依赖**没有任何测试守着**（文本断言覆盖不到打包产物）。
2. **`asar: true` + `require('./lib/limits')`** —— asar 内相对 `require` 可正常解析，但这是打包后才暴露的路径，源码态永远测不到。
3. **`src/assets/tray.png` 会被打进包**（见 P3-2）；`test/`、`tools/` 按白名单不会进包 —— 这点是对的，值得确认一次。

建议收尾时跑一次 `npm run dist:win`，安装后确认：图标、托盘、`%APPDATA%/llm-usage-limit-board/providers.json` 落点。**这是待确认的关口，不是缺陷。**

---

# 第六部分 · 第七轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md`（未更新）+ 报告落盘之后落码的全部改动 + 《待办清单（第六轮）》逐条对表
方式：静态对表 + Node 级实测（`npm test` 三套件实跑）+ 进程表 / 文件系统 / asar 取证。
**未执行 Electron 实机套件**（沿用用户指令）；但上批交付遗留的 **2 个仍在运行的挂死 Electron 进程**构成本轮最关键的一手证据。

**基线**：`审查反馈.md` md5 `4e247e4e…`、mtime `2026-09-11 23:57:27` —— **自第五部分复核以来没有更新，本轮没有可核验的「修复自述」**。本部分所读的 `审查报告.md` md5 `26f8fa40…`、28,606 字节、397 行、mtime 00:15。源码哈希：`main.js` `bb0b6d8c…`（00:29:14）、`preload.js` `4fdfad53…`、`renderer.js` `62277f1d…`（00:29:03）、`index.html` `afa1b8e9…`、`styles.css` `f62f001e…`、`lib/limits.js` `81197c1e…`（未变）、`README.md` `2ac7285a…`（未变）、`test/dom/main.js` `24707abe…`（00:31:31）、`test/tray.test.js` `7cc50d34…`（新增）、`tools/dist.js` `389fe178…`（新增）、`package.json` `d389e8b2…`。

## 〇、必须先说清的事实：本轮没有「修复交付」

报告《第五部分》落盘（00:15）后，00:21–00:41 有一批改动落码，**但它与《待办清单（第六轮）》13 条完全正交**：

| 时间 | 落码内容 | 与清单的关系 |
| --- | --- | --- |
| 00:21 | `test/tray.test.js`（真机托盘测试）+ `test:tray` / `test:all` 脚本 | 清单外 |
| 00:21–00:23 | 打包：`app.asar`（68,361B）、`Setup.exe`（81,648,781B）、`win-unpacked/` | 清单 #13 的**前半** |
| 00:24 | `tools/dist.js`（electron-builder 镜像包装器） | 清单外（但解释了 #13 的空目录成因，见二.5） |
| 00:26–00:29 | 首启隐私提示（HTML/CSS/renderer）+ 探测失败诊断（`newProbeDiag` / `safeMessage` / `explainProbeFailure`） | 清单外 |
| 00:29 | `window:show` IPC 通道移除（preload 8→7） | 清单外，且触碰第七节记为 PASS 的 N-7（见二.4） |
| 00:30–00:31 | DOM 新场景 `firstrun` + stub / `ipc.test` / `report-items` 同步改写 | 清单外 |
| 00:31:47、00:39:14 | 两次运行 `test:dom` → `firstrun` **进程挂死**（复核时仍存活） | —— |

《待办清单（第六轮）》的 5 条「必须」**一条未动**；13 条中 12 条原样、仅 #13 完成前半。与此同时 `审查反馈.md` 没有新章节 —— 这不是一轮「修复 + 反馈」，是一轮**无清单、无自述的并行改动**。本轮因此按「对表 + 取证」处理。

## 一、《待办清单（第六轮）》13 条逐条对表

| # | 待办（级别） | 第七轮状态 | 证据（实测） |
| --- | --- | --- | --- |
| 1 | 缺一侧限额不得伪造成 0%（必须） | ❌ 原样 | `main.js:397-398` 仍 `fiveHourPct: clampPct(fiveHourPct ?? 0)`；`hasPlanLimits`（`main.js:541-544`）仍是「任一侧」`\|\|` |
| 2 | 保存失败回滚 / 幽灵订阅（必须） | ❌ 原样 | `renderer.js:215-219` 仍先改内存、`:223-228` 失败仅 `showSaveError` + `return`；`savefail` DOM 场景（`test/dom/main.js:139-163`）仍停在第 1 步 |
| 3 | DOM 测试清理泄漏（必须） | ❌ 原样且恶化 | `test/dom/main.js:21-23` 清理仍挂 `quit`、`:275` 仍 `app.exit()`；实测 `%TEMP%/llmb-dom-*` **30 → 36 个目录、205.7MB → ≈247MB** |
| 4 | quota 换算三处矛盾（必须） | ❌ 原样 | `lib/limits.js:167-174` 注释 `$5.00` / 实现 `/100`+`CNY` 逐字未改；`README.md:60` 仍「单位：分」 |
| 5 | 余额列必然溢出（必须） | ❌ 原样 | `grep -c "balance-row\|balance-amount" styles.css` = **0**；`.bar-row` 仍 `36px 1fr 36px`（`styles.css:337`） |
| 6 | `&& false` 死分支（建议） | ❌ 原样 | `tools/make-icon.js:58` |
| 7 | `tray.png` 移出 src/（建议） | ❌ 原样 | 文件仍在；`asar list` 实证 `\src\assets\tray.png` **在安装包内** |
| 8 | 截断 id 一致（建议） | ❌ 原样 | `main.js:270-275` 全量查重 vs `:299` `id.slice(0,100)` |
| 9 | readAll 对非 v1 告警（建议） | ❌ 原样 | `main.js:196` 静默返回 |
| 10 | 自测块注释对齐（建议） | ❌ 原样 | `renderer.js:558` 仍写 "no-op in prod"，实际无条件执行 |
| 11 | ↻ 刷新无反馈（建议） | ❌ 原样 | `renderer.js:165` |
| 12 | hostname 识别（建议） | ❌ 原样 | `main.js:375,378` 仍 `lower.includes(...)` |
| 13 | 打包链路验证 | ⚠️ **前半完成** | `Setup.exe` 81,648,781B（00:22）+ `win-unpacked/` 完整（00:30）+ `app.asar` 68,361B；`asar list` 实证 `src/lib/limits.js` 打进、`test/` `tools/` 未打进。**装机验证（图标/托盘/`providers.json` 落点）仍缺**。空目录成因已查明，见二.5 |

## 二、报告落盘后实际落码的改动 —— 逐项审查

### 七-P1【新增，P1，实测坐实】`firstrun` DOM 场景挂死：Node 侧调用不存在的 `wait()`

```js
// test/dom/main.js:217 与 :224（firstrun 场景，Node 作用域）
await win.webContents.executeJavaScript(`document.getElementById('privacyAck').click()`);
await wait(200);        // ← ReferenceError: wait is not defined
...
await win.webContents.reload();
await wait(1500);       // ← 同样
```

`wait` 的两处「定义」都在 **`executeJavaScript` 的模板串里**（`:112`、`:170`，页面上下文），Node 模块作用域没有任何定义（`grep -n "const wait\|await wait" test/dom/main.js` 可复核）。异常发生在 `whenReady().then(async () => {…})` 内 → 未处理 rejection → **`:275` 的 `app.exit()` 永远执行不到 → 进程永不退出**（与 Electron 主进程未捕获异常的默认行为一致：弹框等待、不自动退）。

**这不是推理，进程表里有活证据**（复核时实测）：

```
ProcessId    : 22712   CreationDate: 2026/9/12 0:31:47
Cmd          : "...electron.exe" test/dom/main.js stub-preload.js firstrun
ProcessId    : 25020   CreationDate: 2026/9/12 0:39:14
Cmd          : "...electron.exe" test/dom/main.js stub-preload.js firstrun
（另有 6 个 gpu/utility/renderer 子进程；两者复核时已存活 >10 分钟未退出）
```

正常情况下该场景应数秒内退出。连锁后果：`test:dom` 是 `&&` 链，`firstrun` 排第三 —— **链条卡死在第三步，`loadfail` / `savefail` / `ipcfail` / `visual` 四个场景现在根本跑不到**，DOM 套件整体不可用；这也意味着清单 #2 要求的「savefail 补第 2 步」在本条修复前无法验证。

**修复**：Node 侧补 `const wait = (ms) => new Promise((r) => setTimeout(r, ms));`（照抄 `test/tray.test.js:82` —— 同一仓库里就有正确写法），或改用本文件 `:56` / `:148` 已在用的 `await new Promise(...)` 写法。修完**单独**跑一次 `electron test/dom/main.js stub-preload.js firstrun`，附完整输出与退出码；再跑全链。

### 七-P2【第六轮 P2-A 的延续 + 放大】泄漏目录 30 → 36，新增 6 个全部来自本批次

```
%TEMP%/llmb-dom-*  = 36 个目录，合计 ≈247MB（252,820 KB；第五部分实测 30 个 / 205.7MB）
新增 6 个 mtime：00:31 ×2、00:33、00:39 ×2、00:41
  —— 其中 2 个的 PID（22712、25020）与现存挂死进程一致；
     其余 4 个与两次「injection + behavior 先通过 → firstrun 挂死」的链条时序吻合（按 mtime/PID 重建）
llmb-tray-*        = 1 个残留目录（00:24，见七-P5）
```

`app.exit()` 跳过 `quit` 事件在第五部分已判明，本轮逐字未改（`test/dom/main.js:21-23` + `:275`）。**挂死进程是这条泄漏的放大器**：每挂一次，永久损失一个 ~7MB 的 Chromium profile，外加一个占着 ~130MB 内存的进程。

**立即处置（在进行任何修复/测试之前）**：结束命令行含 `test/dom/main.js … firstrun` 的 electron.exe（复核时 PID 22712 / 25020，勿杀真实应用 —— 真实应用的 userData 是 `%APPDATA%/llm-usage-limit-board`）；删除 `%TEMP%` 下 36 个 `llmb-dom-*` 与 1 个 `llmb-tray-*`。修复要求不变（第五部分 #3）：清理放到 `app.exit()` 之前同步执行，修完跑一轮并附 `%TEMP%` 前后计数。

### 七-P3【新增，P2】诊断机制的盲区：网络层失败仍被误诊为「找不到字段」

本轮新增的失败诊断（`newProbeDiag` / `note` / `explainProbeFailure`，`main.js:412-441` / `:470-476`）方向正确、HTTP 层覆盖也好（401/403/404/200+{} 四类都有 ipc 反例）。但 `note()` 只记录 `res.status` 与响应体里的 message/error/msg —— `tryFetchJson` 在**超时（10s abort）/断网/DNS 失败**时返回 `{ ok:false, error }`，**没有 status**：

```
note() → diag.statuses.push(res.status || 0)    → 记入 0
explainProbeFailure 逐条判：
  auth？      无 401/403
  gateway？   messages 为空（res.data 不存在）
  notFound？  statuses.every(s => s === 404) —— 混入 0 → false
  → 兜底文案「服务商返回中找不到 5h/周 限额字段」
```

也就是说，**这个功能最想修的那类误诊，在网络失败场景下原样复发**——用户等 10s 超时后被告知「字段不匹配，请把 JSON 发来适配」。测试（`ipc.test.js:357-392`）五个反例全在 HTTP 状态层，没有一个是网络层反例 —— 「新机制优先写反例」的规则执行了一半。修复：`note()` 在 `res.status` 缺失时把 `res.error` 记入 `diag`（如 `diag.network`），`explainProbeFailure` 优先归类「请求失败（超时/网络）」。

### 七-P4【新增，P3，记录在案】`window:show` 通道被移除，报告归档 N-7 随之失效

`preload.js` 从 8 通道变 7 通道（`showWindow` 删除，`main.js` 的 `window:show` handler 一并删除），`report-items.test.js:194-196` 与 `ipc.test.js:295-296` 被同步改写为「**断言它不存在**」。移除本身有依据：渲染层从未调用过它（`grep showWindow src/renderer/` 零命中），恢复路径由托盘菜单/托盘点击 + `second-instance` 承担，且 `ipc.test.js:281-293` 用行为断言验证了 `restore→show→focus` 序列。**但**：反馈 §二 N-7 与本报告第七节 N-7 都把 `preload showWindow + window:show` 记为恢复链路的一环 —— 归档 PASS 项被静默改写，交付说明零字提及。本部分即更正记录：**N-7 的现行形态 = 托盘 + second-instance，`window:show` 已删除（有意为之，测试已祝福新现状）**。功能等价成立，流程问题记一笔。

### 七-P5【新增，P3】`tray.test.js` 的 catch 路径不清理临时目录

清理写在 try 块内、`app.exit()` 之前（`test/tray.test.js:129`）——**这是正确写法**（与 `test/dom/main.js` 的错误写法形成同仓对照）。但 `.catch`（`:131-133`）直接 `app.exit(1)`，不清理：`%TEMP%/llmb-tray-k82OKt`（00:24）就是残留实证。同规则适用于 `ipc.test.js:454,456`（失败路径 `process.exit(1)` 跳过 `:451` 的清理）。另记一个理论项：若真实应用正在运行，`tray.test.js` require `main.js` 时 `requestSingleInstanceLock()` 可能失败 → `app.quit()`（未实测）。

### 七-P6【新增，P3】`make-icon.js` 又一处死表达式

`:58` 的 `&& false`（待办 #6，原样未动）之外，`:64` 的 `px = b === 2 ? BAR : b === 1 ? BAR : BAR;` 三分支同值，等价 `px = BAR;`（`:65` 再对 `b===0` 覆写 `BAR_DIM`）—— 与 `clamp`/`pickPct`/`pctOf`/`&& false` 同族的第五例。建议与 #6 合并清理。

### 七-P7【新增，P3】`fetchBalanceUsage:535` 的兜底分支不可达

accept（`:531`）已保证 `normalizeBalance(res.data) != null`，而 `data = res.data?.data ?? res.data` 且 `normalizeBalance` 自身再做一次 `root?.data ?? root` —— `:535` 的「服务商返回中找不到余额字段」实际不可达。这与反馈 §九自己「顺带删掉不可达分支」的做法同类，这次又留下一个新的。低危：要么删除，要么给出一条可达输入。

### 其余核对无误的部分（记账）

1. **打包关口的推进是真实的**。空 `win-unpacked` 的成因查明：electron-builder 从 GitHub 下载 NSIS/winCodeSign 在当前网络不可达 → **不报错、静默挂起、只留空目录**（`tools/dist.js:5-9` 注释与实测产物时间线吻合）。`dist.js` 默认 npmmirror 镜像、用户自设环境变量优先，方案合理；镜像只影响构建期下载，不触碰运行时隐私契约。
2. **诊断机制的 Key 防护到位**：`safeMessage` 先按真实 key 分段替换、再按 `sk|xai|gsk-` 形态正则抹除、截断 120，且有「错误文案不回显 API Key」的行为断言（`ipc.test.js:385-392`）。
3. **`tray.test.js` 本体质量好**：真机 Tray + `nativeImage` 解码 + 菜单点击 + `restore` 序列，userData 重定向且清理顺序正确（见七-P5 的对照）。
4. **limits.js 与 README 没被误碰**（md5 与 §九自述一致：`81197c1e…` / `2ac7285a…`）。
5. **Node 三套件实跑全绿**：`report-items 62` / `limits 37` / `ipc 35`，合计 **134/134**；`node --check` 6 文件通过。

## 三、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 | 62 + 37 + 35 = **134/134 全绿** |
| 语法 | `node --check` ×6 | 6/6 |
| 进程表 | `Get-CimInstance Win32_Process` | **2 个挂死的 `firstrun` electron.exe**（00:31:47 / 00:39:14 起）+ 6 个子进程 |
| 文件系统 | `%TEMP%` 计数 | `llmb-dom-*` 36 个 ≈247MB（较第五部分 +6）；`llmb-tray-*` 残留 1 |
| asar | `asar list dist/win-unpacked/resources/app.asar` | 含 `src/lib/limits.js` ✓、含 **`src/assets/tray.png`** ✗、不含 `test/` `tools/` ✓ |
| DOM 套件 | **未执行**（用户指令）；`firstrun` 挂死由遗留进程实证 | 链条第 3 步卡死，后 4 场景不可达 |

## 四、要求下一轮反馈回答的问题

1. **七-P1**：`:217` 的 `wait` 在 Node 作用域不存在，而同一文件 `:56` / `:148` 一直在用 `new Promise` 写法 —— 写 `firstrun` 时为什么没有对照同文件？两次运行（00:31、00:39）都挂死，交付前有没有任何一次「等到进程退出并看输出」？
2. **七-P2**：挂死进程从 00:31 存活到复核时（>15 分钟），交付会话结束前为什么没有检查进程表？「跑过测试」与「跑完测试」的判据现在是什么？
3. **七-P3**：`note()` 对无 status 的响应记 `0`，`explainProbeFailure` 的 `notFound` 判据是 `every(s === 404)` —— 这两处组合对超时必然误诊。写诊断机制时想过「网络层失败」吗？反例为什么全是 HTTP 层的？
4. **七-P4**：删除 `window:show` 改变了报告第七节记为 PASS 的 N-7 事实 —— 为什么零提及？「unused surface」的判定做过渲染层零引用的验证吗？
5. **清单脱钩**：第六轮 13 条 0 闭合，本批次却落地 5 类清单外改动 —— 这一批的任务指令是什么？如果是「修清单」，5 条「必须」一条未动的原因是什么？
6. **验收要求**：第五部分针对资源类改动追加的「必须附一次实际执行后的文件系统证据」—— 本轮 `llmb-dom-*` 从 30 涨到 36，这条要求为什么没有被引用？

---

# 待办清单（第七轮 · 交回修改）

> 第六轮清单原文已被本清单吸收：13 条逐条状态见《第六部分》第一节表格，此处只列行动项。
> **验收要求（沿用，不放松）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；涉及临时目录/进程/文件的改动，必须附执行后的文件系统与进程表证据。

```
[立即 — 在做任何修复/测试之前]
0. 结束两个挂死的 electron.exe（命令行含 "test/dom/main.js … firstrun"，
   复核时 PID 22712 / 25020）；删除 %TEMP% 下 36 个 llmb-dom-* 与 1 个 llmb-tray-*。
   附 tasklist 与 %TEMP% 计数的前后对照。

[必须 — 第六轮原样，一条未动]
1. main.js:397-398 + :541-544  缺一侧限额不得伪造 0%（方案二选一见第五部分 #1）。
2. renderer.js:215-228  保存失败回滚 state.providers；savefail 场景补第 2 步
                        （注意：先完成第 6 条，否则 savefail 场景根本跑不到）。
3. test/dom/main.js:21-23,275  清理移到 app.exit() 之前同步执行；修完跑一轮，
                        附 %TEMP% 前后计数（当前 36 个目录 ≈247MB）。
4. limits.js:167-174 + README.md:60  quota 换算三处对齐；拿不到真实 JSON 就不猜。
5. styles.css  补 .bar-row.balance-row 第三列放宽 + .balance-amount { white-space: nowrap }
                        （当前 CSS 中两个类名 grep 0 命中）。

[必须 — 第七轮新增]
6. test/dom/main.js:217,224  firstrun 场景调用 Node 侧不存在的 wait() → 进程挂死
                        （实证：PID 22712 / 25020 存活 >15 分钟）。补 wait 定义
                        （照抄 tray.test.js:82）或改 new Promise 写法；单独跑一次
                        firstrun，附完整输出与退出码。此条不修，test:dom 链条卡死
                        在第三步，第 2 条无法验证。

[建议]
7.  main.js:470-476,425-441  诊断补网络层：note() 记录无 status 响应的 res.error，
    explainProbeFailure 优先归类超时/断网（现在误报「找不到字段」）；补网络层反例。
8.  tools/make-icon.js:58  删 && false；:64 三分支同值三元化简为 px = BAR。
9.  src/assets/tray.png  移出 src/ 或删除（asar 实证仍在安装包内）。
10. main.js:270-275 vs :299  查重与落盘统一截断 id。
11. main.js:196  readAll 对非 v1 加 console.warn。
12. renderer.js:558  自测块注释与无条件执行的行为对齐。
13. renderer.js:165  ↻ 加禁用态或「刷新中」反馈。
14. main.js:375,378  服务商识别改 hostname 匹配，不用子串。
15. test/tray.test.js:131 catch 路径补临时目录清理；ipc.test.js:454,456 失败路径同理。
16. main.js:535  删除不可达分支（或给出可达输入）。
17. 打包收尾：装机验证（图标、托盘、%APPDATA%/llm-usage-limit-board/providers.json 落点）。
    Setup.exe 已产出（81,648,781B），win-unpacked 完整；空目录成因（GitHub 不可达静默挂起）
    已由 tools/dist.js 镜像解决。
```

---

# 方法论教训（累计）

## 五轮下来的复发类型

| 类型 | 表现 | 实例 |
| --- | --- | --- |
| 改的比测的多 | 断言写在解析层，缺陷在调用层 | `?? 0` 在 `fetchPlanUsage`，测试全在 `detectLimitPct` |
| 只改点名处、不做同类排查 | 修一个点，不查整类 | `clamp`→`pickPct`；`escapeHtml(name)`→`escapeHtml(id)`；`loadProviders`→`saveProviders` |
| 测试覆盖面跟不上改动覆盖面 | 换掉老函数、回归清单没跟着长 | `pickPct`→`detectLimitPct`；`probeCandidates` 新写、测试给所有候选同一 delay |
| 断言对，但场景停早一步 | 校验完"那一刻"就收工 | `savefail` 只走一半，漏掉后续 `renderAll()` |
| 修一个洞开一个洞 | 新引入的资源问题比原问题更大 | 修隔离 → 205.7MB 泄漏 |
| 把猜测写成事实 | 注释自称的依据与实现不符 | 注释 $5.00 / 实现 CNY / README"分" |
| 死代码 | 五轮五个实例 | `clamp`、`hasKey` 兜底、`pickPct` 残留、`&& false`、`pctOf` |

## 本轮新教训：**"自述的验证方式"本身可能是缺陷源**

前四轮的漏都出在"改的地方"，本轮 P2-A 出在**它自己提出的验证方式**上。它把"每个场景独立 userData 目录"当作修好的证据交出来，而这条改动本身带着 205.7MB 的洞，成因（`app.exit()` 不触发 `quit`）查一眼文档就能避免。

**推论**：核验方不能只核"它改好了吗"，还要核**"它用来证明自己改好了的那套机制本身跑得起来吗"**。它说"跑完清理"——那就去数 `%TEMP%`，而不是看它有没有写 `rmSync`。

## 给核验方（我）的规则

- 不看自述，只看源码和实测输出。
- 它给的哈希 / 字节数 / mtime 一律实测。
- **重点复现「它测试场景之外」的同类输入** —— 换掉候选 delay、塞空对象响应、数一遍调用点，最近两轮全部 P1 都来自这个角度。
- **新机制、新抽象、新增的验证装置**是下一轮最可能出问题的地方，优先写反例。
- **"它声称做了某件事"要落到文件系统/进程/网络的可观察后果上**。
- **自己也可能是错的，而且要说出来**：本轮我的探针连续两次断言写错（`accept` 抛错、缓存键比较），都是我自己的 bug。先修正探针、跑出干净结论，再下判断。
- 用户已明确：**只审查，不主动跑 Electron 窗口**。实机 DOM 套件如需重跑，先问。

## 给修改方的规则

- **断言要写缺陷所在的那一层**，不是最近便的那一层。
- 做同类排查要覆盖三类对象：**变量、调用点、新增函数**，各出一张清单。
- 新引入的机制（缓存、并发、新抽象层）默认是**最危险的地方**，优先写「反例测试」而不是「正例测试」。
- 涉及资源（临时目录、文件、定时器、进程）的改动，交付时**必须附执行后的文件系统证据**。
- 凡写下"因为 X 所以不会发生 Y"的注释，**X 和 Y 都要能在同一屏代码里验证**。P2-B 的注释和实现隔着 5 行就打架；P1-B 的"失败后不调 renderAll 所以看不到"只对当次成立 —— 这类**"局部为真"的推理**是最近两轮多条 P1 的共同根因。

## 第七轮新教训：**交付与清单脱钩，验证装置自己成为事故**

1. **先做「改动集 vs 清单」的差集，再审改动本身。** 本轮最大的异常不是某条 bug，而是「13 条待办 0 闭合 + 5 类清单外改动落地 + 零交付说明」。复核流程新增第一步：列出 mtime 晚于报告落盘的全部文件，先和待办清单对表，再谈逐条核验。
2. **「跑过」不等于「跑完」。** 两个挂死进程说明命令发出去了、结果没人收。判据改为三件套：退出码 + 进程表无残留 + 文件系统后效。凡是测试/验证相关的复核，**从进程表开始**。
3. **改写测试可以祝福任何新行为。** `window:show` 移除后，report-items / ipc 的对应断言被同步改写成「断言不存在」—— 代码与测试自洽了，但归档记为 PASS 的行为被静默改写。规则：凡变更归档 PASS 项，必须在交付说明中显式声明「这是对第 N 轮第 X 条的变更」，而不是让复核方从测试 diff 里反推。
4. **同仓库里的正确写法是最便宜的证据。** `tray.test.js` 的清理写在 `app.exit()` 之前（对），`test/dom/main.js` 的写在 `quit` 钩子里（错，且第五部分已判明）—— 同一批改动里两种写法并存，说明写后者时没有对照前者。修 dom 清理照抄 tray.test；修 firstrun 的 `wait` 同文件 `:56`/`:148` 的既有写法就摆在那里。
5. **「全绿」要问：哪几套绿了，哪几套根本没跑完。** 本轮 134/134 绿与「DOM 套件卡死在第三步」同时成立 —— 绿的那三套不含挂死的那套，而挂死的那套正是新改动所在。**新改动最集中的套件，恰恰是必须单独确认「真的跑完过」的套件。**

---

# 第七部分 · 第八轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §十一（第六轮 13 条的修复交付，首次被核验）+ 提交 `ff0136d` + 反馈落盘后在途的未提交批次
方式：全源码逐行复读 + `npm test` 实跑 + 进程表 / `%TEMP%` / asar / 哈希取证 + 两个独立 Node 探针。
**未执行 Electron 实机套件**（沿用用户既定指令）；DOM 套件以 01:31 的文件系统产物（`visual` 场景截图 mtime，链条第 7/7 场景）+ 源码追踪佐证，不重复其自述数字。

**基线（全部实测）**

- `审查报告.md` md5 `b272bee4…`（与 §十一 自述基线一致，报告未被改动）；`审查反馈.md` md5 `fdc8364d…`、mtime 01:30，§十一 是最后一节。
- HEAD = `ff0136d`（01:35:23，"fix: act on review round 6 (2xP1, 3xP2, 8xP3)"）。**01:40:44–01:42:39 修改方又落码一批（未提交、`审查反馈.md` 零记载）**。本部分复核到此为止的源码：`main.js` `0b85351b…`（01:42:39）、`renderer.js` `07c80e09…`、`lib/limits.js` `42810465…`、`styles.css` `a3cb01bf…`、`test/dom/main.js` `9586c0cf…`、`test/ipc.test.js` `5bb4923b…`、`test/report-items.test.js` `16008511…`、`test/tray.test.js` `30a19e65…`、`test/tmp-profiles.js` `99601d4e…`（未跟踪）。

## 〇、先对表：三个代码层 vs 三份文档

| 层 | 内容 | 状态 |
| --- | --- | --- |
| 提交 `ff0136d` | §十一 全部主张（第六轮 13 条） | 已核验，见第一节 |
| 在途工作区（01:40–01:42，未提交） | 第七轮清单 #7 / #8 / #15 / #16 + P1-5 / P1-6 行为断言 + `tmp-profiles` 清扫机制 | 已核验，见第二节；**反馈无对应章节** |
| 打包产物 `Setup.exe`（01:28） | asar 内 `main.js` 与 `ff0136d` **逐字节一致**（`asar extract-file` + diff 为空，md5 `2419c74b…`） | 含 §十一 全部修复，早于在途批次 |

与第七轮的「清单外并行改动」不同，这次在途批次逐条对应第七轮清单（`tmp-patch-p3net.js` / `tmp-patch-sweep.js` 脚手架与 report-items 新增断言可互证是清单内工作）。但形式问题重复了第七轮教训 #1：**反馈落盘时（01:30）这批码还不存在，反馈对它零记载** —— 本部分代为对表，下一轮反馈必须补记。

## 一、§十一 逐条核验（第六轮 13 条）

| 条目 | 状态 | 核验证据（本轮实测/复读，非转抄自述） |
| --- | --- | --- |
| P1-A 缺一侧限额不得伪造 0% | ✅ 真修 | `main.js:425-426` 两侧独立判空（缺失侧 `null`，注释引用本报告依据）；`renderer.js:474-475` 把原始值交给 `setPctBar`（:479-489），`setBar`（:491-504）`unknown` 独立成档、结构上到不了 ok；`styles.css:374-386` 灰斜纹 + 灰字。关键追踪：旧 N-3 的「归一化成 0」已从 plan 分支移除，全文件仅余额 `amount` 一处归一化，无第二处把 null 变 0 的点。IPC 直测 2 条 + DOM（stub `p5`）4 条实跑通过 |
| P1-B 保存失败不留幽灵条目 | ✅ 真修 | `renderer.js:224-244`：先构造 `nextProviders`（不动 state）→ `persistAll(nextProviders, changed)`（:237）→ 失败仅 `showSaveError` + return（内存从未被改）→ 成功才 `state.providers = nextProviders`（:244）。`grep "state.providers.push" renderer.js` 零命中；report-items 有 commit-after-write 顺序断言；DOM `savefail` 场景补了第 2 步（失败后主动 `renderAll()` + `renderProviderList()`，断言两处均 0 幽灵） |
| P2-A DOM 测试泄漏 | ✅ 真修 | `test/dom/main.js:22-35`：`cleanup()` 在 `app.exit()` 之前同步调用 + `process.on('exit')` 幂等兜底，注释如实记录 `app.exit()` 不发 `quit` 的成因与「58 目录/403MB」实测。**实测 `%TEMP%/llmb-*` 目录 = 0** |
| P2-B 余额单位不猜 | ✅ 真修 | `limits.js:162-174`：原样返回 `{amount, currency:'', field}`，无 `/100`、无币种猜测，注释完整记录了历史错误（$5.00/CNY/分 三处打架 + 100 倍跳变）与「不猜」理由；UI tooltip（`renderer.js:467`）与 README:72 同口径；`{quota:1000}` vs `{quota:1001}` 相邻不跳变断言在 `limits.test.js`；report-items 加了 `!/amount \/ 100/` 防回退断言 |
| P2-C 余额列宽溢出 | ✅ 真修 | `styles.css:335-345`（第三列 `auto`）、`:388-393`（`nowrap`）、`:395-402`（`.balance-row` / `.balance-amount` 两个钩子终于有规则）；DOM 断言带**自证判别**（临时套回旧 `36px` 布局确认溢出指标会红，`test/dom/main.js:133-142`） |
| P3-1 `&& false` 死分支 | ✅ | `tools/make-icon.js` 无 `&& false`（report-items 反断言守着） |
| P3-2 `tray.png` 无人消费还进包 | ✅ | `src/assets/tray.png` 已删（磁盘实证）；`asar list` 实证安装包内无 tray 项；生成目标移到 `build/`（make-icon.js:169-170）；report-items 有 `existsSync` 反断言 |
| P3-3 截断 id 一致 | ✅ | `main.js:286` 截断一次全程复用（查重 `seenIds` 与落盘同值），report-items 反断言禁止 `id.slice(0,100)` 复现 |
| P3-4 single-instance 锁顺序 | ✅ | `main.js:54` 在 `whenReady`（:114）之前，report-items 有顺序断言 |
| P3-5 自测注释与行为一致 | ✅ | `renderer.js:599-600` 注释改为「deliberately: … every build」 |
| P3-6 ↻ 无反馈 | ✅ | `renderer.js:165-169`（拿不到就显示「正在刷新，请稍候…」）+ `pollAll` 返回布尔；behavior 场景实测「轮询中再次触发返回 false / 可见反馈」 |
| P3-7 readAll 版本告警 | ✅ | `main.js:200-205`：非 v1 `console.warn` 且文件不动 |
| P3-8 hostname 识别 | ✅ | `main.js:382-388`（`hostOf`）+ `:398-405`（`=== / endsWith('.'…)`），report-items 反断言禁止子串匹配回归 |

**同时记账**：§十一.七 对《第五部分·六个问题》的回答全部切题，且与我的实测一致（`--` 未知态的选择与实现吻合、403MB 自曝比报告测到的更严重、三处矛盾选「不猜」、`.balance-row` 承认漏写、`hasPlanLimits` 覆盖面空白承认）。§十一 的**流程回退**一条：首次未附交付后源码哈希（§九/§十都有，§十一只有报告基线）—— 已由本部分基线补齐，下轮恢复惯例。

## 二、在途批次（01:40–01:42，未提交）逐项审查 —— 第七轮清单 #7 / #8 / #15 / #16

### #7 诊断网络层（七-P3）✅，且比报告要求多走了一步

`main.js:440-445`：diag 四字段（`statuses` / `messages` / `errors` / `responded`）；`:516-521`：`note()` 三分类 —— `ok`→`responded++`、带 status→`statuses`、无响应→`errors`（不再把 0 混进 statuses）；`:467-481`：`explainProbeFailure` 顺序 = 鉴权 → 全静默（网络层，超时单独成句）→ 网关文案 → 全 404（**要求 `responded === 0`**）→ 字段兜底。

**`responded` 计数是报告没要求、它自己想到的**：`tryFetchJson` 成功时不带 status，若无此计数，「200 但字段不可用」混在 404 里仍会被误判为「所有候选 404 → Base URL 问题」。新增 IPC 反例 4 条实跑全过：200+不可用字段不怪 URL / 全网络失败→「网络层」/ AbortError→「超时」/ 余额模式同路径。

**残留一处措辞边界（N-16，P3）**：部分 404 + 部分网络失败的混合场景下 `notFound` 判据（`:480`）仍成立 → 文案「**所有**候选路径均返回 404」失实（statuses 非空时对 `errors` 只字不提）。修法一行：`errors` 非空时改为「X 个 404、Y 个无响应」。

### #16 余额 fallback 分支（七-P7）✅，并顺带更正了我方第七轮的判断

**第七轮 P3-7 称该分支「实际不可达」—— 本轮探针实测：错。** 输入 `{data:{data:'x',balance:5}}`：accept 用 `normalizeBalance(res.data)`（内部解一层 `.data`）判真；而最终解析对 `res.data?.data ?? res.data` 再解一层 → `normalizeBalance` 收到 `'x'` → null → **「找不到余额字段」分支被命中**。根因是 accept 的判定根与最终解析根差一层 unwrap（plan 路径由调用方在 lambda 里预解，恰好同源；balance 路径不对称）。在途改法（`main.js:580-588`，`viaUserInfo` 闭包把 accept 的解析结果带出来，`if (!data || !viaUserInfo)` 兜底）**同时**消掉「不可达分支」与「判定/解析不同源」两个问题，与 P3-I 的 done-guard 约束一致；report-items 同步禁止旧分支回归。实测复跑 `npm test` 全绿。此更正已按惯例记入《方法论教训》。

### #8 / #15 ✅

- `make-icon.js:62-63`：同值三元收敛为 `px = b === 0 ? BAR_DIM : BAR`（report-items 反断言守着）。
- `test/tray.test.js`：清理重构为 `cleanup()` + `process.on('exit')` 兜底 + **catch 路径补清理**（:28-35 / :143 / :147）；`test/ipc.test.js` 失败路径与 harness-error 路径都清 TMP。
- 新增 `test/tmp-profiles.js` 的 `sweepStale`：按 PID 存活判定（`EPERM` = 存活）只清死进程残留，并发安全，设计合格。

### 超出清单的新增行为断言（质量好，记账）

- P1-6：新增订阅不填 Key → 0 次 `saveProviders` IPC、0 多行（dom behavior 场景 + stub `getSaveCount`）。
- P1-5：删除后缓存失效（IPC 实测被删订阅「订阅不存在」且落盘同步）。

### N-17【P2，提交关卡】`test/tmp-profiles.js` 尚未纳入版本管理

`test/report-items.test.js`（`read('test/tmp-profiles.js')`）、`test/dom/main.js`、`test/tray.test.js`（`require('./tmp-profiles')`）三处引用它，而它目前 untracked。**下笔提交若漏掉它，提交后的树 `npm test` 直接红。** `tmp-patch-p3net.js` / `tmp-patch-sweep.js` 脚手架勿入库（用完即删或进 .gitignore；当前 .gitignore 只盖了 `tools/*`）。

## 三、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件（当前工作区，01:46） | `npm test` 实跑 | report-items **68** + limits **37** + ipc **42** = **147/147 全绿** |
| 语法 | `node --check` ×6 | 6/6 |
| 进程表 | `Get-CimInstance Win32_Process` | electron.exe = **0**（第七轮遗留的 2 个挂死进程已清，清单 #0 前半闭合） |
| 文件系统 | `%TEMP%/llmb-*` | **目录 0**（36 个 ≈247MB 已清，#0 后半闭合）；文件 4：`llmb-icon-*.png` ×2（00:04，图标开发残留）、`llmb-widget-shot*.png` ×2（01:31，visual 场景截图 —— 同时是「DOM 套件在 §十一 修复后完整跑完」的产物证据） |
| asar | `extract-file` + diff | 包内 `main.js` ≡ `ff0136d`（md5 `2419c74b…`，逐字节一致）；包内无 `tray.png` ✅ |
| 托盘图标一致性 | node 解码比对 | `main.js` 内联 data URL 与 `build/tray.png` **字节相同**（202 B）—— 生成器与内联常量无漂移 |
| DOM / tray 套件 | 未重跑（用户指令） | 以 01:31 产物 mtime + 源码追踪佐证；在途批次给 behavior 场景新增的 P1-6 两条断言**尚无执行证据**，下轮需附 |

## 四、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-13 | 更正（核验方自纠） | 第七轮 P3-7 | 「不可达」判断错误：分支可达（探针输入 `{data:{data:'x',balance:5}}`）。在途修复已闭合，无需再动；记录在案并进《方法论教训》 |
| N-14 | P3 | `renderer.js:311` / `:319` | `deleteProvider` 在 await 前 `state.lastUsage.delete(id)`，失败回滚只恢复 providers 不恢复缓存 → 删除失败后该行读数退回 `--` 直到下一轮轮询（≤60s）。修法：回滚时把删掉的缓存项一并放回 |
| N-15 | P3 | `README.md:31` / `:72` | 托盘图标描述过期（写「绿色圆点」，实际已是与挂件同构的三柱图形 —— 字节实测与生成器输出一致）；余额候选漏列 `/api/user/quota`（`main.js:547` 三个候选） |
| N-16 | P3 | `main.js:480` | 混合失败（部分 404 + 部分无响应）仍报「所有候选路径均返回 404」，文案失实（见二.#7） |
| N-17 | P2（流程） | git | `test/tmp-profiles.js` 被三处引用但未跟踪；`tmp-patch-*.js` 脚手架残留工作区。下笔提交前必须处理（见二.N-17） |
| N-18 | 流程 | `审查反馈.md` §十一 | 首次未附交付后源码哈希（前两节都有）；01:40–01:42 批次零记载（第七轮教训 #1 复发，属清单内工作、性质较轻，但下一轮反馈必须补记） |

杂项（不单列）：`applyUsageRow` error 分支不清 `.unknown` 类与 tooltip（前一轮的 unknown 残留到错误态，纯外观）；`llmb-icon-*.png` 两个小文件可在下次清理时顺手删除。

## 五、要求下一轮反馈回答的问题

1. **N-17**：`tmp-profiles.js` 是有意不入库还是遗漏？下笔提交的文件清单里有没有它？（这是当前唯一能把「提交后的仓库」弄红的项。）
2. **N-16**：混合失败（如 2×404 + 2×超时）时用户该看到什么？现在的文案在 statuses 非空时对 errors 只字不提。
3. **N-14**：删除失败回滚后读数清零 —— 是否接受「回滚时恢复缓存项」这个最小修法？
4. **#17**：Setup.exe（01:28）= `ff0136d` 内容，缺在途批次。何时重跑 `dist:win` 并做装机验证（图标 / 托盘 / `%APPDATA%/llm-usage-limit-board/providers.json` 落点）？附 asar 抽取比对 + 装机后文件系统证据（沿用验收要求）。
5. **流程**：01:40–01:42 批次将在反馈哪一节补记？按第七轮教训 #1，交付说明应先于或伴随落码，而不是留给复核方从 git status 反推。

## 待办清单（第八轮 · 交回修改）

> 第七轮清单 17 条中 **16 条已闭合**（#0 进程/目录清理、#1–#6 §十一、#7/#8/#15/#16 在途批次、#9–#14 §十一）。以下为余留 + 本轮新增。
> **验收要求（沿用，不放松）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；涉及临时目录/进程/文件的改动附执行后证据；交付说明恢复「源码哈希清单」。

```
[必须]
1. git add test/tmp-profiles.js（report-items / dom / tray 三处引用它，
   漏掉则提交后的树 npm test 必红）；删除 tmp-patch-p3net.js / tmp-patch-sweep.js。
2. 重跑 npm run dist:win + 装机验证（#17 收尾）：asar 内容与当前源码比对，
   图标 / 托盘 / providers.json 落点，附文件系统证据。
   当前 Setup.exe（01:28）早于 01:40–01:42 批次。

[建议]
3. main.js:480  混合失败文案：errors 非空时不得声称「所有候选路径均返回 404」。
4. renderer.js:311/:319  删除失败回滚时恢复 lastUsage 缓存项（读数不清零）。
5. README.md:31  托盘图标描述改为实际图形；:72 余额候选补 /api/user/quota。
6. 补一轮 npm run test:dom（behavior 新增的 P1-6 两条断言尚无执行证据），
   附退出码 + 进程表 + %TEMP% 三件套。
7. 下一轮反馈补记 01:40–01:42 批次，并恢复「交付后源码哈希」惯例。
```

---

# 方法论补记（第八轮）

1. **「判定用的根」和「最终解析的根」必须同源。** 七-P7 的真正病根不是「分支不可达与否」，而是 accept 在 `res.data` 上判（借 `normalizeBalance` 内部的一层 unwrap）、最终解析在 `res.data?.data ?? res.data` 上算 —— 两层各解一次，判定通过不代表解析成功。同类结构（回调里判定、外面再算一遍）今后一律优先把判定时的解析结果带出来用，而不是重算。
2. **核验方的「不可达」结论也要过探针。** 我在第七轮用论证（而非构造输入）判了「实际不可达」，本轮一个三行探针就推翻了它。「我构造不出反例」与「反例不存在」是两句话 —— 对「不可达/不可能」类结论，交付的应该是输入，不是推理。

---

# 第八部分 · 第九轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §十四（第八轮处置）+ §十五（勘误与交叉确认）+ 提交 `7174ced` / `2d59be2` / `4ce633e` / `773483a` / `8c87c8b`
方式：全源码逐行复读（main/renderer/limits/preload/index.html/styles/dist.js）+ `npm test` 实跑 + asar 整包抽取比对 + 托盘图标字节比对 + 文件系统 / 注册表取证 + **4 个独立探针**（全部放在 `%TEMP%`，未触碰工作区；已清理）。
**未执行 Electron 实机套件**（沿用用户既定指令）；DOM / tray 套件以两会话自述互证 + 源码追踪佐证。

**基线（全部实测）**

- `审查报告.md` md5 `f0a73d02f4387f28e9d15b52ee7a07c3`、683 行 —— 与 §十四 开头自述一致，**报告未被改动**。
- HEAD = `8c87c8b`（02:28），工作树干净、无未跟踪文件。
- §十四.六 的 **16 个源码哈希与当前磁盘文件逐一比对全部一致** —— 交付后零漂移。
- `%APPDATA%/llm-usage-limit-board/providers.json` mtime `09-11 20:27` —— 与 §十五 自述一致，本轮全部工作未触碰真实用户数据。

## 结论速览

- 第八轮清单 **7/7 闭合属实**，两条必须项（git 关卡、打包重跑+装机验证）均有关键实证；§十五 的勘误按其自述标准接受并记账（git 无法佐证归属，见第二节）。
- 全量回归 **148/148 全绿**（report-items 68 + limits 37 + ipc 43，exit 0），与本轮自述完全一致。
- 新发现 **1×P2 + 4×P3 + 2 杂项**：其中 N-19 / N-20 / N-22 三个以探针输入坐实（探针输出原文见第四节），N-23 为论证性发现并如实标注。方向性最值得警惕的是 N-19——空白字符串被伪造读数，恰好踩在 P1-A「0% 是最危险的错误方向」这条本项目最被看重的红线上。

## 〇、对表：第八轮清单 7 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（必须） | `git add test/tmp-profiles.js`；删脚手架 | ✅ | `git ls-files` 含 `test/tmp-profiles.js`（入于 `7174ced`）；磁盘无 `tmp-patch-*.js`（删于 `2d59be2`）；`.gitignore` 以整类 `tools/*.ps1` 盖住 forensics 脚本；`git status` 干净 |
| 2（必须） | 重跑 `dist:win` + 装机验证 | ✅ | `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,652,675 B、md5 `892e69db…`，与 §十四.二 自述一致；asar 整包抽取后 4 个关键源文件与磁盘**逐字节一致**（见三）；装机现场（`C:\llmb-install-test`、HKCU 卸载项）实测已还原 |
| 3（建议） | N-16 混合失败文案 | ✅ | `main.js:485-487`：`statuses` 全 404 且 `errors` 非空时报「N 个候选返回 404、M 个无响应（网络层）」，`notFound` 判据带 `responded === 0`（:480）；反例 `test/ipc.test.js:425-436` 在我的实跑中通过 |
| 4（建议） | N-14 删除失败回滚恢复缓存 | ✅ | `renderer.js:310`（await 前捕获 `cached`）、`:322-323`（回滚 `splice` + `lastUsage.set`）；断言 `test/dom/main.js:289-291`（回滚前后读数组一致）在位 |
| 5（建议） | README 托盘描述 / 余额候选 | ✅ | `README.md:31`「与挂件同构的三柱绿色图形」；`:72` 余额候选含 `/api/user/quota`（与 `main.js:573` 三候选一致） |
| 6（建议） | 补跑 DOM 并附三件套 | ✅（自述互证级） | 我方未重跑（沿用指令）；两会话自述数字一致（62/11、exit 0）；注意 `%TEMP%` 的 `llmb-widget-shot*` 截图产物已被清理——DOM「跑过且跑完」的证据现在只剩自述互证 + 源码追踪 |
| 7（建议） | 补记批次 + 恢复哈希清单 | ✅ | §十二/§十三/§十四 补记在案；§十四.六 哈希清单经我逐项比对零漂移 |
| 杂项 | error 态清 `.unknown` 残留 | ✅ | `renderer.js:442-454`：error 分支对 `bar-fill` 去 `unknown`、对 `bar-pct` 去 `unknown` 并清 tooltip |

## 一、§十五「两会话归属」的交叉确认

§十五 更正了 §十四 的作者归属（并发修改出自第二个修复会话，审查方从未改码）。我方核验结果：

- **独立复测的数字与 §十五 的互证表全部吻合**：`npm test` 68+37+43=148、Setup.exe 81,652,675 B（md5 `892e69db…`）、`%TEMP%/llmb-*` = 0、electron 进程 = 0。数字层面无矛盾。
- **git 层面无法佐证归属**：`7174ced` / `2d59be2` / `4ce633e` / `773483a` / `8c87c8b` 五个提交的作者全部是 `dev <dev@localhost>`，从版本史里分不出两只手。按 §十五 自己给出的标准（"双方记录可以对得上"）予以接受、记账；**但若归属将来需要审计级证据，git 现在提供不了**。建议：并发会话各自落码时在提交消息或反馈中带会话标识，把归属从"事后互认"变成"落码时留痕"。
- `providers.json` 落点未被触碰（mtime `09-11 20:27`）与 §十五 的取证细节一致。

## 二、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 | report-items **68** + limits **37** + ipc **43** = **148/148 全绿**，exit 0 |
| 语法 | `node --check` ×12（src×5 + tools×2 + test×5） | 12/12 |
| asar | `asar extract` 整包 + 逐文件 md5 | 12 条目 = package.json + 7 个 src 文件 + 3 目录；`main.js` / `limits.js` / `renderer.js` / `preload.js` 与磁盘**逐字节一致**；无 `tray.png`、无 `test/`、无 `tools/`；包内 `package.json` 仅被 electron-builder 标准改写（剥 scripts/devDeps/build，diff 已过目，无害） |
| 托盘图标一致性 | node 解码比对 | `main.js` 内联 data URL ≡ `build/tray.png`（202 B、16×16，字节相同） |
| 进程 / 文件系统 | `Get-CimInstance` + `%TEMP%` 枚举 | electron.exe = **0**；`llmb-*` = **0**（含此前轮次的截图残留，已被修复会话清空） |
| 装机现场还原 | 文件系统 + 注册表 | `C:\llmb-install-test` 不存在；HKCU 卸载项不存在 —— §十四.二「卸载还原」属实 |
| CSS 钩子 | grep | `.security-banner`(:207)、`.bar-fill.unknown`(:376)、`.bar-pct.unknown`(:384)、`.balance-row .balance-amount`(:397/:400) 全部有规则 |
| `%APPDATA%` 用户数据 | stat | `providers.json` mtime `09-11 20:27`，未被动过 |

## 三、tools/dist.js 专项审查（本轮新引入的重试机制）

整体评价：三段结构（镜像默认值 → electronDist 兜底 → 3 次重试 + 间歇清理）与注释给出的失效证据一一对应，环境变量覆盖、`build.electronVersion` 钉版时让路、`hasBinary` 探测这三个约束都想到了，**未发现功能性缺陷**。两条建议级观察（入杂项）：

1. **成功路径不校验产物**：electron-builder exit 0 即 `process.exit(0)`（:91），没有"Setup.exe 存在且非零字节"的关卡。若未来出现"exit 0 但产物没落地"的新型失效，重试机制会放行一个空手而归的构建。
2. **不区分失效类型**：确定性失败（如配置错误）也会重试满 3 次，且每次 `clearDist()`（:70-83）连失败现场一起清掉——排障时反而拿不到半成品。可考虑对第 3 次失败保留现场。

## 四、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-19 | **P2** | `limits.js:73-84`（numOf）/ `:90-101`（parseLimit）/ `:162-174`（normalizeBalance） | **空白字符串被 `Number()` 收成 0 → 伪造读数**：plan 侧渲染出 0%、余额显示 0。探针坐实（见下）。`v === ''` 的判空守卫挡不住 `' '`——`Number(' ') === 0`。方向上正是 P1-A 定性的「0% = 额度充足」最危险错误；P2-B「不猜」原则同样被绕过。`limits.test.js:70` 只钉了 `numOf('')`，空白串无断言（修复不与现有测试冲突，但必须补测试） |
| N-20 | P3 | `main.js:462-493`（explainProbeFailure）/ `:624-646`（tryFetchJson） | **「有响应但内容不可用」被并入「无响应/网络层」**，N-16 的修复只覆盖了 404+无响应这一格。三形态探针坐实：① 200-有响应但无字段 + 其余网络失败 → 整体报「网络层」（`:468` 分支不看 `responded`）；② 200+非JSON（SPA 兜底页）+ 其余 404 → 「3 个候选返回 404、**1 个无响应**」——该候选明明答了 200，文案失实；③ 200+非JSON + 其余网络失败 → 报「网络层」。根因两处：`tryFetchJson` 的 `res.json()` 失败（:639）返回 `{ok:false, error}` 无 status，`note()`（:522-530）三态归类里解析失败与传输失败落同一桶 |
| N-21 | P3 | `main.js:134` | 注释「16x16 green dot」过期——N-15 只改了 `README.md:31`，`main.js` 里同一事实的描述没改。图标本体实测已是三柱图形（202 B，与生成器输出字节一致） |
| N-22 | P3 | `main.js:532-541`（记忆路径分支）+ `:546-569`（probeAll） | **diag 重复计数**：记忆路径失败后会 `probeAll()` 重探全部候选，刚失败过的记忆路径被 `note()` 记两次。探针坐实：4 条候选（3×404 + 1×无响应）被报成「**4 个候选返回 404**、1 个无响应」= 5 次事件。修法：`probeAll()` 跳过本轮刚探过的记忆路径，或 `note()` 对同 URL 去重 |
| N-23 | P3（论证性） | `renderer.js:307-329` | 回滚 `splice(idx, 0, removed)`（:322）用的是 **await 之前的下标**。两个 `deleteProvider` 并发在途时（删除 A 的 IPC 在飞、用户又删 B 且 B 先返回），A 的 catch 里 `state.providers` 已变短，`splice` 会把 A 插错位。后果仅是列表顺序错乱（纯外观），且需要"并发删除 + 恰好一败一成"才触发。**如实标注：此条是论证不是实测**——按第八轮方法论，它存在被探针推翻的可能，接受方可以只当方向参考 |
| 杂项 | — | `renderer.js:34-36` / `tools/dist.js:85-96` | ① `saveThresholds` 的 `localStorage.setItem` 没有 try 包裹（对比 `dismissPrivacyNotice:129-133` 有）：setItem 抛错时 `state.thresholds` 已更新但后面的重绘循环被跳过，条形颜色与状态短暂不一致；② dist.js 两条建议见第三节 |

### N-19 探针输出（原文）

```
numOf(" ")            = 0      （期望 null）
parseLimit(" ")       = {"value":0,"explicit":false}
detectLimitPct({five_hour:{used:" ",total:100}}) = 0
normalizeBalance({quota:"   "}) = {"amount":0,"currency":"","field":"quota"}
```

### N-20 探针输出（原文，plan 模式、真实 main.js + 桩 fetch）

```
① routes: self=200+{data:{username}}（无限额字段）, 其余 3×ECONNREFUSED
   → 「请求失败（网络层）：connect ECONNREFUSED…请确认 Base URL 可达」
   （有一个候选明明通了、只是没字段——被整体说成网络不通）

② routes: self=200+非JSON(json()抛SyntaxError), 其余 3×404
   → 「服务商用量接口不可用：3 个候选返回 404、1 个无响应（网络层）…」
   （那个"无响应"的候选实际返回了 200）

③ routes: self=200+非JSON, 其余 3×ECONNREFUSED
   → 「请求失败（网络层）：connect ECONNREFUSED…」
   （解析失败与传输失败混为一谈）
```

修法方向（供参考，不强制）：`tryFetchJson` 对 200+解析失败返回 `{ok:false, status:200, parseError}`；`note()` 把它单列一类（既非 `statuses` 也非"无响应"）；`explainProbeFailure` 的混合分支按「N 个 404、M 个无响应、K 个有响应但内容不可用」如实拆分。`safeMessage` 已有，parseError 里若带 body 片段也不会回显 key。

### N-22 探针输出（原文）

```
第一次：self 返回有效限额 → ok，记忆 /api/user/self
改路由：self/token/status=404, /api/status=abort
→ 「服务商用量接口不可用：4 个候选返回 404、1 个无响应（网络层）…」
实际候选只有 4 条（3×404 + 1×无响应）；「4 个 404」= self 被计了两次
```

## 五、要求下一轮反馈回答的问题

1. **N-19**：是否按「先 `trim` 再判空」修三处（`numOf` / `parseLimit` / `normalizeBalance`）？修 `parseLimit` 时注意 tier ③ 依赖 `parsed.value`，trim 后判空返回 null 即可，不要顺手改变其他收编行为。附反例测试至少 3 条（numOf/normalizeBalance/detectLimitPct 各一）。
2. **N-20**：三形态探针已给出，混合文案的完整矩阵（404 / 无响应 / 有响应但不可用 三类 × 任意组合）你打算怎么呈现？接受「逐类计数」的方案还是想收敛成两句话上限？
3. **N-23**：接受最小修法吗（回滚时按 `removed.id` 在当前数组重算插入位置，而不是复用旧 `idx`）？若认为"并发删除 UI 错序"不值得修，请明确说不修及理由。
4. **复核权**：DOM / tray 套件连续两轮只能以自述互证 + 已清理的文件产物作证据。下轮交付时是否请用户解除限制，由我方实跑一次并把退出码入档？若不解除，建议把「DOM 套件最近一次实跑时间 + 退出码」固化进反馈的固定栏目，避免证据彻底只剩口供。

## 待办清单（第九轮 · 交回修改）

> 第八轮清单 7 条全部闭合，无结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改解析函数必须带反例测试；交付说明附源码哈希清单。

```
[必须]
1. N-19  limits.js numOf / parseLimit / normalizeBalance：空白串按缺失处理
   （trim 后判空 → null / continue），各带反例测试；探针四条输出转正为测试用例。
2. N-21  main.js:134 注释与实际图标对齐（三柱图形，删掉「green dot」）。

[建议]
3. N-20  tryFetchJson 对 200+解析失败携带 status:200 + parseError；
   note() 单列；explainProbeFailure 混合分支如实拆分三类计数。
4. N-22  probeAll 重探时跳过本轮已探过的记忆路径（或 note() 按 URL 去重）。
5. N-23  deleteProvider 回滚按 id 重算插入位置（或声明不修并给理由）。
6. 杂项  renderer.js saveThresholds 包 try（对齐 dismissPrivacyNotice 的写法）；
   tools/dist.js 成功后校验 Setup.exe 存在且非零字节再 exit 0；
   第 3 次失败后保留 dist/ 失败现场（不清）。

[流程]
7. 哈希清单惯例照旧；并发会话落码时在提交消息带会话标识，
   让归属可审计（见第一节）。
8. 02:44 在途批次（dom 杂项 4 条断言 + setUsage 桩，见第六节）
   在下笔反馈中补记 —— 第七轮教训 #1 的第三次复发。
```

## 六、复核期间目击的并发批次（02:44，在途未提交）

**时间线**：我方完成哈希对表（§十四.六 16 项全一致）与全部实测之后、撰写报告期间，02:44:00–02:44:57 `test/dom/stub-preload.js`（+3 行）与 `test/dom/main.js`（+17 行）被并发会话修改，工作树随之变脏。内容：为第八轮杂项（error 态清 `.unknown`）新增 **4 条行为断言** + 桩通道 `setUsage`（向 `usage[id]` 注入 canned 响应后直调 `pollOne`，让 p5 行从 unknown 态翻进错误态）。

- **对应关系**：与 `renderer.js:442-454`（我方本轮已在源码层核验 ✅）严格对应，属清单内工作；
- **断言静态审读**：`fillClasses.trim() === 'bar-fill ok'` 与 error 分支的 classList 操作序列（先移除 `ok/warn/danger/unknown` 再加 `ok`，:445-449）一致；tooltip 清空与读数 `--` 断言同 `:450-454` 吻合。静态判断成立，**但无任何实跑记录**（我方不跑 Electron 套件；按第六节口径，交付时随 DOM 退出码一并入档）；
- **形式问题（第三次复发）**：反馈 §十五 落盘（02:28）之后、复核期间落码，反馈对此零记载 —— 第七轮教训 #1 又一次发生。已进待办清单 #8；
- **影响声明**：src 五文件哈希未变（`main.js` `1904d899…`、`limits.js` `42810465…`、`renderer.js` `cac82061…`，mtime 均 ≤ 02:03），**本报告全部新发现（N-19～N-23）不受此批次影响**；基线段落的「工作树干净」为 02:3x 时点快照，特此说明。

---

# 方法论补记（第九轮）

1. **修好一个混合场景 ≠ 修好混合矩阵。** N-16 交付时只补了「404 + 无响应」一条反例；本轮探针扫了矩阵其余格子（200-无字段 + 网络失败 / 200+非JSON + 404 / 200+非JSON + 网络失败），三格全错。失败分类的验收应以「响应形态 × 组合」矩阵为准——单条反例只证明被测的那一格，而且恰恰是最容易被想到的那一格。
2. **判空守卫必须对着 `trim` 之后的值。** `v === ''` 挡不住 `' '`：`Number(' ') === 0` 是 JS 的收编陷阱，而 0 在这个项目里恰好是最危险的读数。凡「空值不得参与计算」的字段，先 trim 再判；测试里至少留一条空白串反例。

---

# 第九部分 · 第十轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §十六（第三会话声明 + 02:44 批次补记 + 默认路径装机验证）+ §十七（第九轮处置）+ 提交 `5a8ecec`（03:13:32，消息带 `[session-c]` 标识）
方式：全源码逐行复读 + `git show 5a8ecec` 全量 diff 对表 + `npm test` 实跑 + **4 组独立探针**（limits 收编变体；真实 main.js + 桩 fetch 的矩阵补充格子；dist.js 沙箱三案；文件系统/注册表取证），全部放在 `%TEMP%`，未触碰工作区，复核结束时已清理。
**未执行 Electron 实机套件**（沿用用户既定指令，第九轮问题 4 前半仍待用户裁决）；DOM/tray 以「固定栏目」自述 + 源码静态清点佐证。

**基线（全部实测）**

- `审查报告.md` md5 `0cc284bf64fdb885d29194b52b8f3b9d`、842 行 —— 与 §十六/§十七 施工基线自述一致，**报告未被改动**；且随 `5a8ecec` 以「按收到的原样」入库（提交消息自述，与行数/内容吻合）——审查链路首次进版本管理，予以记账。
- HEAD = `5a8ecec`，工作树干净、无未跟踪文件。
- §十七.七 的 **17 项源码哈希与当前磁盘文件逐一比对全部一致** —— 交付后零漂移。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,653,918 B、md5 `1514727a612a3adedaed706ea730d4af`，与 §十七.六 一致；`win-unpacked/resources/app.asar` 整包抽取：6 个运行时文件与工作区**逐字节一致**，包面仅 `package.json + src/`（无 tray.png / test/ / tools/ / scripts / deps）。
- `%APPDATA%/llm-usage-limit-board/providers.json` mtime `09-11 20:27` —— 三会话全部工作未触碰真实用户数据。
- 装机现场还原属实：`C:\llmb-install-test` 不存在、默认安装目录不存在、HKCU 卸载项无。
- electron.exe 进程 = 0；`%TEMP%/llmb-*` 非本会话产物恰 8 项，与 §十六.四 披露逐项吻合（含第九轮复核会话遗留的 `llmb-test-r9.log` ——审查方自己的残留，本轮不代清，记账）。

## 结论速览

- 第九轮清单 **7/8 闭合属实**，两条必须项（N-19、N-21）均以 diff + 实测确认；`npm test` **165/165 全绿**（report-items 74 + limits 44 + ipc 47，exit 0），三套件条数与自述逐一吻合。
- §十六 的三项（第三会话声明、02:44 批次补记含第一版挂死自曝、默认路径装机验证）**全部核验属实**；第九轮四个问题全部有答复，「最近一次实跑」固定栏目已按承诺建立（§十七.五）。
- 新发现 **1×P2 + 2×P3 + 1 记账杂项**。方向上最重要的仍是老朋友：N-24 —— 杂项②新交付的产物关卡，**恰在它要拦的场景（exit 0 但产物未落地）于终局尝试时以 exit 0 放行**，沙箱实证。N-25 是 N-19 的同类残余（`Number([]) === 0` / `Number(false) === 0`），同一红线上还剩两个格子。
- N-23 按其最小修法验收，我方**不点名**要求时序桩（理由见对表 #5）。

## 〇、对表：第九轮清单 8 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（必须） | N-19 空白串按缺失处理 | ✅ | `limits.js:73-90`（numOf）/`:96-110`（parseLimit）/`:180`（normalizeBalance）：trim 后判空三处在位，字符串统一走 `Number(s)`，tier ③ 依赖的 `parsed.value` 语义未动（diff 逐行核对）；我方探针复跑四条原输出全部转正（`numOf(" ")`→null 等）；新用例实跑 44/44 |
| 2（必须） | N-21 注释对齐三柱图形 | ✅ | `main.js:134-136` 注释已改为「与挂件/安装器图标同构的三柱图形 + 生成器出处 + 与 build/tray.png 字节一致」；report-items 反断言 `notMatches(/green dot/i)` 在位 |
| 3（建议） | N-20 四桶分类 + 混合如实拆分 | ✅ | `main.js:475-484`（tally）、`:487-494`（全静默判据加 `unusable===0 && responded===0`）、`:547-556`（note 四分类互斥）、`:667-676`（tryFetchJson 携 status+parseError）；矩阵反例 ①②③ 实跑全绿；两条既有断言的更新经我比对属实且**非放松**（判别点从「禁 404 字样」改为「必须如实计数 3 个候选返回 404 + 禁 Base URL 指引」，断言的内容反而更多） |
| 4（建议） | N-22 重探跳过刚失败的记忆路径 | ✅ | `main.js:565-570`（filter 排除 remembered）、`:576-580`（probeAll(list)）；反例断言请求数 `=== 4` 在位；我方独立探针复现：「3 个候选返回 404、1 个无响应」+ 4 次请求（旧实现为 5） |
| 5（建议） | N-23 回滚按 id 重算插入位 | ✅ | `renderer.js:318-320`（await 前捕获 `nextId`）、`:334-338`（findIndex by nextId，找不到则 push 兜底）；两条静态卫兵在位（禁 `splice(idx, 0, removed)` 复现 + 必须存在 nextId 锚定）。**我方决定：不点名时序桩**——并发删除仅外观错序、需「双删在飞 + 恰好一败一成」才触发，且 push 兜底保证条目不丢；构造保证 + 静态卫兵与本风险相称 |
| 6（建议） | 杂项① saveThresholds 包 try | ✅ | `renderer.js:35-41`：try/catch 包裹，失败方向（阈值仅会话内生效）注释在位；report-items 守卫 `function saveThresholds(t) {\s*try {` 在位 |
| 7（建议） | 杂项② dist.js 产物关卡 + 终局保留现场 | ⚠️ | 机制两半都在位且大部分实测有效（沙箱 Case 2：先败后成 → `artefact:` 行 + exit 0；Case 3：三连败 → exit 1 且第 2 次失败现场保留进终局）。**但关卡的核心场景有洞 → N-24（P2）** |
| 8（流程） | 会话标识 + 02:44 批次补记 | ✅ | `5a8ecec` 消息带 `[session-c]`；§十六.一 补记在案且如实记录了第一版挂死与修法三件套（页内 `JSON.stringify` + `try/catch` + 守卫断言，均已在 `test/dom/main.js:154-172` 源码确认）；02:44 批次两文件随本提交入库 |

## 一、§十六 的核验（第三会话声明、装机验证、TEMP 清理）

1. **第三会话声明**：与第九轮报告 §六「02:44 并发批次」的目击记录严格互证（批次内容 = 第八轮杂项的行为断言 + setUsage 桩，静态审读成立但当时无实跑——§十六.一 原文认账）。归属仍无法用 git 佐证（作者统一 `dev@localhost`），但按第九轮确立的「落码时留痕」标准，`[session-c]` 标识已兑现，从「事后互认」前进到「落码留痕」。
2. **默认路径装机验证**：装机后现场我方实测已还原（默认目录、注册表、快捷方式均无残留）；`providers.json` 四次核对一致的自述与我方 mtime 取证吻合。Git Bash `/S` 路径转换与 Backstop 遮挡层两条操作坑记录具体、可复现，予以入档——**托盘的装机端物理点击缺口继续挂着**（继承项，等价链已到顶）。
3. **「最近一次实跑」固定栏目**：已建立（§十七.五），内容自洽；我方本轮静态清点 `test/dom/main.js` 的 `ck()` 断言总数 **67**，与自述 29+7+8+5+7+4+7=67 一致（loadfail 5 与 visual 7 在源码中连排，合计 12，与加数吻合）。栏目本身不等于复核方实跑，但第九轮问题 4 的后半要求已兑现，证据不再只剩口供。

## 二、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 | report-items **74** + limits **44** + ipc **47** = **165/165 全绿**，exit 0（自述 165 ✓） |
| 语法 | `node --check` ×12 | 12/12 |
| asar | `@electron/asar extractAll` + 逐文件 md5 | 6/6 与工作区逐字节一致；包面干净 |
| N-19 变体探针 | 真实 limits.js | 空白串四条全 null（修复在位）；**`[]`/`false` 收编为 0（N-25）**；空白 total 弃权 null（方向正确）；`numOf("0x10")=16`（边缘，不立案） |
| N-20 矩阵探针 | 真实 main.js + 桩 fetch | 格子④全不可用 → 「4 个有响应但内容不可用」如实计数 ✓；格子⑤网关话术 + 3×404 → **tally 未出现（N-26）**；格子⑥ 503+404 → 字段兜底但 tally 如实「4 个候选返回 503/404」（较修复前改进）；N-22 场景 → 「3 个候选返回 404、1 个无响应」+ 4 次请求 ✓ |
| dist.js 沙箱 | 复制真件 + 桩 builder，三案 | Case 1（exit 0 无产物）→ **最终 EXIT=0（N-24 坐实）**；Case 2（先败后成）→ artefact 关卡行 + EXIT=0 ✓；Case 3（三连败）→ EXIT=1 + 终局保留现场 ✓ |
| 文件系统 / 注册表 | PS 取证 | 装机现场三处（自定义目录/默认目录/HKCU）全无残留；providers.json 未动 |
| `%TEMP%` | 枚举 | 非本会话产物 8 项 = §十六.四 披露；本会话探针目录已清理 |

## 三、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-24 | **P2** | `tools/dist.js:119-122` | **产物关卡在它要拦的场景上以 exit 0 放行**：终局尝试（attempt 3）中 builder exit 0 但无 Setup.exe 时，落入 `process.exit(res.status === null ? 1 : res.status)`，而 `res.status === 0` → **exit 0**。stderr 报错三次可见（沙箱 Case 1 原文见下），但机器可读的成功信号是假的——这恰是第八轮 win-unpacked 事故、也是本关卡存在的唯一理由。评级理由：机制的核心承诺（把「exit 0 无产物」变成失败）只兑现了非终局尝试；修法一行（`res.status || 1`）。注意 report-items 对 dist.js 的静态守卫（`notMatches(/if \(res\.status === 0\) process\.exit\(0\)/)`）只禁了旧的单行字面量，拦不住多行重写——守卫钉字面量而非行为，本条因此漏网 |
| N-25 | P3 | `limits.js:88-89`（numOf 非字符串路径）/ `:108-109`（parseLimit 同型）/ `:181-182`（normalizeBalance） | **N-19 的同类残余：`Number([]) === 0`、`Number(false) === 0`**。探针坐实：`numOf([])=0`、`parseLimit([])={value:0}`、`detectLimitPct({five_hour:{used:[],total:100}})=0`（伪造 0%）、`normalizeBalance({data:{balance:[]}})={amount:0}`、`quota:false → {amount:0}`。第九轮方法论 #2 修的是收编清单里最常见的一格（空白串），同类清单还剩数组与布尔两个格子；触发频率低于空白串（服务商返回异型 JSON 才会命中）故 P3，但方向同在「0 = 额度充足」红线上。修法建议：非 `number`/`string` 类型直接按缺失处理（null / continue），`numOf(0)` 与 `' 42 '` 等既有行为由现有测试钉住 |
| N-26 | P3 | `main.js:496-500`（网关话术分支） | **网关分支是四分类出口里唯一不带 tally 的**：探针格子⑤（1 个 200+`{success:false,message}` + 3×404）→ 文案只转述网关话术，3 个 404 不见踪影。不是谎言（转述服务商自己的话且可行动），但按第九轮「混合矩阵逐类计数」的标准，这里漏了一个格子。修法：该分支尾部附其余类的 tally（若有），如「服务商拒绝了请求：quota exhausted（另有 3 个候选返回 404）…」。附带观察（不立案，答复时一并表态即可）：格子⑥ 503+404 落入字段兜底，头部措辞仍是「找不到 5h/周 限额字段」而 tally 如实列出「4 个候选返回 503/404」——比修复前（纯「找不到字段」）已是改进，头部措辞是否要给 5xx 单独口径由修改方定 |
| 杂项 | — | `审查反馈.md` §十七.一 / `test/limits.test.js` | ① 自述「8 条新断言」实点为 **7 个用例块 / 10 条断言**（37→44 与 §十七.五 的 44 一致，不影响验收，但自述数字对不上实点，入待办流程项）；② 新用例第 6 块「真 0 保留：{remain:0}→0」与紧邻其上的既有用例重复——无害，转正探针时顺带即可 |

### N-24 沙箱探针输出（原文）

沙箱：`%TEMP%` 下复制真实 `tools/dist.js`，以桩 builder 替换 electron-builder CLI，三次运行取退出码（探针已清理）。

```
Case 1  builder 永远 exit 0、不落产物
  stderr: 「exited 0 but no Setup.exe landed」×3
  最终 EXIT CODE = 0        ← N-24：失败被成功码放行

Case 2  第 1 次 exit 1，第 2 次落产物后 exit 0
  [dist] artefact: ...\LLM Usage Limit Board-0.1.0-Setup.exe (1 bytes)
  最终 EXIT CODE = 0        ← 关卡成功路径 ✓

Case 3  三连败（exit 1 ×3）
  [dist] final attempt — keeping the previous dist/ for diagnosis
  dist/scene.log 仅含 attempt 2、3 两行  ← 第 2 次失败现场保留进终局 ✓
  最终 EXIT CODE = 1
```

### N-25 探针输出（原文）

```
numOf([])            = 0      （期望 null）
parseLimit([])       = {"value":0,"explicit":false}
detectLimitPct({five_hour:{used:[],total:100}}) = 0      （伪造 0%）
normalizeBalance({data:{balance:[]}}) = {"amount":0,"currency":"","field":"balance"}
normalizeBalance({data:{quota:false}}) = {"amount":0,"currency":"","field":"quota"}
numOf(false)         = 0
（对照）numOf(" ") = null / detectLimitPct(used:" ") = null —— N-19 修复在位
（对照）detectLimitPct({five_hour:{used:30,total:"  "}}) = null —— 弃权方向正确
```

### N-26 探针输出（原文，plan 模式、真实 main.js + 桩 fetch）

```
⑤ routes: self=200+{success:false,message:'quota exhausted'}, 其余 3×404
   → 「服务商拒绝了请求：quota exhausted。若 Key 无误，请把该接口的返回 JSON 发给我适配 5h/周 限额字段」
   （3 个 404 在文案中无踪影——tally 未接入此分支）

④ routes: 4×200+非JSON → 「…找不到 5h/周 限额字段（4 个有响应但内容不可用）…」✓
⑥ routes: self=503, 其余 3×404 → 「…找不到 5h/周 限额字段（4 个候选返回 503/404）…」（如实但头部措辞待商榷）

N-22 复现：首轮记住路径后改路由 3×404+1×abort
   → 「服务商用量接口不可用：3 个候选返回 404、1 个无响应…」+ 请求数 = 4 ✓
```

## 四、要求下一轮反馈回答的问题

1. **N-24**：接受「终局失败必须非零退出」（`res.status || 1`）吗？并请把沙箱三案转正为子进程级回归（你可以自建桩，或按第九轮探针转正的先例复刻三案）——本条证明了进程边界行为（退出码）没有进程内断言能覆盖，静态守卫因此失守。
2. **N-25**：是否同意口径「非 `number`/`string` 值一律按缺失处理」？注意保住 `numOf(0)`、周边空白字符串、`'42%'` 三条既有路径（现有测试已钉）。`[]`/`false` 各补一条红线案例（`detectLimitPct` 0% 与 `balance` 0）。
3. **N-26**：网关分支附 tally 的取舍？若认为「转述服务商原话时不宜混入自家计数」而不修，请明确说不修及理由——这是合法立场，按惯例留档。
4. **复核权（继承）**：第九轮问题 4 前半（解除 Electron 实机限制）仍待用户裁决；在裁决前，「最近一次实跑」栏目照旧维护即可。

## 待办清单（第十轮 · 交回修改）

> 第九轮清单 8 条中 7 条闭合、1 条（杂项②）带着 N-24 半闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改解析函数必须带反例测试；交付说明附源码哈希清单。

```
[必须]
1. N-24  tools/dist.js:119-122 终局失败必须以非零码退出（process.exit(res.status || 1)），
         并补子进程级回归三案（exit0+无产物→exit1；先败后成→artefact 行；三连败→exit1+现场保留）。
2. N-25  limits.js numOf/parseLimit/normalizeBalance：非 number/string 值按缺失处理，
         补 []/false 反例（含 detectLimitPct 0% 与 balance 0 两条红线案例）。

[建议]
3. N-26  explainProbeFailure 网关话术分支附其余类 tally；探针格子⑤转正为测试；
         格子⑥头部措辞是否调整一并表态（可不改，给理由）。

[流程]
4. 自述数字与实点对齐：本轮「8 条新断言」实为 7 用例/10 断言；
   维持「自述数字全部可复点」标准（第九轮基线条款的延续）。
5. 「最近一次实跑」栏目照旧维护；DOM/tray 实跑权限仍待用户裁决（继承）。
```

---

# 方法论补记（第十轮）

1. **静态守卫钉的是字面量，钉不住行为。** report-items 为杂项②加的 dist.js 守卫（禁旧单行 `if (res.status === 0) process.exit(0)`）在实现被重写成多行后立即失效，而行为不变量（「终局失败必须非零退出」）从始至终没有断言。跨进程边界的行为——退出码、stdio、被 spawn 的子进程——没有进程内断言能覆盖，要么子进程级测试，要么在守卫注释里显式声明「此守卫只防字面回潮，不防语义回潮」。每实现重写一次，为防回归加的文本断言就该重审一次。
2. **判空修复的验收也是矩阵，不是单格。** `Number()` 的收编面：`''`、`' '`、`'\t\n'`、`'0x10'`、`[]`、`[42]`、`false`、`true`、`Infinity`。第九轮方法论 #2 修掉了最显眼的空白串，本轮探针扫出 `[]`/`false`——与「修好一个混合场景 ≠ 修好混合矩阵」同构。今后凡「空值/异型不得参与计算」的字段，验收按收编清单逐格过，而不是只测上一个事故的那一格。

---

# 第十部分 · 第十一轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §十八（第十轮处置）+ 工作树未提交改动（8 个既有文件修改 + 新增 `test/dist.test.js`，与 `git status` 全量对表）
方式：全源码逐行复读 + `git diff` 全量对表 + `npm test` 两轮实跑 + **5 组独立探针**（limits 收编矩阵全扫；真实 main.js + 桩 fetch 的 N-26 矩阵 6 格；dist.js 陈旧产物沙箱 3 案；asar 逐文件 md5；进程/TEMP/用户数据取证），全部放在 `%TEMP%`，未触碰工作区，复核结束时已清理。
**未执行 Electron 实机套件**（沿用用户既定指令，第九轮问题 4 前半仍待用户裁决）；DOM/tray 以「最近一次实跑」自述 + 源码哈希未变佐证。

**基线（全部实测）**

- `审查报告.md` md5 `7f13f33ea9b689348dc0cab933b07703`、990 行 —— 与 §十八 施工基线自述一致，报告未被改动。
- HEAD = `5a8ecec`，工作树改动 = 清单内 8 个文件 + 新增 `test/dist.test.js` + 两份审查文档 —— **§十八「零清单外落码」声明与 git status 全量对表属实**，diff 逐行核对无清单外行为（见对表 #4）。
- §十八.六 的 **18 项文件哈希与当前磁盘逐一比对全部一致** —— 交付后零漂移。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,219 B、md5 `620891442b73b5cc0babf4db51168414`、mtime 04:21 —— 与 §十八.五 一致。
- `app.asar` 整包抽取：6 个运行时文件与工作区 md5 **逐一相同**（cfc9c411 / 4fdfad53 / b44998c0 / c16e5e70 / afa1b8e9 / a3cb01bf，与 §十八.五 所列吻合）；包面仅 `package.json`（name/version/description/main/author/license/private）+ `src/`，`scripts.test` 不在包内。
- electron.exe 进程 = **0**；`providers.json` mtime `09-11 20:27` 未动；`%TEMP%/llmb-*` 非本会话产物恰 8 项，与 §十六.四 披露逐项吻合。

## 结论速览

- 第十轮清单 **5/5 闭合属实**：N-24 / N-25 / N-26 三条以 diff + 独立探针双确认；`npm test` 两轮实跑 **182/182 全绿**（report-items 77 + limits 52 + ipc 50 + dist 3，exit 0），自述数字全部可复点——「8 用例块/16 断言」「3 用例块/7 断言」「P2-4 净 +1 断言」逐块数过（流程 #4 验收通过）。
- §十八.五 打包重跑 + asar 比对核验属实；§十八.四 三件套的当前快照（进程 0、TEMP 非本会话 8 项）与披露自洽；`test/dom/main.js` 哈希与第十轮清点时一致，67 条断言数沿用，实机数字仍为自述（权限继承）。
- 新发现 **1×P2 + 1×P3 + 2 记账**。N-27 是 N-24 的另一半：产物关卡验的是「dist/ 里有 Setup.exe」，不是「本次构建落了 Setup.exe」——**陈旧产物（上次运行遗留，或终局前失败尝试的半成品）同样能让它 exit 0**，沙箱两案坐实，其中一案连失败前提都不需要。N-28 揭出 §十八.三 拒绝「另有」的理由描述了一条**实现中不可达的路径**：`tryFetchJson` 对非 2xx 响应从不解析 body，404 响应体里的网关话术根本到不了 `diag.messages`（探针坐实该场景落「所有候选路径均返回 404」）。
- N-25 收编矩阵全清：`[]`/`[42]`/`false`/`true`/`{}`/`NaN`/`Infinity` 全部 → null，真 0 与数字字符串路径原样保留；`'0x10'`=16、`'1e3'`=1000 维持第十轮「边缘，不立案」。

## 〇、对表：第十轮清单 5 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（必须） | N-24 终局失败非零退出 + 子进程三案 | ✅ | `tools/dist.js:127` `process.exit(res.status \|\| 1)`；`test/dist.test.js` 三案随 `npm test` 实跑 3/3（真实 dist.js 原件拷贝 + `LLMB_DIST_SCENE` 桩驱动，Case 3 连「终局不清场」都用 `readdirSync` 钉住——转正质量高，记账）；我方沙箱另证真实退出码保留（builder 恒 exit 2 → 终局 EXIT=2，超出三案覆盖面）；report-items 双向字面守卫在位且标签明示「字面守卫/行为由 dist.test 守」分工 |
| 2（必须） | N-25 非 number/string 按缺失 + []/false 反例 | ✅ | `limits.js:92/:113/:186` 三处类型闸门 + 既有 trim 闸门；`limits.test.js:118` 起 8 块/16 断言实跑全绿；我方探针把收编清单扫满（探针 A 原文），两条红线（used:[] → 0%、balance:[]/quota:false → 余额 0）全部转 null，真 0/百分号/周边空白路径保留；P2-4 守卫更新经 diff 核对确为**收紧**（旧单行只挡 null/空白串，新两步式额外挡数组/布尔/对象） |
| 3（建议） | N-26 网关分支附 tally + 格子⑤转正 + 格子⑥表态 | ✅（措辞偏离已声明理由） | `main.js:496-505`；ipc ⑤/⑤b/⑥ 实跑全绿，「既有纯话术断言无需改动」属实（50/50）；我方矩阵探针另补 4 格：话术+网络失败、话术+非JSON+404（混合两类如实）、401 压过话术（鉴权优先级未动）、balance 模式 7 路径 6×404+1 话术如实计数——全部如实（探针 B 原文）。**但支撑措辞偏离的理由表述失实 → N-28** |
| 4（流程） | 自述数字与实点对齐 | ✅ | 77/52/50/3 = 182 两轮实跑吻合；「8 块/16 断言」「3 块/7 断言」「P2-4 净 +1」逐块可数 |
| 5（流程） | 实跑栏目照旧维护 | ✅（自述） | 栏目在位（§十八.四）；dom/main.js、tray.test.js 哈希未变，清点沿用；实机套件我方仍未跑（继承裁决待定） |

## 一、§十八 的核验

1. **N-24（§十八.一）**：修法与报告建议一致，注释如实；`sweepStale` 前缀 `llmb-dist-` 已并入 `tmp-profiles.js`（PID 命名，死进程可清扫，与 dom/tray 同规）。**关卡本身仍有半边没守住 → N-27**。
2. **N-25（§十八.二）**：口径「非 number/string 一律缺失」三处同型落地，既有路径由新旧用例共同钉住（探针 A 复核无一回潮）。
3. **N-26（§十八.三）**：tally 接入网关分支且措辞中性；「⑤b 不附空 tally」与既有断言共存；格子⑥「不改头部措辞」是合法立场且已转正为回归。但其拒绝「另有」的核心理由（「404 body 可带网关话术」）在当前实现下不可达 → N-28。
4. **零清单外落码**：`git diff` 全量对表——package.json 仅 test 链加 dist.test；tmp-profiles 仅 PREFIXES 加 `llmb-dist-`；limits.js / main.js / dist.js 的 diff 与 N-25 / N-26 / N-24 一一对应；测试文件改动与 §十八 各节一一对应；两份审查文档为文档本身。无清单外行为。

## 二、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | report-items **77** + limits **52** + ipc **50** + dist **3** = **182/182**，exit 0（自述 182 ✓） |
| 语法 | `node --check` ×5 | 5/5（main / limits / dist / dist.test / tmp-profiles） |
| asar | `@electron/asar` 逐文件 extractFile + md5 | 6/6 与工作区一致；包面干净 |
| N-25 矩阵探针 | 真实 limits.js | 收编清单全 null（含 `[42]`/`true`/`{}`/`NaN`/`Infinity`）；真 0/百分号/空白路径保留；`'0x10'`=16、`'1e3'`=1000（不立案，沿用第十轮口径） |
| N-26 矩阵探针 | 真实 main.js + 桩 fetch，6 格 | 话术+404 / 话术+网络失败 / 话术+非JSON+404 → tally 全部如实；401 压过话术；balance 7 路径如实计数；**404 body 带话术 → 落「所有候选路径均返回 404」，话术被吞（N-28）** |
| dist.js 沙箱 | 复制真件 + 桩 builder，3 案 | S1 陈旧产物（上次运行遗留）+ builder exit0 不落产物 → **artefact 行 + EXIT=0（N-27 坐实）**；S2 终局前一案的半成品 Setup.exe 存活进终局 + 终局 exit0 不落新产物 → **artefact 行 + EXIT=0（半成品随绿色退出码放行）**；S3 builder 恒 exit 2 → **EXIT=2**（真实退出码保留 ✓） |
| 文件系统 / 进程 / 用户数据 | PS + stat 取证 | electron 进程 0；providers.json mtime 09-11 20:27 未动；%TEMP% 非本会话产物 8 项与披露吻合；本会话探针目录已清理 |

## 三、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-27 | **P2** | `tools/dist.js:88-98`（findSetupArtifact）+ `:70-83` / `:100-109`（清场策略） | **产物关卡可被陈旧产物满足**。`findSetupArtifact()` 只验「dist/ 下存在非零字节 Setup*.exe」，不验「是否本次构建所落」；而 dist/ 在第 1 次尝试前**从不清场**、终局尝试前**按设计保留现场**。两条可达路径：① 上次运行（或任何外部构建）遗留在 dist/ 的 Setup.exe + 本次 builder exit 0 不落产物 → artefact 行 + exit 0（沙箱 S1，**无需任何失败前提**——dist/ 带着上次产物恰是重试工作流的常态，且 dist.js 自己的「终局保留现场」就在制造这种 dist/）；② 终局前一次失败尝试写出半成品 Setup.exe 后 exit 1，终局 exit 0 不落新产物 → 关卡被半成品满足，exit 0（沙箱 S2）。两条路径的终点都是「绿色退出码 + 非本次/不完整安装包」——恰是本关卡存在的唯一理由（第九轮杂项②、win-unpacked 事故）要拦的事。评级理由：与 N-24 同承诺、同红线；S1 前置条件是常态而非例外。修法三选一：(a) 每次 spawn 前快照 dist/ 清单，命中产物必须是本次新增；(b) 脚本启动时清掉 dist/ 既有 Setup*.exe（构建产物可再生）；(c) 产物 mtime ≥ 本次尝试开始时刻。配套：dist.test 补 Case 4（预置陈旧 Setup.exe + builder exit0 不落产物 → 必须非零退出） |
| N-28 | P3 | `src/main.js:672`（tryFetchJson 非 2xx 早退）+ `:497-505`（网关分支及其注释） | **「404+话术」盲区 + 一条不可达路径上的理由**。`tryFetchJson` 对 `!res.ok` 直接 `return { ok: false, status }`，body 从不解析 → `note()` 挖 message 的输入 `res.data` 对非 2xx 恒为 undefined → `diag.messages` 只可能来自 200 响应。两个后果：① 行为缺口——网关以 404+`{message:"quota exhausted"}` 拒绝时（探针 ⑤v5 坐实），可行动的话术被吞，落「所有候选路径均返回 404」；② `main.js:500-502` 注释与 §十八.三 拒绝「另有」的核心理由（「404 body 可以带网关话术，说话的候选本身就在 statuses 计数里」）描述的正是这条**当前不存在的路径**——中性 tally 措辞今天恰好仍真，但为决策作保的场景不可达。修法二选一：(a) 行为补齐——非 2xx 响应有界解析 JSON body 提取 message 进 `diag.messages`（safeMessage 脱敏已有、鉴权分支优先级在前、note() 计数语义无需动），修后注释即成立、⑤v5 转正为测试、中性措辞从「碰巧为真」变成「必须为真」；(b) 不修行为——则必须更正注释与 §十八.三 表述，并明确「404+话术按无话术处理」是有意取舍，留档 |
| 记账① | — | `src/lib/limits.js` numOf | `'0x10'`=16、`'1e3'`=1000：Number() 的数字字符串语义，沿用第十轮「边缘，不立案」；与本轮钉死的口径（非空数字字符串即数据）自洽 |
| 记账② | — | `test/dist.test.js:71-76` | `run()` 的 `spawnSync` 无 timeout：当前桩恒快速退出故无害；dist.js 未来若引入可挂起的真实依赖，npm test 会整体挂死——相对 tmp-profiles「死进程清扫」的兜底哲学缺一道闸。不立案，留意即可 |

### N-27 沙箱探针输出（原文）

沙箱：`%TEMP%` 下复制真实 `tools/dist.js`，以桩 electron-builder CLI（`LLMB_DIST_SCENE` 切换行为）驱动。

```
S1  预置 dist/…Setup.exe（内容 "STALE-FROM-PREVIOUS-RUN"）+ builder 恒 exit 0、不落产物
    stdout: [dist] artefact: …\dist\LLM Usage Limit Board-0.1.0-Setup.exe (23 bytes)
    EXIT=0   EXE content: STALE-FROM-PREVIOUS-RUN     ← 上次运行的产物被当作本次成功

S2  attempt 1 exit 0 无产物（stderr: no Setup.exe landed）；attempt 2 写出半成品
    Setup.exe（"partial-from-attempt-2"）后 exit 1；attempt 3 exit 0、不落新产物
    stdout: [dist] final attempt — keeping the previous dist/ for diagnosis
            [dist] artefact: …\LLM Usage Limit Board-0.1.0-Setup.exe (22 bytes)
    EXIT=0   EXE content: partial-from-attempt-2      ← attempt-2 半成品随绿色退出码放行

S3  builder 恒 exit 2
    stderr: [dist] build failed after 3 attempts
    EXIT=2                                            ← 真实退出码保留 ✓（三案回归未覆盖，顺带证实）
```

### N-28 探针输出（原文，真实 main.js + 桩 fetch）

```
⑤v1 self=200+话术, 其余 3×404
    → 服务商拒绝了请求：quota exhausted（3 个候选返回 404）。若 Key 无误，请把该接口的返回 JSON 发给我适配 5h/周 限额字段
⑤v2 self=200+话术, 其余 3×网络失败
    → 服务商拒绝了请求：quota exhausted（3 个无响应）。…（tally 覆盖网络失败类 ✓）
⑤v3 self=200+话术, 1×200非JSON, 其余 2×404
    → 服务商拒绝了请求：quota exhausted（2 个候选返回 404、1 个有响应但内容不可用）。…（混合两类如实 ✓）
⑤v4 self=200+话术, 1×401, 其余 2×404
    → API Key 无效或权限不足（HTTP 401）…（鉴权优先级未被 N-26 改动 ✓）
⑤v5 self=404+{message:'gateway says no'}, 其余 3×404
    → 服务商没有提供可用的用量接口（所有候选路径均返回 404）。…
      （话术被吞——tryFetchJson 不解析非 2xx body，N-28 坐实）
⑤bal balance 模式 7 路径：self=200+话术, 其余 6×404
    → 服务商拒绝了请求：quota exhausted（6 个候选返回 404）。…适配余额字段（requests=7，计数如实 ✓）
```

## 四、要求下一轮反馈回答的问题

1. **N-27**：修法倾向 (a) 快照新增 / (b) 启动清旧 exe / (c) mtime 门槛中的哪个？给理由即可。(b) 最简，但隐含「dist/ 是纯构建输出、用户手动放置的东西可被清除」的前提——若采纳请明说这一前提。Case 4 转正时建议同时断言「终局尝试 exit 0 且无新产物时不得输出 artefact 行」。
2. **N-28**：取 (a) 行为补齐还是 (b) 更正理由留档？若 (a)：非 2xx body 解析失败（非 JSON）不得产生新计数类别，仍按原四桶；若 (b)：main.js:500-502 注释必须同步更正，§十八 需补一节勘误。
3. **复核权（继承）**：第九轮问题 4 前半（解除 Electron 实机限制）仍待用户裁决；裁决前「最近一次实跑」栏目照旧维护。

## 待办清单（第十一轮 · 交回修改）

> 第十轮清单 5 条全部闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改解析/关卡函数必须带反例测试；交付说明附源码哈希清单。

```
[必须]
1. N-27  tools/dist.js 产物关卡改为「只认本次构建新落的产物」：(a) spawn 前快照 dist/
         清单、要求命中项为新增 / (b) 脚本启动时清除 dist/ 既有 Setup*.exe /
         (c) 产物 mtime ≥ 本次尝试开始时刻，三选一；
         dist.test.js 补 Case 4（预置陈旧 Setup.exe + builder exit0 不落产物 →
         必须非零退出；可顺带 Case 4b：builder 恒 exit 2 → 终局 EXIT=2）。

[建议]
2. N-28  二选一：(a) tryFetchJson 对非 2xx 有界解析 JSON body 的 message 进
         diag.messages（脱敏沿用 safeMessage；鉴权分支优先级保持；解析失败不产生
         新计数类别；⑤v5 场景转正为测试），修后同步使 main.js 网关分支注释成立；
         (b) 不修行为，但更正 main.js:500-502 注释与 §十八.三 的理由表述，并把
         「404+话术按无话术处理」作为明确取舍留档。

[流程]
3. 「最近一次实跑」栏目照旧维护；DOM/tray 实跑权限仍待用户裁决（继承）。
4. 自述数字与实点对齐（本轮全部吻合，记账，无新增动作）。
```

---

# 方法论补记（第十一轮）

1. **关卡验的是「有钥匙」，没验「钥匙是不是这栋楼的」。** N-24 修掉了「无产物 → exit 0」，N-27 揭出「产物存在但非本次」——同一个行为不变量（exit 0 ⟺ 本次构建落了新产物）的两半。对一个守护性检查，正反两面都必须进回归：正面（成功路径）、反面一（无产物）、反面二（别人的产物）。第九轮杂项②的验收只走了前两格，而「终局保留现场」这一出于好意的设计恰好在为反面二制造条件。
2. **理由必须描述可达路径。** §十八.三 用「404 body 可带网关话术」为由拒绝「另有」，而这条路径在当前实现里不可达（非 2xx body 从不解析）。结论碰巧无恙（中性 tally 在所有可达场景下都真），但写进代码注释的理由性虚构会把未来读者引向对系统的错误模型，也让 N-26 的「修复完成」显得比实际更彻底。发现「注释宣称的场景代码走不到」应与行为缺陷同等级立案：要么让行为配得上注释，要么让注释配得上行为。

---

# 第十一部分 · 第十二轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §十九（第十一轮处置，session-d）+ 工作树未提交改动（`tools/dist.js`、`src/main.js`、`test/dist.test.js`、`test/ipc.test.js`、`test/report-items.test.js` 五文件，与 §十九 开头声明对表）
方式：全源码逐行复读 + 18 项哈希逐一比对 + `npm test` 两轮实跑 + **8 组独立探针**（dist 沙箱新场景 A1/A2/A3：陈旧产物遮蔽、0 字节产物、外部 touch；真实 main.js + 桩 fetch 诊断矩阵 B1–B5：鉴权压话术、泛化话术、混合 tally、成功路径隔离、key 回显）+ asar 整包抽取逐文件 md5 + 进程/TEMP/用户数据取证。探针全部放在 `%TEMP%`，未触碰工作区，复核结束时已清理。
**未执行 Electron 实机套件**（沿用用户既定指令，第九轮问题 4 前半仍待用户裁决）；DOM/tray 以「最近一次实跑」栏目自述 + 源码哈希未变佐证。

**基线（全部实测）**

- `审查反馈.md` md5 `70356fbfd8f5553514e04f7d9319ad79`、2375 行（§十九 在案，本轮以其为施工验收对象）；`审查报告.md` 本节落盘前 md5 `7eda124f678e246d34616aec06cb0651`、1128 行 —— 与 §十九 施工基线自述一致，session-d 期间报告未被改动。
- HEAD = `5a8ecec`，工作树 = 第十轮交付 + 第十一轮交付（均未提交）+ 两份审查文档。
- §十九.五 的 **18 项文件哈希与当前磁盘逐一比对全部一致** —— 第十一轮交付后零漂移；对照 §十八.六，变更恰好只有 5 项（main / dist.js / report-items / ipc.test / dist.test），其余 13 项逐项相同 —— **「零清单外落码」在哈希级成立**。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,621 B、md5 `8746fe4638daa4f9aeb222e322278bf4`、mtime 09-12 05:17 —— 与 §十九.四 一致；`win-unpacked/resources/app.asar` mtime 05:16（本地）与构建时刻自洽。
- `app.asar` extractAll：**6/6 运行时文件与工作区 md5 逐一相同**（main `abb1de26` = N-28 后新哈希、preload `4fdfad53`、limits `b44998c0`、renderer `c16e5e70`、index `afa1b8e9`、styles `a3cb01bf`）；包面 12 条目 = package.json + src/（assets/icon.ico + lib + renderer），tray.png / test/ / tools/ = 0 条目。
- electron.exe 进程 = **0**；`providers.json` mtime `09-11 20:27` 未动；`%TEMP%` llmb-* 非本会话产物恰 **8 项**，与 §十六.四 披露逐项吻合（本复核方探针目录 `llmb-review-r12*` 已自清）。

## 结论速览

- 第十一轮清单 **4/4 闭合属实**：N-27 取 (a) 并做了**必要且正确的细化**（快照在尝试循环内逐次拍 + 签名 `mtimeMs:size` 比对而非名字比对），dist.test 3→7 全绿；N-28 取 (a)，清单四条约束逐条对账成立；`npm test` 两轮实跑 **190/190**（report-items 79 + limits 52 + ipc 52 + dist 7，exit 0），自述数字全部可复点。
- **零清单外落码属实**（哈希级对表，见基线）；asar 与工作区逐字节一致，包面干净。
- 新发现 **1×P3 + 2 记账**：N-29 —— `findSetupArtifact` 只检查目录序**第一个** Setup*.exe，首个是陈旧产物时把「存在本次新产物」误判为「没落产物」（探针 A1 坐实：假失败 + 清掉新鲜产物 + 多烧一轮完整构建 + 诊断语失实）。失败安全（任何路径都不会错误接受非本次产物）且重试自愈，故 P3。
- **裁决（§十九.二/七.2 留给复核方的「泛化 message 口径」）：接受现状——话术优先、tally 兜底，不设过滤；但必须转正为测试钉住**（理由四条见本轮第五节）。

## 〇、对表：第十一轮清单 4 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（必须） | N-27 产物关卡只认本次新落的产物 + Case 4/4b | ✅ | `tools/dist.js:139` 快照在尝试**循环内**（每次 spawn 前各拍一次——这是 S2「终局保留现场」路径被堵住的关键，终局开局时拍的快照已含 attempt-2 半成品）；`:114-127` 签名比对（`mtimeMs:size`，同名覆盖得新 mtime 放行）；`test/dist.test.js` 7/7 随 `npm test` 两轮实跑全绿；报告建议的「终局 exit 0 且无新产物时不得输出 artefact 行」已落进 Case 4/4b/5 的断言（`dist.test.js:152/:162/:169`）；「predates this attempt」诊断行在位（`dist.js:149`）；字面守卫 + 分工标签在位（`report-items.test.js:429`）。**我方沙箱在 7 案之外另补两格**：0 字节新鲜产物 → EXIT=1（A2，7 案未覆盖 → 随清单转正为 Case 7）；外部 touch 陈旧产物 → 被放行（A3 → 记账①） |
| 2（建议） | N-28 (a) 行为补齐 + 四条约束 | ✅ | `gatewayMessage`（`main.js:469-473`）+ `tryFetchJson` 非 2xx 有界解析（`:687-699`，解析失败 bodyMessage 留空）+ `note()` 统一走 safeMessage 进 diag（`:572-573`）。四条约束逐条对账：**鉴权分支优先级保持**（探针 B1：401+话术 → 鉴权文案）；**解析失败不产生新计数类别**（⑤v5b 实跑 + 断言禁幻影类别）；**脱敏**（探针 B5：404 body 回显完整 key → `***`，零泄漏）；**⑤v5 转正**（`ipc.test.js:551`，断言 tally 说 4 不说 3、且禁「所有候选路径均返回 404」回潮）。`:505-510` 注释由「碰巧为真」变「必须为真」，与实现一致 |
| 3（流程） | 「最近一次实跑」栏目照旧维护 | ✅ | `test/dom/main.js 72e4934e`、`stub-preload 5756264c`、`tray.test 45718a86` 三哈希实测与 §十八.六 相同 —— 「本轮零改动」属实；§十九.三 如实标注「本轮未重跑、沿用 04:0x 记录」而非冒领；实跑权限裁决继续继承（见待办 #4） |
| 4（流程） | 自述数字与实点对齐 | ✅ | 79+52+52+7=190 两轮实跑吻合；「+2 守卫块/+4 断言」（N-27 守卫 2 matches + N-28a 守卫 2 matches）、「+2 用例块/+8 断言」（⑤v5 四断言 + ⑤v5b 四断言，逐条数过）、「+4 用例块/+13 断言」（Case 4 五 + Case 4b 两 + Case 5 三 + Case 6 三，逐条数过）——与 §十九.三 表逐格相符 |

## 一、N-27 核验：修法论证对表 + 我方沙箱补格

§十九.一 的 (a)/(b)/(c) 论证经得起对表：

- **快照在循环内而不是 spawn 前一次**：这是清单外的一处关键实现选择（报告原文只说「spawn 前快照」）。若快照只在第 1 次尝试前拍，attempt-2 写出的半成品在终局尝试（不清场）时签名不在快照内 → 会被当成新产物放行——恰是 S2。现在的逐次快照 + Case 5 实跑双确认堵死。**这一步细化与「签名而非名字」的细化同 worth 记账：两条都是报告没明说、修错就破功的自由度。**
- **(b) 被否的理由成立**：启动清场发生在很久之前，管不到终局尝试开局时的 dist/（终局保留现场是第九轮杂项②的设计，Case 3 实测确认）。
- **(c) 被否的理由成立**：before/after 两次 stat 的同一性比较不依赖时钟语义；mtime 门槛要论证快照时点与文件时点的粒度关系。

我方沙箱补充（`%TEMP%/llmb-review-r12-dist`，复制真实 `tools/dist.js` 原件 + 桩 builder，方法与 `test/dist.test.js` 同构）：

```
A2  builder exit 0 写出 0 字节 Setup.exe
    → EXIT=1、无 artefact 行、stderr "no Setup.exe landed" / "build failed after 3 attempts"
    （size 守卫在「新鲜」路径上同样有效；现有 7 案只覆盖「无文件」与「陈旧文件」两格 → 随 N-29 转正为 Case 7）

A3  builder exit 0 仅 utimesSync 触碰陈旧产物（内容不变）
    → EXIT=0、attempts=1、artefact 行、放行内容仍是 "STALE-0.0.9-FROM-PREVIOUS-RUN"
    （签名 = mtimeMs:size 的固有盲区：mtime 失配即放行，不验内容 → 记账①）
```

## 二、N-28 核验：探针 B 组（原文，真实 main.js + 桩 fetch）

```
B1  self=401+{message:'quota exhausted at gateway'}, 其余 3×404
    → API Key 无效或权限不足（HTTP 401），请检查该订阅的 Key 是否填错/已失效
      （鉴权优先级未动；话术被 note() 收进 diag.messages 但鉴权分支先返回、不透出
        ——与 N-28 前行为一致（彼时 401 body 同样不解析），鉴权文案本就更可行动，观察项不立案）

B2  self=404+{message:'Not Found'}, 其余 3×404
    → 服务商拒绝了请求：Not Found（4 个候选返回 404）。若 Key 无误，…适配 5h/周 限额字段
      （泛化话术改走网关分支，替换了原 notFound 分支的「请确认 Base URL 是否指向中转站的根地址」
        ——§十九.二 披露的语义变化，实测吻合；裁决见第五节）

B3  self=404+{message:'gateway says no'}, 2×404, 1×ECONNREFUSED
    → 服务商拒绝了请求：gateway says no（3 个候选返回 404、1 个无响应）。…（三类 tally 如实 ✓）

B4  self=404+{message:'nope'}, token=200 有效数据（used:300/total:1000）
    → ok=true  fiveHourPct=30  weeklyPct=null（说话的 404 兄弟不影响成功路径 ✓）

B5  self=404+{message:'bad key sk-probe-key-123456 rejected'}, 其余 3×404
    → 服务商拒绝了请求：bad key *** rejected（4 个候选返回 404）。…
      key leaked?=false  *** present?=true（非 2xx 新路径不是 key 回显通道 ✓）
```

## 三、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | report-items **79** + limits **52** + ipc **52** + dist **7** = **190/190**，exit 0（自述 190 ✓） |
| 语法 | `node --check` ×4 | 4/4（main / limits / dist.js / dist.test；limits 未变沿用） |
| N-27 沙箱 | 复制真件 + 桩 builder，3 新案 | A1 陈旧产物遮蔽新产物 → **假失败坐实（N-29）**；A2 零字节拒收 ✓；A3 touch 放行（记账①） |
| N-28 矩阵探针 | 真实 main.js + 桩 fetch，5 格 | B1 鉴权压话术 ✓；B2 泛化话术落网关分支（§十九.二 披露属实）；B3 混合 tally 如实 ✓；B4 成功路径隔离 ✓；B5 key 零泄漏 ✓ |
| asar | `@electron/asar` extractAll + 逐文件 md5 | 6/6 与工作区逐字节一致；包面 12 条目，tray.png / test/ / tools/ = 0 |
| 文件系统 / 进程 / 用户数据 | tasklist + stat 取证 | electron.exe = 0；providers.json mtime `09-11 20:27` 未动；%TEMP% 非本会话产物 8 项与披露吻合；本复核方探针目录已清理 |

## 四、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-29 | **P3** | `tools/dist.js:117`（`.find()` 首个命中）+ `:148-150`（诊断 hint） | **关卡的首个命中被陈旧产物遮蔽 → 假失败 + 多烧一轮完整构建 + 诊断语失实**。`findSetupArtifact` 只检查目录序第一个 Setup*.exe：若它是快照内的陈旧产物，立即 `return null`——**不检查同目录是否还有本次新落的产物**。探针 A1：预置 `…-0.0.9-Setup.exe`（字典序在 0.1.0 之前）+ builder exit 0 且每次都写新鲜 0.1.0 → attempt 1 报「no Setup.exe landed」（此时新产物明明在盘上），hint 称「predates this attempt」（失实——存在的是新产物，只是被遮蔽）→ `clearDist()` 把新鲜产物**连同**陈旧产物一并清掉 → attempt 2 重新完整构建后才放行（实测 attempts=2）。**可达性**：启动不清场是 (a) 修法的既定前提（(b) 被否，§十九.一），版本号变更后 dist/ 留旧版 Setup.exe 是常态；触发只需旧版文件名字典序在前。**失败安全**（任何路径都不会错误接受非本次产物）且重试自愈，故 P3 而非 P2。修法：遍历**全部** Setup*.exe，任一签名不在快照中即命中；全部命中才报「无新产物」；hint 同步改准 |
| 记账① | — | `tools/dist.js:122` | A3：外部 `utimes` 触碰（改 mtime、不改内容）会让陈旧产物以「新签名」通过关卡。electron-builder 自身不会触碰无关文件，本关卡的威胁模型（防构建事故、非对抗进程）内可接受；若要闭合，内容哈希（81MB md5 亚秒级）即可，是否值得由修复方裁量，本轮不立案 |
| 记账② | — | `test/dist.test.js` | A2 的「0 字节新鲜产物」格子 7 案未覆盖（现有只测「无文件」与「陈旧文件」）。已随 N-29 清单要求转正为 Case 7 |

### N-29 沙箱探针输出（原文）

```
A1  预置 dist/LLM Usage Limit Board-0.0.9-Setup.exe（"STALE-0.0.9-FROM-PREVIOUS-RUN"）
    + builder exit 0 且每次尝试都写新鲜 0.1.0 Setup.exe
    attempt 1 stderr:
      [dist] electron-builder exited 0 but no Setup.exe landed in dist/ — treating as a failure
      [dist]   (a Setup.exe exists in dist/ but predates this attempt — not accepting it …)
    EXIT=0  attempts=2   artefact: …0.1.0-Setup.exe (13 bytes)  内容 "fresh-build-2"
    （新鲜产物被 attempt-2 前的 clearDist 连带清除，由第二次完整构建重新产出——多烧一轮）

A2  builder exit 0 写出 0 字节 Setup.exe
    EXIT=1  无 artefact 行  stderr 含 "no Setup.exe landed" / "build failed after 3 attempts"

A3  builder exit 0 仅 utimesSync 触碰 0.0.9 陈旧产物
    EXIT=0  attempts=1  artefact: …0.0.9-Setup.exe (29 bytes)  内容 "STALE-0.0.9-FROM-PREVIOUS-RUN"
```

## 五、裁决：泛化 message 口径（回答 §十九.二 / §十九.七.2）

**裁决：接受现状——话术优先、tally 兜底，不设过滤；但该行为必须转正为测试钉住（清单 #2）。** 理由四条：

1. **tally 恒在**：无论话术说什么，括注里的「4 个候选返回 404」始终如实——状态码事实不丢，用户可行动信息中最硬的一类（查 Base URL / 查状态码）仍可得。
2. **黑名单不可枚举**：「泛化话术」（Not Found / Forbidden / …）因网关而异，任何枚举式过滤最终都会误吞某个网关的**真实自定义**话术——丢可行动信息的代价高于展示一句泛化话术的代价。
3. **语义一致性**：200 路径自初版起同一暴露（200+`{message:'ok'}` 同样占住网关分支），只给非 2xx 路径加过滤会制造两套口径。
4. **与既有断言不冲突**：⑤v5 的 'gateway says no' 不是状态短语，也没有过滤可触发。

附带说明：这是**口径裁决**，不是对 N-28 修复的否定——修复把「注释宣称的场景」变成真路径，方向正确；转正用例（⑤v6）的目的是把 §十九.二 的「附带观察」从口供变成回归，防未来无意引入过滤或无意回归。

## 六、要求下一轮反馈回答的问题

1. **N-29**：修法是否按清单（遍历全部匹配 + hint 改准 + Case 7 转正 + 可选 A1 场景用例）？若另有取舍，说明理由。
2. **⑤v6（裁决落实）**：转正用例是否照做？若认为「Not Found」不该走网关分支，请给出反例场景与理由——裁决可以重开，但要拿场景说话。
3. **提交状态**：第十/十一/十二轮已复核交付仍整体压在工作树（HEAD 仍 `5a8ecec`）——请说明提交计划（分层提交或下一轮一并提交），保持审查链路的 git 可溯性。

## 待办清单（第十二轮 · 交回修改）

> 第十一轮清单 4 条全部闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改关卡/解析函数必须带反例测试；交付说明附源码哈希清单。

```
[建议]
1. N-29  tools/dist.js findSetupArtifact：遍历全部 Setup*.exe，任一签名不在本次
         快照中即命中；全部命中快照才报「无新产物」。诊断 hint 同步修正
         （存在被遮蔽的新产物时不得声称 "predates this attempt"）。
         dist.test 补 Case 7：builder exit 0 写出 0 字节 Setup.exe → 必须非零
         退出（探针 A2 转正）；可顺带 A1 场景用例（预置陈旧产物 + builder
         直接写新鲜产物 → 首次尝试即放行、builder 调用次数 === 1）。

2. ⑤v6  泛化 message 口径已裁决（本轮第五节：接受现状、不设过滤）：以 1 个
         用例块转正钉住——404+{message:'Not Found'} → 网关分支头部 +
         tally 如实列出（防未来无意引入过滤或回归）。

[流程]
3. 提交：三个已复核轮次的交付仍整体压在工作树（HEAD 5a8ecec）——给出提交
   计划（分层或一并），保持审查链路 git 可溯。

4. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承）。
```

---

# 方法论补记（第十二轮）

1. **「存在」是 ∃ 命题，别让首次命中替它发言。** `findSetupArtifact` 用同一次 `.find()` 同时干「找产物」与「判新旧」两件事，而「存在本次产物」量化的是**全部**匹配——首命中 + 早退把 ∃ 写成了关于第一个元素的命题，于是「第一个是旧的」被说成了「没有新的」，连累建立在同一窄视野上的诊断信息（hint 在被遮蔽场景说了假话）。判存在用 any（遍历、任一即可），判全否用 every；两个量词共用一个实现时，实现必须配得上较弱的那一个。
2. **失败安全也要为误报买单。** N-29 从不错误放行（安全性质完好），却以「假失败 + 清掉新鲜产物 + 一轮完整重建 + 失实诊断」为代价。此前三轮对这道关卡的全部探针都在打「漏报」方向（exit 0 无产物 / 陈旧产物 / 半成品），「误报」方向的第一格由本轮 A1 补上——**同一不变量有正反两面之外还有第三面：检查拒绝时给出的理由也必须真。**

---

# 第十二部分 · 第十三轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §二十（第十二轮处置，session-e）+ 工作树未提交改动（本轮声称改动 = `tools/dist.js`、`test/dist.test.js`、`test/ipc.test.js`、`test/report-items.test.js` 四文件 + 本文档，§二十.五 哈希清单可对表）

方式：全源码逐行复读（`tools/dist.js` 176 行全文、`test/dist.test.js` 250 行全文、`ipc.test.js` ⑤v6 块、`report-items.test.js` N-27/N-29 守卫块、`main.js` 诊断段）+ **19 项哈希逐一比对**（§二十.五 vs §十九.五 对照，含 dist 产物）+ `npm test` 两轮实跑 + `node --check` ×6 + **6 组独立探针**（dist 关卡补格 C1/C2/C3：陈旧+零字节并存、顺序反转、纯零字节；诊断矩阵补格 D1/D4/D5：话术+不可用混合 tally、标量 JSON body、error 键链）+ asar `extractAll` 逐文件 md5 + 进程/TEMP/用户数据取证。探针全部放在 `%TEMP%` 随机命名目录，复核结束时已清理。

**未执行 Electron 实机套件**（沿用用户既定限制——第九轮问题 4 前半，本轮为**连续第五轮**请求用户裁决）；DOM/tray 以「最近一次实跑」栏目自述 + 三哈希未变佐证。

**基线（全部实测）**

- `审查反馈.md` md5 `38bd5a65047545801ec9811a05bd2594`、2582 行（§二十 在案，本轮以其为施工验收对象）；`审查报告.md` 本节落盘前 md5 `ad1e3b6a4377559e870ae7a838ba904a`（wc -l 实测 1291 行，§二十 自述 1292——末行换行口径差，以 md5 一致为准）——session-e 期间报告未被改动。
- HEAD = `5a8ecec`，工作树 = 第十轮交付 + 第十一轮交付 + 第十二轮交付（均未提交）+ 两份审查文档。`git status` 逐文件对表 = 三轮已复核变更集之并集（package.json / limits.js / main.js / ipc.test / limits.test / report-items / tmp-profiles / dist.js + 未跟踪 dist.test.js），**无第四类来源**。
- §二十.五 的 **19 项文件哈希与当前磁盘逐一比对全部一致**——第十二轮交付后零漂移；对照 §十九.五，本轮变更恰好 4 项（dist.js `7a994251`、report-items `102e1f2e`、ipc.test `303c14e7`、dist.test `7153f1a2`），src 六文件逐项未变——**「零清单外落码」在哈希级成立**，§二十.四 的 asar 沿用前提（src 零改动）随之成立。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,621 B、md5 `8746fe4638daa4f9aeb222e322278bf4`、mtime 09-12 05:17——与 §十九.四 一致（本轮未重打包；dist.js 是构建期工具、不在包面，N-29 不触碰 asar）。
- electron.exe 进程 = **0**（测试前后）；`providers.json` mtime `09-11 20:27` 未动；`%TEMP%` llmb-* 测试与探针前后恰 **8 项**（与 §十六.四 披露逐项吻合；dist 沙箱 spawn 零残留）。

## 结论速览

- 第十二轮清单 **4/4 闭合属实**：N-29 按清单修法落地且**无另有取舍**——`findSetupArtifact` 改为遍历全部 Setup*.exe 的 ∃ 判定（`dist.js:114-137`），诊断 hint 的可达域收窄到「结构性为真」的状态（论证见一）；`dist.test` 7→9 案（Case 7 四断言、Case 8 五断言，逐条实点）。⑤v6 照第十二轮裁决落实（3 断言，未重开裁决，四条理由表态认同）。`npm test` 两轮实跑 **80+52+53+9 = 194/194**，exit 0，自述数字全部可复点。
- **零清单外落码属实**（哈希级对表，见基线）。
- 新发现：**无新编号缺陷**（N-24→N-29 连续三轮修复链上第一次）；2 条记账级 + 1 个矩阵组合格缺口转为待办建议。`findSetupArtifact` 本体经我方沙箱三格补探未再发现漏判路径。
- **单笔提交解除待触发**：§二十.三 的提交计划经本部分复核通过，按清单 #1 执行（操作提醒见五）。

## 〇、对表：第十二轮清单 4 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（建议） | N-29 遍历全部匹配 + hint 改准 + Case 7 + A1 场景用例 | ✅ | `dist.js:125-132` walk-all（任一签名不在快照即命中；全部命中才 null）；0 字节闸门在 walk 内（`:129`）；hint 文案改复数可证形（`:163`）且其可达域论证结构性成立；`dist.test.js:208-240` Case 7/8 实跑通过；字面守卫重审 + ∃ 守卫在位（`report-items.test.js:429-439`） |
| 2（建议） | ⑤v6 裁决落实 | ✅ | `ipc.test.js:585-601`：404+`{message:'Not Found'}` → 网关分支头部 + tally「4 个候选返回 404」+ 禁 notFound 回潮，3 断言实跑通过 |
| 3（流程） | 提交计划 | ✅（方案） | 一并单笔 + 消息格式已定（§二十.三）；本部分复核通过即解除待触发，执行回执进下一轮反馈 |
| 4（流程） | 实跑栏目照旧 | ✅ | 三哈希（dom/main、stub-preload、tray.test）实测与 §十九.五 相同——「零改动沿用」属实；§二十.四 如实标注「本轮未重跑」而非冒领 |

## 一、N-29 核验：修法对表 + hint 可达域 + 我方沙箱补格

**walk-all 的 ∃ 语义正确**：`findSetupArtifact` 现在对全部 Setup*.exe 求值——0 字节跳过（A2 口径）、快照签名命中跳过（陈旧口径）、**任一**新鲜签名即返回（∃），循环自然结束（全部为陈旧/空）才报 null。与清单三要素逐条相符，无自由度偏离（多新鲜产物记首个已在 §二十.一 披露，∃ 语义下任一均真）。

**hint 的「必须为真」论证成立**（对照第十一轮方法论 #2 的标准）：hint 行（`dist.js:158-164`）只在 `findSetupArtifact(known) === null` 且 `findSetupArtifact(new Map())` 为真时可达——前者⟺每个非空 Setup.exe 的签名都在本次快照内，后者⟺存在非空 Setup.exe。两条件合取下「every non-empty Setup.exe predates this attempt」**恒真**：N-29 修复前 A1 场景里那句失实诊断（存在新鲜产物却说 predates）在现在的控制流下结构上不可达。我方按第十一轮方法论逐字核过这条注释，它现在配得上行为。

我方沙箱补格（`%TEMP%`，复制真实 `tools/dist.js` 原件 + 桩 builder，与 `test/dist.test.js` 同构；9 案之外的三格）：

```
C1  预置 0.0.9 陈旧产物（字典序在前）+ builder 每次写出 0 字节 0.1.0
    → EXIT=1  attempts=3  无 artefact 行
      hint=TRUE（此时唯一的非空 Setup.exe 是 0.0.9 陈旧产物——「every non-empty
      … predates」为真）  final="build failed after 3 attempts"
    （Case 4 × Case 7 的组合格：陈旧与零字节并存时两个拒收口径互不干扰 ✓）

C2  预置 0.2.0 陈旧产物（字典序在后）+ builder 首试写新鲜 0.1.0
    → EXIT=0  attempts=1  artefact 行记 0.1.0  0.2.0 陈旧产物原样幸存
    （Case 8 的顺序反转格：walk 与目录序无关 ✓）

C3  仅预置 0 字节 Setup.exe + builder exit 0 不落产物
    → EXIT=1  无 artefact 行  hint=FALSE（find(new Map()) 对纯零字节目录为 null）
    （hint 可达域的反向格：没有非空 Setup.exe 时 hint 正确地沉默 ✓）
```

## 二、⑤v6 核验 + D 组探针（原文，真实 main.js + 桩 fetch）

⑤v6 用例块与 §二十.二 引文逐字一致，3 断言实跑通过；「不重开裁决」的表态与四条理由的认同成立——我方同样构造不出「泛化话术该被吞」的反例场景。

本轮另补三个**既有 5 格矩阵未覆盖**的组合格（`ipc.test.js` 的 ⑤ 系全部只喂 `message` 键与 JSON 对象 body；`gatewayMessage` 的 `error`/`msg` 键与标量 body 在全部测试中零覆盖——`grep "body: { error" test/` 无命中）：

```
D1  self=404+{message:'busy now'}, token=200+非JSON(不可用), 其余 2×404
    → 服务商拒绝了请求：busy now（3 个候选返回 404、1 个有响应但内容不可用）。若 Key 无误，…
    （话术 ×「内容不可用」× 404 三类首次同格：tally 两类并列、说话候选计入 404
      ——N-26 全集合语义在含 unusable 的混合下依然成立 ✓）

D4  self=404+body 为 JSON 标量（'"a plain string body"'）
    → 服务商没有提供可用的用量接口（所有候选路径均返回 404）。…
    （标量 body 走 N-28 新路径：typeof 闸门拦住，不伪造话术、不崩溃 ✓）

D5  self=404+{error:'denied by upstream'}
    → 服务商拒绝了请求：denied by upstream（4 个候选返回 404）。…
    （error 键链在非 2xx 路径可达 ✓——此前 ⑤ 系全部只测 message 键）
```

**复核方自我纠错（按惯例记录）**：D1 首跑 FAIL，root cause 是**我自己的断言**——我把说话候选（self 的 404）从 404 计数里剔了出去（断言「2 个候选返回 404」），而实现按 N-26 全集合语义正确地计 3（4 候选 = 3×404 + 1×不可用）。修正断言后 3/3 转绿。「断言失败先怀疑断言」对复核方同样适用。

## 三、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | report-items **80** + limits **52** + ipc **53** + dist **9** = **194/194**，exit 0（自述 194 ✓） |
| 语法 | `node --check` ×6 | 6/6（main / limits / dist.js / dist.test / ipc.test / report-items） |
| 哈希对账 | md5sum ×19 | 19/19 与 §二十.五 / §十九.五 一致；变更恰 4 项 = 清单落点 |
| N-29 沙箱 | 复制真件 + 桩 builder，3 新案 | C1 陈旧+零字节并存双拒 + hint 为真 ✓；C2 顺序反转一试即过 ✓；C3 纯零字节时 hint 正确沉默 ✓ |
| 诊断矩阵 | 真实 main.js + 桩 fetch，3 新格 | D1 话术+不可用混合 tally ✓；D4 标量 body 不伪造话术 ✓；D5 error 键链 ✓ |
| asar | `@electron/asar` extractAll + 逐文件 md5 | 6/6 与工作区逐字节一致（main `abb1de26` / limits `b44998c0` / renderer `c16e5e70` / preload `4fdfad53` / index `afa1b8e9` / styles `a3cb01bf`）；包面 12 条目，tray.png / test/ / tools/ = 0 |
| 文件系统 / 进程 / 用户数据 | tasklist + stat 取证 | electron.exe = 0（前/后）；providers.json mtime `09-11 20:27` 未动；%TEMP% llmb-* 前 8 → 后 8（dist 沙箱 + 本复核方探针目录零残留） |

## 四、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| （无新编号缺陷） | — | — | 连续三轮修复链（N-24→N-29）上第一次零新缺陷；`findSetupArtifact` 本体经 C 组三格补探未再发现漏判/误判路径 |
| 记账① | — | `test/report-items.test.js:436` | N-29 守卫的循环正则 `for \(const [A-Za-z] of …\)` 只匹配**单字母**循环变量——将来一次无害重命名（`f`→`file`）会让守卫误报红。守卫标签自称「只防字面回潮」，但现状连合法等价写法也拦；下次触碰该守卫时放宽为 `[A-Za-z]+` 即可，不单独立案 |
| 记账② | — | `test/ipc.test.js` ⑤ 系 | N-28 新路径（非 2xx bodyMessage）的反例覆盖有缺口：`gatewayMessage` 的 `error`/`msg` 键与标量 JSON body 在全部测试中零覆盖（D4/D5 坐实行为正确但无回归钉）。随待办 #2 转正 |

## 五、对「单笔提交」的裁决（清单 #3 → 解除待触发）

§二十.三 的单笔方案（分层需拆同一函数的连续重写、中间态无法独立绿）我方维持认可：复核方对**最终态**已四轮实测（182/190/194/194），单笔 = 把已复核的最终态入库，git 可溯性由提交消息承载。**本部分复核通过，提交解除待触发**。执行操作提醒（对着仓库现状说，不是新要求）：

1. `test/dist.test.js` 目前**未跟踪**——`git add` 必须显式包含（§二十.八 自己点过名的同类风险：它已在 test 链末位，提交后的树缺它 `npm test` 必红）。
2. 两份审查文档随提交入库（审查报告将含本部分，落盘哈希与 §二十.五 不同属预期——文档在提交前最后一刻才冻结）。
3. `dist/` 在 `.gitignore`，安装包不入库，与 §二十.三 「18 项 + 两份文档」的清单一致。
4. 执行后以 `git show --stat` 回执（下一轮反馈第一条）。

## 六、要求下一轮反馈回答的问题

1. **提交执行**：commit hash + `git show --stat` 回执，并自证 18 项 + 两份文档全部入库、`test/dist.test.js` 未漏（提交后的树上 `npm test` 全绿即为硬证据）。
2. **D 组转正（若采纳待办 #2）**：给出 3 用例块的落点与断言数；若不采纳，给出理由（例如认为 D4/D5 属理论格）。
3. **Electron 实跑权限（继承，第五轮请求）**：DOM/tray 实跑权限是否对复核方解除，请用户裁决——本栏目已连续五轮以「待用户裁决」结转，用户一句话即可关闭这条悬案。

## 待办清单（第十三轮 · 交回修改）

> 第十二轮清单 4 条全部闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改关卡/解析函数必须带反例测试；交付说明附源码哈希清单。

```
[必须]
1. 提交  执行 §二十.三 的单笔提交：内容 = §二十.五 18 项 + 两份审查文档；
         显式 git add 未跟踪的 test/dist.test.js；dist/ 不入库。消息按 §二十.三
         提案（fix: act on review rounds 10-12 (N-24..N-29, ⑤v6) [session-e]）。
         执行后 git show --stat 回执 + 提交后树上 npm test 全绿自证。

[建议]
2. D 组补格转正（N-28 新路径反例覆盖收尾）：D1 话术+「内容不可用」混合 tally、
   D4 标量 JSON body 不伪造话术、D5 error 键链 —— 3 用例块 / 约 9 断言进
   test/ipc.test.js。（C 组三格由 Case 4/7/8 的组合逻辑覆盖，转正收益低，不强求。）

[记账]
3. N-29 守卫循环变量正则 [A-Za-z] 放宽为 [A-Za-z]+（下次触碰该守卫时顺带，不单独立案）。

[流程]
4. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承第五轮）。
```

---

# 方法论补记（第十三轮）

1. **单类格全绿 ≠ 组合格安全。** 诊断矩阵的每一类格（话术格 ⑤v5/⑤v6、不可用格 ⑤b/⑤v5b、纯 404 格、503 格 ⑥）各自有断言且全绿，但「话术 × 不可用 × 404」三类共存的格子到第十三轮才第一次被踩（D1）。组合格按类别数平方增长、全踩不现实，但有一条可执行的底线：**每新增一个计数类别（N-20 加 unusable、N-28 加非 2xx 话术），就必须至少补一条它与既有类别的混合格**——类别引入时没人补，缺口就一直留着。这与第九轮「修误诊要把同一判定链所有分支过一遍」是同一条纪律的矩阵版。
2. **复核方的断言也在审计范围内。** D1 首跑 FAIL，先查实现、后查断言，root cause 在断言（把说话候选从 404 计数里剔了出去）。结果上这次失败反而多产出一条证据：实现按 N-26 全集合语义计 3，与 §二十/第十一部分引用过的「tally 计全集合」逐字一致——**断言先怀疑断言，不是修改方的单方纪律**；本条按第七轮以来的惯例记入双方共用的账本。

---

# 第十三部分 · 第十四轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §二十一（第十三轮处置，session-f）+ 工作树未提交改动（本轮声称改动 = `test/ipc.test.js`、`test/report-items.test.js` 两个测试文件 + `审查反馈.md` 本节，§二十一.五 哈希清单可对表）

方式：git 取证三件（`git show --stat` 提交回执、`--diff-filter=A` + `git ls-tree` 验 create mode、**提交树内 18 项哈希 vs §二十.五 逐项比对**）+ 工作树 18 项哈希 vs §二十一.五 逐一比对 + `npm test` 两轮实跑 + `node --check` ×2 + 工作树 diff 范围核验（恰 3 文件）+ **5 组独立探针**（E1–E5：`gatewayMessage` 键链的「非 nullish 吸收」形状，真实 main.js + electron 桩 + 桩 fetch）+ 进程/TEMP/用户数据/dist 产物取证。探针全部放在 `%TEMP%` 随机命名目录，复核结束时已清理。

**未执行 Electron 实机套件**（沿用用户既定限制——第九轮问题 4 前半，本轮为**连续第六轮**请求用户裁决）；DOM/tray 以「最近一次实跑」栏目自述 + 三哈希未变佐证（三哈希实测与 §二十.五 一致）。

**基线（全部实测）**

- HEAD = `846c84a`，其父提交实测 = `5a8ecec`——与 §二十一 开工声明「开工时 HEAD = 5a8ecec」及单笔提交叙事相容。
- 提交回执逐项相符：11 files changed, +1712/−26；author `dev <dev@localhost>`；date 2026-09-12 07:05:08 +0800；subject 与 §二十.三 冻结提案**逐字一致**；正文按 Round 10/11/12 分层列条。
- create mode 实证：`git ls-tree 846c84a test/dist.test.js` = `100644 blob`；`--diff-filter=A` 全提交恰此一项——§二十.八 点过名的「漏 add 未跟踪文件」风险确已排除。
- **提交树内 18 项哈希与 §二十.五 逐项一致**（其中 report-items `102e1f2e`、ipc.test `303c14e7` 正是 §二十.五 的值）——「提交内容 = 已复核最终态」在哈希级成立，工作树的 D 组/正则改动确在提交之后。
- 工作树 18 项哈希 vs §二十一.五：**18/18 一致**（两「变」项对上新值）；`git status` 恰 3 文件（两测试 + 反馈文档），与 §二十一.五「变更恰好 2 项 + 本节文档」对表一致——**零清单外落码**。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 81,654,621 B、md5 `8746fe4638daa4f9aeb222e322278bf4`、mtime 09-12 05:17——与第十三部分基线一致（本轮未重打包，dist.js 不在包面）。
- `审查报告.md` md5 `8e7e6a7d10a460001ac1aa25a720217a`、1438 行——session-f 期间报告未被改动 ✓；入库版 `审查反馈.md` md5 `38bd5a65047545801ec9811a05bd2594` ✓（§二十一.一 冻结口径相符；`git show | wc -l` 实测 2581 行 vs 自述 2582——末行换行口径差，沿用「以 md5 一致为准」惯例）；工作树 `审查反馈.md` 2696 行（§二十一 落盘后）。
- electron.exe 进程 = **0**（测试与探针后 `tasklist | findstr /i electron` 零匹配）；`providers.json` mtime `09-11 20:27` 未动；`%TEMP%` llmb-* 复核开始恰 **8 项**（与 §十六.四 披露逐项吻合）→ 探针期间 +2（我方外层沙箱 + 内层 mkdtemp）→ 清理后仍 8 项，零残留。

## 结论速览

- 第十三轮清单 **4/4 闭合属实**：#1 单笔提交回执全项相符且提交树 = §二十.五 最终态（哈希级），「提交后树上 194/194」由 diff 推证成立（见一）；#2 D 组 3 用例块落点 `ipc.test.js:604-653`、断言实点 4+4+4=12，与报告第十二部分 §二 三条探针逐字对应；#3 守卫正则放宽落点 `report-items.test.js:442`，`loops >= 2` 原样、对现 dist.js 仍计 2；#4 零改动三哈希属实。
- `npm test` 两轮实跑 **80+52+56+9 = 197/197**，exit 0 ×2——自述数字全部可复点。
- 新发现：**1 个新编号缺陷 N-30（P3，诊断话术）**——`gatewayMessage` 的 `??` 键链被「存在但不可用」的早位键吸收，网关真实话术被吞（E1/E2/E5 三格实证）；E3 反例证明键链设计意图是穿透，非有意取舍。另有 1 条记账结转（msg 尾键零覆盖，E4 证行为成立）。
- 哈希/进程/TEMP/用户数据/产物五路取证零漂移，无任何清单外改动迹象。

## 〇、对表：第十三轮清单 4 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（必须） | 单笔提交 + 显式 add dist.test.js + 回执 + 提交后树全绿 | ✅ | 回执逐项相符（见一）；create mode 100644 实证；树内 18 项哈希 = §二十.五；194/194 由「工作树 diff 恰为纯新增用例块」推证 + 我方 197/197 两轮 |
| 2（建议） | D 组补格转正 D1/D4/D5 | ✅ | `ipc.test.js:605/622/638` 三块实跑全绿；断言实点 12（4+4+4）；与探针语义逐字对应（D1 三类混合 tally=3+1、D4 标量 body 落 notFound、D5 error 键 tally=4） |
| 3（记账） | N-29 守卫正则 [A-Za-z] → [A-Za-z]+ | ✅ | `report-items.test.js:442`（附成因注释 ：439-441）；`loops >= 2` 断言原样；dist.js:103/:125 单字母 `f` 下新旧正则均计 2 |
| 4（流程） | 实跑栏目照旧 + Electron 权限 | ✅ | dom/main `72e4934e`、stub-preload `5756264c`、tray.test `45718a86` 实测未变——「零改动沿用」属实；裁决继续待用户（第六轮请求） |

## 一、清单 #1 核验：单笔提交——回执、树内哈希、「提交时点全绿」的推证

**回执层**：`git show --stat 846c84a` 的 11 文件清单、+1712/−26、各文件增删行数与 §二十一.一 回执逐项相符；author/date/subject/正文分层（Round 10→11→12）逐字核对无误。`test/dist.test.js` 以 create mode 100644 入库——上一轮清单 #1 专门点名的两条风险（漏 add 未跟踪文件、dist/ 误入库）分别以 `--diff-filter=A` 恰一项和 `git status` 无 dist/ 条目排除。

**内容层**：对提交树逐文件 `git show 846c84a:<path> | md5sum`，18 项与 §二十.五 **逐项一致**——提交装入的就是我方第十三部分复核过的那个最终态，不是它的近似。两份审查文档随提交入库（提交内反馈文档截至 §二十，md5 与 §二十一.一 自述一致）。

**「提交后树上 194/194」的推证**（不重跑历史时点的等价证据）：工作树相对提交的 diff 恰为——ipc.test.js +51 行（1 行分组标题 + 3 个用例块 = 恰 +3 条）、report-items.test.js ±5 行（守卫正则一处，条目数不变）。故提交树 = 工作树 − 3 条 ipc 用例，我方实测工作树 197/197 ⇒ 提交树 194/194。旧守卫正则 `[A-Za-z]` 对提交树同样成立：`tools/dist.js:103/:125` 的循环变量现仍为单字母 `f`，计 2 不红。链条闭合，§二十一.一 的自证数字成立。

## 二、清单 #2 核验：D 组转正——落点、断言实点、与探针的对应

三块落点 `test/ipc.test.js:604-653`（分组标题 ：604；D1 :605、D4 :622、D5 :638），与报告第十二部分 §二 的三条探针**逐字对应**：D1 的 self=404+`{message:'busy now'}` × token=200+jsonThrows × 2×404、断言「3 个候选返回 404」+「1 个有响应但内容不可用」并列；D4 的标量 body 落 notFound 分支 + 禁「服务商拒绝了请求」+ 禁幻影计数；D5 的 error 键话术 + tally=4 + 禁 notFound 回潮。断言实点逐条可数：每块 strictEqual(ok) + 3 条文案断言 = 4+4+4 = **12**，与 §二十一.二 自述一致（自述还如实记录了「报告估 9、实点 12」——按实点报，此风可取）。三块一次全绿（我方 `npm test` 两轮均含）。

用例注释还把三条探针的出处（report part 12 §二、`main.js:470/471` 行号锚点）写进了测试——下一轮任何人重读都能对回探针原文，此做法值得沿袭。

## 三、清单 #3 核验：正则放宽 + 清单 #4 零改动

`report-items.test.js:439-443`：正则放宽为 `for \(const [A-Za-z]+ of fs\.readdirSync\(dist\)\)`，附成因注释（不变量是「两次遍历 dist/」，不是循环变量名）；**计数断言 `loops >= 2` 原样**。这不是放松：对现 `tools/dist.js`（两处 `for (const f of …)`，:103/:125）新旧正则都计 2——80/80 两轮为证；放宽只消灭「合法重命名 f→file 的误报红」，防回潮能力不变。§二十一.三 的表述与实测相符。

清单 #4：三哈希（dom/main、stub-preload、tray.test）实测与 §二十.五 一致，「本轮未重跑 DOM/tray」的如实标注属实，沿用 §十八.四 记录的口径成立。

## 四、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | report-items **80** + limits **52** + ipc **56** + dist **9** = **197/197**，exit 0 ×2（自述 197 ✓） |
| 语法 | `node --check` ×2 | 2/2（ipc.test / report-items.test——本轮仅动这两码件） |
| 提交取证 | git show --stat / ls-tree / diff-filter=A | 回执全项相符；create mode 100644 恰一项；树内 18 项哈希 = §二十.五 |
| 哈希对账 | md5sum ×18（工作树）+ ×18（提交树）+ 3 文档 | 18/18 与 §二十一.五 一致；18/18 与 §二十.五 一致；报告 md5 未变 |
| E 组探针 | 真实 main.js + electron 桩 + 桩 fetch，5 新格 | E1/E2/E5 吸收坐实（N-30）；E3 反例穿透成立；E4 msg 尾键行为成立 |
| 文件系统 / 进程 / 用户数据 / 产物 | tasklist + stat + md5sum | electron.exe = 0；providers.json mtime `09-11 20:27` 未动；%TEMP% llmb-* 8 → 8；Setup.exe md5/大小/mtime 未变 |

## 五、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-30 | P3 | `src/main.js:471-473` | `gatewayMessage` 的键链 `body.message ?? body.error ?? body.msg` 用 **nullish** 语义串联，但下游校验要求「非空字符串」——于是「存在但不可用」的早位键（空串/纯空白/数字/对象）会**吸收**整条链：后面的 `error`/`msg` 键里即使有网关的真实话术也到不了 UI。与 N-19（空串在 Number 侧变 0）同族，属「空值吸收」家族在键链上的一处 |
| 记账③ | — | `test/ipc.test.js` ⑤ 系 | `msg` 尾键（链的第三位）仍零测试覆盖——第十三轮记账② 的 error/msg 双缺口，D5 只转正了 error 一半。E4 实证 msg 键行为成立（链尾可达、话术正常出），缺的只是回归钉 |

**N-30 探针（原文，真实 main.js + electron 桩 + 桩 fetch；`providers:save` 五订阅、`usage:fetch` 实路径，全部请求打向桩零外发）**：

```
E1  self=404 + {message:'', error:'quota exhausted'}，其余 3×404
    → 服务商没有提供可用的用量接口（所有候选路径均返回 404）。请确认 Base URL …
    （error 键的 'quota exhausted' 被空串 message 吸收——话术全丢，落 notFound 分支 ✗）

E2  self=404 + {message:503, error:'real words from error'}，其余 3×404
    → 服务商没有提供可用的用量接口（所有候选路径均返回 404）。…
    （非字符串 message 同样吸收——N-25 的「非 number/string 按缺失」口径没有覆盖到这条链 ✗）

E3  self=404 + {message:null, error:'null falls through'}，其余 3×404
    → 服务商拒绝了请求：null falls through（4 个候选返回 404）。若 Key 无误，…
    （反例格：null 正确穿透——键链的**设计意图**就是逐键穿透，
      只有「非 nullish 但不可用」的值会破坏它，故判缺陷而非设计取舍）

E4  self=404 + {msg:'words in msg key'}，其余 3×404
    → 服务商拒绝了请求：words in msg key（4 个候选返回 404）。…
    （msg 尾键行为成立——记账③ 只缺回归钉，不缺实现）

E5  self/token/status/api 全 200 + {message:'', error:'quota exhausted'}（无限额字段）
    → 服务商返回中找不到 5h/周 限额字段，请把接口 JSON 发给我适配
    （2xx 路径同型吸收：note() 走 gatewayMessage(res.data)，话术同样被吞 ✗）
```

**影响面**：纯诊断话术——失败文案把「网关说了什么」错报成「没提供接口/找不到字段」，与 N-26/N-28 修的是同一条用户旅程；不触及数据、存储、隐私与控制流（notFound/mixed/field 各分支的判定本身没有错，错的只是没先看话术）。**修法建议**（给下一轮，非强制）：键链从「取首个非 nullish 再校验」改为「按序取首个**非空字符串**」——

```js
const m = [body.message, body.error, body.msg].find((v) => typeof v === 'string' && v.trim());
return m ?? '';
```

一处修复同时覆盖 2xx（`note()` 的 `gatewayMessage(res.data)`）与非 2xx（N-28a 的 `bodyMessage`，`main.js:697`）两条路——两路共用这一个函数，反例钉落在 ipc.test.js ⑤ 系即可。E1/E2/E5 三形状转正为回归钉（今日全红），E3/E4 可一并入钉（今日全绿，钉住穿透语义与尾键）。

## 六、要求下一轮反馈回答的问题

1. **N-30**：是否采纳「按序取首个非空字符串」修法；给出落点（函数仅 5 行，预计 `main.js:471-473` 一处）与反例测试的用例块/断言数。若不采纳，给理由（例如认为「message 为空串/非字符串而 error 有话术」的网关形状罕见——请附对真实服务商返回形状的判断依据）。
2. **Electron 实跑权限（继承，第六轮请求）**：DOM/tray 实跑权限是否对复核方解除，请用户裁决——本栏目已连续六轮以「待用户裁决」结转，用户一句话即可关闭这条悬案。

## 待办清单（第十四轮 · 交回修改）

> 第十三轮清单 4 条全部闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改解析函数必须带反例测试；交付说明附源码哈希清单。

```
[建议]
1. N-30 修复：gatewayMessage（main.js:471-473）键链改「按序取首个非空字符串」
   （[body.message, body.error, body.msg].find(...) 形态或等价循环）；空串/纯空白/
   非字符串一律视为缺席，继续向后找。反例回归钉：E1 空串吸收（非 2xx）、
   E2 非字符串吸收（非 2xx）、E5 空串吸收（2xx 无限额字段）三形状转正；
   E3 null 穿透、E4 msg 尾键一并入钉。2xx/非 2xx 两路共用此函数，落 ipc.test.js ⑤ 系。

[记账]
2. msg 尾键回归钉（E4）——若 #1 采纳则并入 #1 的用例块，不单独立案。

[流程]
3. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承第六轮）。
```

---

# 方法论补记（第十四轮）

1. **`??` 是「缺席」的错译。** `a ?? b` 只跳过 null/undefined，而消费方（`typeof m === 'string' && m.trim()`）定义的「缺席」是「非空字符串」——两个定义的差集（空串、数字、对象）就是吸收发生的地方。N-19（`Number(' ') === 0`）与 N-30 是同一族：**「缺」要按消费方的类型定义，不能拿 nullish 近似**。键链的正确形态是「按序找第一个满足类型的非空值」，而不是「取第一个非 nullish 再校验」——校验放在链后，校验不过时链已经断了。
2. **「提交时点全绿」不必 time machine。** 无法回到历史时点重跑时，若工作树相对提交的 diff 恰为**纯新增用例块**（本轮实测：ipc 恰 +3 条、report-items 仅守卫正则一处、条目数不变），则「树上绿 = 工作树绿 − 新增块数」是一条完整的推证链；再补一个「旧守卫对现循环变量名不红」的点位检查（dist.js 仍为单字母 `f`）即闭合。比 stash 重跑便宜，证据等级相当——前提是 diff 范围必须先核到「恰为纯新增」，这个前提要用 git diff 逐行看，不能转抄自述。

---

# 第十四部分 · 第十五轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §二十二（第十四轮处置，session-g）+ 工作树未提交改动（本轮声称改动 = `src/main.js`、`test/ipc.test.js` 两码件 + `审查反馈.md` 本节；工作树另载第十三轮已复核交付 `test/report-items.test.js` 与两份审查文档）
方式：全源码逐行复读（`src/main.js` 的 `gatewayMessage` 及 `note()`/`tryFetchJson` 两调用点、`test/ipc.test.js` E 组 5 块全文）+ 18 项哈希逐一比对 + git diff 逐 hunk 对表 + `npm test` 两轮实跑 + `node --check` ×2 + **两组独立探针**（红侧：旧 `??` 链补丁副本 ×3 格，复刻 §二十二.一 的修前对照；绿侧：F 组 8 格，真实 main.js + electron 桩 + 桩 fetch 的键链补格、脱敏与跨模式核验）+ asar `extractAll` 逐文件 md5 + src 全量 `??` 同族扫查 + 进程/TEMP/用户数据取证。探针全部放在 `%TEMP%`，复核结束时已清理（含一次装置崩溃孤儿的补清，见记账②）。
**未执行 Electron 实机套件**（沿用用户既定限制——第九轮问题 4 前半，本轮为**连续第七轮**请求用户裁决）；DOM/tray 以「最近一次实跑」栏目自述 + 三哈希未变佐证。

**基线（全部实测）**

- `审查报告.md` md5 `6b4c224429df7255a83dd0ce9e512834`、1579 行——与 §二十二 开工基线自述**逐字一致**，session-g 期间报告未被改动。
- HEAD = `846c84a`（父提交实测 `5a8ecec`）；`git status` 恰 5 文件 = 第十三轮交付（report-items）+ 第十四轮交付（main.js、ipc.test）+ 两份审查文档——与 §二十二.五 三方对表一致。
- §二十二.五 的 **18 项文件哈希与当前磁盘逐一比对全部一致**；对照 §二十一.五，变更恰好 2 项（main `abb1de26`→`dd4c676a`、ipc.test `19c4c004`→`a6246938`）——**「零清单外落码」在哈希级成立**，并另行在 diff 级复核（见对表后说明）。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,968 B、md5 `3eeb22eb78c5b9c4c4ebae3d1c2f4049`、mtime 09-12 08:10——与 §二十二.四 一致；`win-unpacked/resources/app.asar` mtime 08:08 与构建时序自洽。
- electron.exe 进程 = **0**（复核前/后）；`providers.json` mtime `09-11 20:27:49` 未动；`%TEMP%` llmb-* 复核开始恰 **8 项**（§十六.四 披露逐项吻合）——探针期间一度 +1（我方装置崩溃孤儿，记账②），终态仍 8 项。

## 结论速览

- 第十四轮清单 **3/3 闭合属实**：N-30 修法与清单建议形态**逐字一致**（`main.js:476-477`），落点单一、两路调用点（2xx 的 `note()` `:577` / 非 2xx 的 `tryFetchJson` `:702`）共用确认——一处修复两路生效；E 组 5 用例块 / 19 断言实点可数（4+5+3+3+4），落点 `ipc.test.js:655-740`（自述区间尾端 :737 与实读块尾差 3 行，行号口径，各计数全部吻合，不立案）；`npm test` 两轮实跑 **202/202**（report-items 80 + limits 52 + ipc 61 + dist 9，exit 0 ×2），自述数字全部可复点。
- **「修前三格全红」被我方独立复现**：把 main.js 复制到沙箱并仅将 `gatewayMessage` 函数体补丁回旧 `??` 链（工作区零触碰），E1/E2/E5 三形状 3/3 复现修前失败文案，与 §二十二.一 红绿矩阵逐字一致——修方的 stash 对照法自证成立，且补丁副本法不触碰 git 状态，可作等价替代。
- 新发现：**无新编号缺陷**（连续第二轮零新缺陷）。F 组 8 格补探零异常，其中两条是此前零覆盖的关键格：**error 键路径的 key 脱敏**（F4，新可达路径 × key 泄漏组合格，零泄漏）与 **balance 模式共用网关分支 + tally**（F9，7 候选如实计数）。2 条记账：键链**优先级格**行为正确但零回归钉（F1）；复核方探针装置一次崩溃残留（已补清）。
- **同族扫查**：src 四文件全部 6 处 `??` 逐一过目——对象查找形态（`main.js:413/:675`、`limits.js:177`）、id 兜底（`renderer.js:321`）、显示兜底（`renderer.js:412`）、修复自身（`main.js:477`）——消费方均接受空值或两支同为对象，无「标量吸收」同族危害。**吸收家族（N-19→N-25→N-30）在 src 内清零。**

## 〇、对表：第十四轮清单 3 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（建议） | N-30 修复：键链改「按序取首个非空字符串」+ E1/E2/E5 转正 + E3/E4 一并入钉 | ✅ | `main.js:476-477` 与清单建议形态逐字一致（`[body.message, body.error, body.msg].find(...)` + `?? ''`），git diff 恰此一处（+5 行含 N-30 注释，无他动）；「缺席按消费方定义」的原理已写进源码注释（第十四轮方法论 #1 落地）；E 组落点 `ipc.test.js:655-740`，5 块 / 19 断言实跑全绿；修前三形状由我方补丁副本独立复红（见一） |
| 2（记账） | msg 尾键回归钉（E4）并入 #1 | ✅ | `ipc.test.js:706` E4 块 3 断言实跑通过（修前修后均绿的「钉语义」格，与清单「不单独立案」意见一致） |
| 3（流程） | 实跑栏目照旧 + Electron 权限继承 | ✅ | dom/main `72e4934e`、stub-preload `5756264c`、tray.test `45718a86` 实测与 §二十一.五 逐项相同——「本轮零改动沿用」属实；§二十二.三 如实标注「未重跑」而非冒领；裁决第七轮结转 |

**零清单外落码的 diff 级复核**：`src/main.js` diff = `gatewayMessage` 一处（函数体 2 行换 2 行 + 5 行注释）；`test/ipc.test.js` diff = 单一 hunk `@@ -601,6 +601,144 @@` **纯新增 138 行、0 删除**（D 组 + E 组两个分组标题即全部新增结构，D 组为第十三轮已复核交付）；`test/report-items.test.js` diff = 仅第十三轮正则放宽（`[A-Za-z]+` + 成因注释，已复核闭合）。三方与哈希对表一致。

## 一、N-30 核验：修法对表 + 红绿对照复现 + F 组补格

**红侧对照（复刻 §二十二.一，方法改良）**：把真实 main.js 复制进 `%TEMP%` 沙箱，仅将 `gatewayMessage` 函数体补丁回旧 `??` 链（`body.message ?? body.error ?? body.msg` + 字符串校验），同一探针装置分别驱动两版：

```
红侧（补丁副本，旧 ?? 链）——3/3 复红，与 §二十二.一 矩阵逐字一致
  oE1 self=404 + {message:'', error:'quota exhausted'}
     → 「服务商没有提供可用的用量接口（所有候选路径均返回 404）」，话术无踪     ✗
  oE2 self=404 + {message:503, error:'real words from error'}
     → 同上，'real words' 被数字 message 吸收                                  ✗
  oE5 全 200 + {message:'', error:'quota exhausted'}（无限额字段）
     → 「服务商返回中找不到 5h/周 限额字段」，话术被字段分支吞掉                ✗
绿侧（真实 main.js，同 harness 同断言位）——三形状全绿（E 组随 npm test 在跑）
```

同一装置、同一路由、同一断言谓词，唯一差异是被测函数体——红侧因此逻辑上归因于该缺陷而非装置漂移。补丁副本不动 git 状态（stash 在并发会话下是共享状态操作），建议作为 stash 法的等价替代入册。

**F 组补格（绿侧，真实 main.js + electron 桩 + 桩 fetch；全部断言式，8/8 通过）**：

```
F1  三键均可用 {message:'m words', error:'e words', msg:'z words'}
    → 「服务商拒绝了请求：m words（4 个候选返回 404）」，error/msg 话术均不透出
      （键链顺序语义：首个非空字符串胜出——行为正确但测试零覆盖，随待办 #1 转正）
F2  {message:'   ', error:'w words'} → 「……：w words（4 个候选返回 404）」
      （E1 的空白串变体：'' 与 '   ' 同格处理 ✓）
F3  {error:503}（非 2xx）→ 落 notFound 分支；无「：503」伪造、无网关分支
      （数字 error 键不得 String() 化——E2 的镜像格 ✓）
F4  {message:'', error:'bad key sk-probe-f4-abcdef rejected'}
    → 「服务商拒绝了请求：bad key *** rejected（4 个候选返回 404）」，
      原始 key 串零出现（N-28a 新路径 × error 键 × key 泄漏的组合格首次被踩，
      safeMessage 脱敏在新链路下游依旧生效——安全关键格 ✓）
F5  全 200 + {message:'  ', error:'words in error'}（无限额字段）
    → 网关分支 + 「请把该接口的返回 JSON 发给我适配 5h/周 限额字段」，
      字段分支措辞未回潮（E5 的空白串变体 ✓）
F7  {message:{deep:'x'}, error:'obj falls through'} → 「……：obj falls through」
      且无「[object Object]」（对象早位键跳过、不 String() 化 ✓）
F8  三键全空白 {message:' ', error:'   ', msg:'  '} → 落 notFound 分支，
      无「服务商拒绝了请求：」（空气话术不伪造网关分支 ✓）
F9  balance 模式：全部候选 404 + {message:'', error:'insufficient quota'}
    → 「服务商拒绝了请求：insufficient quota（7 个候选返回 404）。……适配余额字段」
      + requests = 7（balance 共用同一网关分支与 tally，N-26 全集合语义跨模式成立 ✓）
```

## 二、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2（另四套件单跑对点一遍） | report-items **80** + limits **52** + ipc **61** + dist **9** = **202/202**，exit 0 ×2（自述 202 ✓） |
| 语法 | `node --check` ×2 | 2/2（main / ipc.test——本轮仅动这两码件） |
| 修前红对照 | 补丁副本（旧 `??` 链）×3 格 | 3/3 复红，文案与 §二十二.一 逐字一致 |
| F 组补格 | 真实 main.js + 桩 fetch，8 格 | 8/8 全过（F1/F2/F3/F4/F5/F7/F8/F9），零异常 |
| 哈希对账 | md5sum ×18 + 两份文档 | 18/18 与 §二十二.五 一致；报告 md5 未变 |
| asar | `@electron/asar` extractAll + 逐文件 md5 | 6/6 与工作区逐字节一致（main `dd4c676a` = N-30 后新哈希）；包面 package.json + src/（assets/icon.ico + lib + renderer），tray.png / test / tools = 0 |
| 同族扫查 | `??` 全量清点 ×src 4 文件 | 6 处逐一过目，无「标量吸收」同族危害（见结论速览） |
| 文件系统 / 进程 / 用户数据 | tasklist + stat 取证 | electron.exe = 0（前/后）；providers.json mtime 未动；%TEMP% llmb-* 8 → 9（我方孤儿）→ 8 |

## 三、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| （无新编号缺陷） | — | — | 连续第二轮零新缺陷；`gatewayMessage` 修后本体经 F 组八格补探（含安全格 F4、跨模式格 F9、三镜像格 F3/F7/F8）未发现漏判/误判路径 |
| 记账① | — | `test/ipc.test.js` E 组 / `report-items.test.js:447` | **键链优先级零行为覆盖**：三键均可用时 message 胜出的顺序语义（F1 坐实行为正确）没有任何断言钉住——D5/E1–E4 全是单键格或早位键坏格，report-items 对 `gatewayMessage` 的守卫也只是字面存在检查（`:447`）。N-30 把穿透语义的负面格（E3）钉了，正面格（优先序）还空着——随待办 #1 转正，不立案为缺陷 |
| 记账② | — | 本复核方探针装置 | 首次绿侧运行因相对路径 `MODULE_NOT_FOUND` 崩溃，mkdtemp 已建、`rmSync` 未达 → `%TEMP%` 留孤儿 `llmb-review-r15-9jQKNV` 一枚；已定位补清，终态 8 项。「清理放脚本末尾」对崩溃路径不负责——装置纪律与修方三件套同规（方法论 #2） |

## 四、要求下一轮反馈回答的问题

1. **F1 转正（待办 #1）**：是否采纳「三键均可用 → message 胜出、error/msg 不透出」的优先级格？若认为该网关形状罕见而缓办，给理由留档（合法立场）。
2. **提交计划**：第十三轮（D 组 + 守卫正则）与第十四轮（N-30 + E 组）两批已复核交付压在工作树（HEAD 仍 `846c84a`）——一并或分层？执行后 `git show --stat` 回执 + 提交后树上 `npm test` 全绿自证。本轮**无未跟踪码件**（dist.test.js 已在库），沿用第十三部分 §五 的操作提醒：两份审查文档随提交入库、`dist/` 不入库。
3. **Electron 实跑权限（继承，第七轮请求）**：DOM/tray 实跑权限是否对复核方解除，请用户裁决——本栏目已连续七轮以「待用户裁决」结转，用户一句话即可关闭这条悬案。

## 待办清单（第十五轮 · 交回修改）

> 第十四轮清单 3 条全部闭合，无整条结转。以下为本轮新增。
> **验收要求（沿用）**：每条给「文件:行号 + 实际代码片段 + 实测输出」；新改解析函数必须带反例测试；交付说明附源码哈希清单。

```
[建议]
1. F1 优先级格转正：{message/error/msg 三键均可用} → 网关分支头部取 message、
   error/msg 话术不得透出（探针 F1 转正；1 用例块 / 约 4 断言，落
   test/ipc.test.js E 组之后即可；断言同时钉 tally=4 与 error/msg 禁透出）。

[流程]
2. 提交：两批已复核交付（第十三轮 D 组+正则、第十四轮 N-30+E 组）压在工作树
   （HEAD 846c84a）——给出提交计划（一并或分层）并执行；回执 = git show --stat
   + 提交后树上 npm test 全绿。两份审查文档随提交入库，dist/ 不入库。
3. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承第七轮）。
```

---

# 方法论补记（第十五轮）

1. **红侧证据要与绿侧同构到「只差被测体」。** §二十二.一 的 stash 对照法把「转正格修前红」从复核方单方探针升级为修方可复现自证；本轮以补丁副本复刻同构对照——同一 harness、同一路由、同一断言谓词，唯一差异是 `gatewayMessage` 函数体那两行。这样产生的「红」才在逻辑上归因于该缺陷而非装置漂移；且补丁副本不触碰 git 状态（stash 是工作区共享状态，并发会话下有竞态面），可作等价替代入册。红绿对照的成本（一次单文件补丁 + 两次套件跑）与它买到的证据等级相称，值得在「反例转正」类交付中成为固定动作。
2. **装置也要对自己的崩溃负责。** 本轮复核方探针首跑因路径错误崩溃，mkdtemp 已建、清理未达，`%TEMP%` 多出一枚孤儿（已补清，8 → 9 → 8 如实入账）。三件套纪律（进程 / 文件系统 / 用户数据）从来不是修方单方的义务：装置的临时目录应在创建后立即挂上退出清理（或把「清理未达」本身变成显式报告项）——「清理放脚本最后一行」只对正常路径负责，而验证装置恰恰是最常走异常路径的代码。

---

# 第十五部分 · 第十六轮复核

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §二十三（第十五轮处置，session-h）+ 提交 `428118b`（第十三至十五轮交付的入库态——三批交付首次以树级 diff 复阅）+ 工作树未提交改动（恰反馈 §二十三.七 提交后补记，+21 行）
方式：§二十三.一 F1 引文与实际文件逐字节比对 + 提交 `428118b` 元数据 / numstat / 逐文件 hunk 三方对表 + 18 项哈希对账 + 两份文档入库版 vs 工作树 md5 双证 + `npm test` 两轮实跑 + `node --check` ×3 + **三突变探针**（findLast / 键序倒转 / 谓词松弛——验证 F1 钉「能红」与杀伤面）+ asar `extractFile` 复核 `main.js` 哈希 + dist / 进程 / TEMP / 用户数据取证。探针全部位于 `%TEMP%`（沙箱 + 驱动脚本，finally + exit 双钩自清），轮末零残留（8 → 8）。
**未执行 Electron 实机套件**（沿用用户既定限制——第九轮问题 4 前半，本轮为**连续第九轮**请求裁决）；DOM/tray 按「最近一次实跑」栏目照旧维护，三哈希未变佐证。

**基线（全部实测）**

- `审查报告.md` 开工基线 md5 `68134479dfc7ee67d794fce55dec4b8c`、1707 行——与 §二十三 基线自述逐字一致；入库版同哈希（见对表 #2），「报告全程未被修方触碰」双证成立。
- HEAD = `428118b`（父提交实测 `846c84a`）；`git status` 恰 1 文件 = `审查反馈.md`（+21/−0，即 §二十三.七）——与该节「补记使反馈文档重新成为唯一未提交改动」的口径一致。
- 18 项哈希 vs §二十三.四：**18/18 逐项一致**——17 项「未变」逐字相同、恰 1 项「变」= `test/ipc.test.js` `0ecd5319`（F1 后新哈希），「零清单外落码」在哈希级成立。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,968 B / md5 `3eeb22eb78c5b9c4c4ebae3d1c2f4049` / mtime 08:10——未动；`app.asar` mtime 08:08；asar 内 `src/main.js` `extractFile` md5 `dd4c676a`——「本轮零 shipped-code 改动、不触发重打包」字节级成立。
- electron.exe = **0**（前/后）；`providers.json` mtime `2026-09-11 20:27:49` 未动；`%TEMP%` llmb-* 开工恰 **8 项**，两轮套件 + 本轮探针（沙箱与驱动脚本均自清）后仍 **8**。

## 结论速览

- 第十五轮清单 **3/3 闭合属实**：F1 优先级格落点 `test/ipc.test.js:742-761`（分组标题 ：742、用例块 ：743-761，行号与自述恰合；紧随 E5 块 ：722-740），**5 断言实点可数**（ok / 话术头取 message / tally=4 / error 禁透出 / msg 禁透出，两条禁透出分立），块文本与 §二十三.一 引文逐字节一致；提交 `428118b` 元数据（subject / 作者 dev / 09:13:00 +0800）、文件面（恰 5 文件）、总量（793+/4−）与回执全符，逐文件 numstat 亦符（main「9±」= 7+/2−、ipc.test「159+」、report-items「5±」= 4+/1−）。
- **F1 钉「能红」与杀伤面由我方三突变独立坐实**：沙箱副本树仅突变 `gatewayMessage` 键链一行、同一套件实跑——`findLast` 突变与键序倒转突变**恰杀一格 = F1**（敏感且特异）；「松弛成非 nullish」突变杀 **E1/E2/E5 而 F1 保持绿**——§二十三.一 的分工论断（E 组钉负面格、F1 钉顺序语义）实测成立：顺序类回归归 F1、吸收类回归归 E 组，键链回归面双向有钉。
- `npm test` 两轮实跑 **203/203**（report-items 80 + limits 52 + ipc 62 + dist 9，exit 0 ×2），自述数字可复点，F1 块 `ok` 行实点取到。
- 新发现：**无新编号缺陷**（连续第三轮）。两条记账：①提交回执逐文件记号一处不精（报告行「271 +」实为 270+/1−，−1 行是上一轮复核方摘除第十二部分「（当前）」标记的报告卫生动作，非修方触碰——入库/工作树 md5 双证）；②「（当前）」标记**七处陈旧**（第六至十、十三、十四部分并挂——摘标记从未成为固定惯例，个别轮次只顺手摘过自己前任那一处，从未全查），违反「恰一部分挂（当前）」不变量——我方落盘本部分时代为摘除七处并在此披露。

## 〇、对表：第十五轮清单 3 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（建议） | F1 优先级格转正 | ✅ | `ipc.test.js:742-761` 与 §二十三.一 引文逐字节一致；5 断言实点（含 tally=4 与 error/msg 双禁透出）；两轮套件该块 `ok`；**且我方 M1/M2 突变证实该钉能红且恰杀一格 = F1（见一）**——「正面格」从探针事实升级为带牙回归钉 |
| 2（流程） | 提交两批已复核交付 | ✅ | `428118b` 元数据与冻结提案逐字一致；`--stat` 5 文件 793+/4−；逐文件 numstat 全符；入库文档 md5：反馈 `540c31b9` ✓、报告 `68134479` ✓（= 我方开工基线 → 复核方原文随提交入库字节级成立）；dist/ 树内 **0 条目**且被 ignore；工作树 delta 恰反馈 +21 行；提交后树上 `npm test` 自证与我方两轮 203/203 一致 |
| 3（流程） | 实跑栏目照旧 + Electron 权限结转 | ✅ | dom/main `72e4934e`、stub-preload `5756264c`、tray.test `45718a86` 实测与 §二十三.四 逐项相同；权限结转至**第九轮** |

## 一、F1「有牙」独立验证：三突变探针

F1 为正面行为格（修前修后均绿），红绿对照不适用（§二十三.一 的边界记账成立）。本轮前进一步：**回归钉的价值不在绿，而在未来同类回归时能红**——把 `src/` + `test/` 复制进 `%TEMP%` 沙箱（零 node_modules 依赖，electron 同走 Module._load 桩），仅突变沙箱内 `src/main.js:476` 键链一行，同套件实跑：

```
baseline（未突变副本）        62 passed / 0 failed —— 副本忠实
M1  .find( → .findLast(       61/1 —— 恰杀一格：F1（顺序回归 → F1 红）
M2  键序 [message,error,msg] → [msg,error,message]
                              61/1 —— 恰杀一格：F1（倒序 → F1 红）
M3  谓词松成 v != null        59/3 —— 杀 E1、E2、E5；F1 保持绿（吸收回归 → E 组红）
```

敏感性：F1 注释担心的三类篡改（findLast / 反转键序 / 松弛条件）中前两类直接红。特异性：顺序类突变下 F1 是唯一被杀格——报得准、无误伤。M3 杀伤面我方推演 E1/E2、实测 E1/E2/E5：E5 是 2xx 路径同型格（'' 被拾取 → 话术消失 → 落字段分支），松弛回归在两条路径上均有钉——杀伤面比推演多一格属「钉网更密」而非异常，如实入账。**N-30 的修复语义至此在测试面闭环：负面格（E1–E4）、正面格（F1）、跨路径格（E5）齐备，键链回归面双向有钉。**

探针纪律：沙箱 mkdtemp 后 finally + process exit/uncaughtException 双钩清理；驱动脚本用毕自删；轮末 `%TEMP%` llmb-* 仍 8 项（零残留）、electron.exe = 0。

## 二、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | report-items **80** + limits **52** + ipc **62** + dist **9** = **203/203**，exit 0 ×2（自述 203 ✓）；F1 块 `ok` 实点 |
| 语法 | `node --check` ×3 | 3/3（main / ipc.test / report-items——本提交三码件） |
| 提交回执 | `git log/show/diff --numstat` | 元数据逐字 ✓；5 文件 793+/4− ✓；逐文件 7+/2−、159+/0−、4+/1−、353+/0−、270+/1−（回执「9±/5±」记号准确；「271 +」不精——记账①） |
| 文档对账 | `git show 428118b:两文档` md5 | 反馈 `540c31b9` ✓（冻结于 §二十三.六）；报告 `68134479` ✓ = 工作树 = 开工基线 |
| 哈希对账 | md5sum ×18 | 18/18 与 §二十三.四 一致；恰 1 变项 ipc.test `0ecd5319` |
| 码件 hunk 复阅 | `git diff 846c84a 428118b` 逐文件 | main.js 恰 N-30 一处（−2 `??` 链 / +5 注释 / +2 find 链 = 7+/2−）；report-items 恰第十三轮正则放宽（`[A-Za-z]+` + 成因注释，4+/1−）；ipc.test 159+/0− 纯新增（D 组 ：604– + E 组 ：655–740 + F1 :742-761）——三码件与第十二/十三/十四部分复核逐一吻合，无清单外改动 |
| asar | `@electron/asar` extractFile + listPackage | asar `src/main.js` md5 `dd4c676a` = 工作区 = 入库——「不重打包」字节级成立；包面 package.json + src/（assets/lib/renderer），test/tools/dist = 0 |
| F1 有牙 | 三突变探针（沙箱 ×3 + baseline） | baseline 62/0；M1/M2 恰杀 F1；M3 杀 E1/E2/E5、F1 绿（见一） |
| 文件系统 / 进程 / 用户数据 | tasklist + stat + ls 取证 | electron.exe = 0（前/后）；providers.json mtime 未动；%TEMP% llmb-* 8 →（套件 ×2 + 探针，全部自清）→ 8 |

## 三、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| （无新编号缺陷） | — | — | 连续第三轮零新缺陷；F1 块三突变下无误杀/漏杀，落点单一、断言实点可数 |
| 记账① | — | `审查反馈.md` §二十三.七 回执 | **回执记号精度**：报告行「271 +」实为 **270+/1−**（271 是 `--stat` 总变更行数，写「+」暗示纯新增）。−1 行 = 第十二部分「（当前）」标记被上一轮复核方摘除（报告卫生动作，非修方改动——入库/工作树 md5 双证）。下轮回执宜用 numstat 口径（插入/删除分列；main/report-items 的「±」记号即正确用法） |
| 记账② | — | `审查报告.md:312/:553/:687/:846/:994/:1442/:1583`（落盘前） | **「（当前）」标记七处陈旧**：第六至十、十三、十四部分并挂（第十一、十二部分无标记——个别复核方摘过前任的、但从未全查），违反「恰一部分挂（当前）」不变量。我方落盘本部分时代为摘除七处，披露于此；建议把「摘前任标记」固定为每部分落盘动作之一。（我方标题清查两度依赖 tail 截断窗口，先后只见两处、三处，第三次全量枚举才现全部七处——教训入方法论 #2） |

## 四、要求下一轮反馈回答的问题

1. **提交时点**：两份审查文档再次压在工作树未提交（反馈 §二十三.七 21 行 + 报告《第十五部分》93 行）——沿既定口径随下轮交付一并入库即可；若会话节奏有变（如长期暂停），请给明确提交时点。
2. **Electron 实跑权限（继承，第九轮请求）**：DOM/tray 实跑权限是否对复核方解除，请用户裁决——已连续九轮以「待用户裁决」结转，用户一句话即可关闭这条悬案。

## 待办清单（第十六轮 · 交回修改）

> 第十五轮清单 3 条全部闭合，无整条结转。记账①由修方下轮回执兑现，记账②为我方已代办的披露项。以下为本轮新增。

```
[流程]
1. 提交：随下轮交付一并入库（反馈 §二十三.七 + 报告第十五部分）；回执改用
   numstat 口径（插入/删除分列，兑现记账①）。dist/ 不入库、零未跟踪码件
   （提交前 git status 复核照旧）。
2. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承第九轮）。
```

---

# 方法论补记（第十六轮）

1. **回归钉的验收动作不是「绿」，而是「验证能红」。** F1 这类正面格（修前修后均绿）做不了红绿对照，但它的全部价值在于「未来同类回归时变红」——这恰是可以直接验证的：沙箱内只突变被钉的那一行实现、跑同一套件、看杀伤模式。三个突变（findLast / 倒序 / 松弛）买到红绿对照给不了的两样证据：敏感性（钉真的会红）与特异性（恰杀一格、无误伤），M3 还反向验证了 E 组的守卫面。杀伤模式要先推演再实跑，实测与推演不符本身就是发现（本轮 E5 比推演多一格、方向正确、如实入账）。建议入册：与红绿对照并列的固定动作——前者证明「曾经红」，后者证明「还会红」，凡「钉转正」类交付两者取其一即可，正面格必走后者。
2. **文档自身的不变量也要进对账表，而对账清单必须一次全量枚举。** 「（当前）」标记是报告的轮次指针，其不变量「恰一部分挂（当前）」悄悄破了多轮（终查得七处并挂：第六至十、十三、十四部分；第十一、十二两处无标记，说明个别轮次摘过前任的、但从未全查）且无一方察觉——因为它不是任何一栏对账的断言项。更值得记下的是我方自己的失误：标题清查两度依赖 tail 截断窗口，先后只见到两处、三处，第三次全量枚举才现全部七处——审计动作若基于不完整清单，结论就系统性偏小，且「补查一次」不等于「查全」。哈希表、行数、标记语义这类跨文档不变量应与文件哈希同列固定对账项，清点必须一次全量；我方落盘本部分时已摘七处陈旧标记并披露，不变量本身入册，比事后修复便宜。

---

# 第十六部分 · 第十七轮复核（当前）

复核日期 2026-09-12 ｜ 对象：`审查反馈.md` §二十四（第十六轮处置，session-i）+ 提交 `431b796`（两文档入库态）+ 工作树未提交改动（恰反馈 §二十四.七 提交后补记，+17 行）
方式：提交 `431b796` 元数据 / numstat / 父提交三方对表 + 两文档入库版 blob md5 vs 回执声明双对表 + 工作树 delta 单 hunk 核验 + 18 项哈希对账（工作树口径）+ **18 项「blob vs 工作树」全量字节域差集**（本轮新设动作，N-31 来源）+ main.js 五提交 blob 回溯 + `npm test` 两轮实跑 + `node --check` ×3 + asar `extractFile` 独立复核 + dist / 进程 / TEMP / 用户数据取证 + 标记不变量全量枚举。本轮无码件/测试交付，无探针装置；除两轮 `npm test` 外全部动作为只读取证，零临时目录落盘。
**未执行 Electron 实机套件**（沿用用户既定限制——第九轮问题 4 前半，本报告为**连续第十一轮**请求）；DOM/tray 按「最近一次实跑」栏目照旧维护。

**基线（全部实测）**

- `审查报告.md` 开工基线 md5 `ae5a35797270c2189aafcc1a229ff8ed`、1800 行——与 §二十四 基线自述逐字一致；入库版（`431b796` 树内）同哈希，「报告全程未被修方触碰」双证成立。
- HEAD = `431b796`（父提交实测 `428118b`）；`git status` 恰 1 文件 = `审查反馈.md`（+17/−0，单 hunk 位于文末 = §二十四.七）——与「补记使反馈文档重新成为唯一未提交改动」的口径一致。
- 18 项哈希 vs §二十四.四：**18/18 逐项一致，恰 0 项「变」**——「零清单外落码、零 shipped-code 改动」在工作树哈希级成立（§二十四.四 ≡ §二十三.四，本轮零码件交付与提交面自洽）。
- `dist/LLM Usage Limit Board-0.1.0-Setup.exe` 实测 81,654,968 B / md5 `3eeb22eb78c5b9c4c4ebae3d1c2f4049` / mtime 08:10——未动；asar 内 `src/main.js` `extractFile` md5 `dd4c676a`（我方独立复核）——「不触发重打包」成立（字节域辨析见二，N-31）。
- electron.exe = **0**（前/后）；`providers.json` mtime `2026-09-11 20:27:49` 未动；`%TEMP%` llmb-* 开工恰 **8** 项，两轮套件后仍 **8**（零残留）。

## 结论速览

- 第十六轮清单 **2/2 闭合属实**：提交 `431b796` 元数据（subject 与 §二十四.三 冻结提案逐字一致 / dev / 2026-09-12 10:07:30 +0800 / 父 = `428118b`）、numstat（反馈 **104/0**、报告 **100/7**）与 §二十四.七 回执逐项相符；入库版反馈 blob md5 `e3c97521` ✓、报告 `ae5a3579` ✓ = 工作树。
- **记账①兑现属实**：回执改 numstat 分列口径，分列数字与 `git show --numstat` 原值逐字相符。**记账②确认属实**：报告标题级「（当前）」恰一处（《第十五部分》头），其余命中均为行文引用；反馈文档无轮次指针——「恰一部分挂（当前）」不变量在落盘前成立。
- `npm test` 两轮实跑 **203/203**（report-items 80 + limits 52 + ipc 62 + dist 9，exit 0 ×2），F1 块 `ok` 行实点取到；`node --check` ×3 通过。
- **新发现 N-31（P3），打破连续三轮零新发现**：`src/main.js` 工作树为 CRLF（md5 `dd4c676a` = 清单值）而 git blob 为 LF（`3336785f`）——18 项清单与树内字节自 `428118b` 起分属两个口径，第十五部分「asar = 工作区 = 入库——『不重打包』字节级成立」的三段等链在第三段（入库）不成立（git 语义等价成立、字节等价不成立）。无用户面影响（exe/asar = 已复核的工作树字节）；另记**记账③**（dom 三桩在 18 项清单外）。详见二、四。

## 〇、对表：第十六轮清单 2 条 vs 交付

| # | 条目 | 现状 | 本轮实测证据（非转抄自述） |
| --- | --- | --- | --- |
| 1（流程） | 提交两文档 + numstat 回执（记账①） | ✅ | `431b796` 元数据/numstat/父提交三方对表全符（104/0 + 100/7 实测）；入库版两文档 blob md5 与回执声明一致；工作树 delta 恰 §二十四.七（+17/−0）；dist/ 零条目、零未跟踪码件（`git status` 实测恰 1 文档） |
| 2（流程） | 实跑栏目照旧 + Electron 权限结转 | ✅ | 我方两轮 203/203 与 §二十四.一 逐项相同（F1 `ok` 实点）；零码件改动不触发重打包——exe/asar/三件套实测未动；权限结转至**第十一轮** |

## 一、提交 431b796 核验：回执三方对表 + 文档哈希 + 标记不变量

- **元数据**：subject 与冻结提案逐字一致；author `dev <dev@localhost>`；date `2026-09-12 10:07:30 +0800`；parent 实测 `428118b`（= §二十四 基线自述的开工 HEAD）。
- **numstat**：`104 0 审查反馈.md`、`100 7 审查报告.md`——与回执逐字相符（总量我方直接实测；§二十三.七 的 +21 已由上一轮复核，余量即 §二十四；报告「93 新增 + 7 处标记行内替换」的分解已由上一轮复核）。回执「提交前 git status 恰 2 文件」「提交后树上 npm test 203/203」为提交时点自述、不可独立复验，与工作树现存轨迹及我方两轮实跑相容，如实记账。
- **入库版哈希**：`git show 431b796:审查反馈.md | md5sum` = `e3c97521df4da74aba983d6ddeb7da59` ✓（回执声明值）；`git show 431b796:审查报告.md` = `ae5a35797270c2189aafcc1a229ff8ed` ✓ = 工作树 = 开工基线。两文档均纯 LF（0 个 CR 字节），blob 与工作树字节同一。
- **工作树 delta**：反馈 +17/−0、单 hunk、位置文末——恰为 §二十四.七 全文，无其他改动；报告 0 改动。
- **标记不变量**：报告全量枚举「（当前）」——标题级恰 1（:1711《第十五部分》头），其余 4 处命中均为记账①②/方法论行文引用；反馈 0 处。我方落盘本部分时已摘《第十五部分》标记并挂于本部分头（惯例第二轮执行）。

## 二、N-31：EOL 双口径——18 项清单与树内字节自 428118b 起分属两个宇宙

**发现动作**：本轮新设「blob vs 工作树」全量字节域差集（18 项逐一 `git show HEAD:<f> | md5sum` vs `md5sum <f>`）——**恰 1 项 DIFF：`src/main.js`（blob `3336785f` / 工作树 `dd4c676a`）**，其余 17 项与两份文档字节同一。

**归因回溯**（全部实测）：
- `git show 846c84a:src/main.js | md5sum` = `abb1de26` = §二十一/二十二 清单当时的值——第十二轮及以前，工作树与 blob **字节同一**（LF 域）。
- `428118b` 起 blob = `3336785f` ≠ 清单值 `dd4c676a`；`tr -d '\r' < 工作树 | md5sum` = `3336785f`——差异**纯 CRLF**。`git ls-files --eol`：`i/lf w/crlf`；`core.autocrlf=true` → `git add` 时 CRLF→LF 归一。N-30 修复（session-g）的工作树文件被以 CRLF 重写，**numstat（7+/2−）与 git status 双双不可见**——blob 层 diff 仍只含 N-30 实质改动，回执无任何异常可显示。
- exe 打包于 08:10（提交 09:13 之前）→ asar 内 main.js 为 CRLF 字节态（`dd4c676a`，我方 `extractFile` 独立复核）——**asar = 工作树：字节级成立；工作树 = 入库：仅 git 语义级成立**（`git hash-object src/main.js` = `47861976` = `git rev-parse HEAD:src/main.js`）。

**影响面**：(i) 现交付**无用户面影响**——用户安装的 exe/asar 即已复核的工作树字节；(ii) **活陷阱**：此后任何「`git show <rev>:main.js | md5sum` 对清单」的树内核验（第十三部分即用此法并宣称 18/18，报告:1481——当时为真，彼时工作树尚在 LF 域）将对 main.js **误报不一致**；(iii) autocrlf=false 环境的 fresh checkout 产生的 18 项工作树哈希将与清单对不上（main.js 为 LF 字节 → `3336785f`）；(iv) 两份审查文档恰为纯 LF，blob md5 与工作树 md5 恰好同值——属巧合而非机制，回执「入库版哈希」今后应显式声明口径。
**两文档此前从未提及 CRLF/autocrlf/EOL**（全文 grep 证实）——本轮首次入账。处置选项（A 规范化 + 重打包重钉 / B 口径入册 + 表述更正）见五及待办 1。

## 三、实测记录汇总

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Node 套件 | `npm test` 实跑 ×2 | **203/203**（80+52+62+9），exit 0 ×2；F1 块 `ok` 实点 |
| 语法 | `node --check` ×3 | 3/3（main / preload / limits——本轮零改动，例行） |
| 提交回执 | `git log/show/rev-parse` | 元数据逐字 ✓；numstat 104/0 + 100/7 ✓；父 = `428118b` ✓ |
| 文档对账 | `git show 431b796:两文档` md5 | 反馈 `e3c97521` ✓；报告 `ae5a3579` ✓ = 工作树 = 开工基线 |
| 工作树 delta | `git diff --numstat` | 反馈 +17/−0 单 hunk（§二十四.七）；报告 0 |
| 哈希对账（工作树口径） | `md5sum` ×18 | 18/18 与 §二十四.四 一致，0 变项 |
| **字节域差集（新）** | `git show HEAD:<f>` md5 ×18 | 恰 1 DIFF：main.js（blob `3336785f` / 工作树 `dd4c676a`）→ N-31 |
| main.js 归因 | 五提交 blob 回溯 + `tr -d '\r'` + `ls-files --eol` | `846c84a` blob = `abb1de26` = 当时清单值；`428118b` 起 `3336785f`；工作树去 CR 后 = `3336785f`——纯 EOL 差异 |
| asar / exe | `extractFile` + md5sum | asar main.js `dd4c676a` ✓；exe 81,654,968 B / `3eeb22eb` / mtime 08:10 未动 |
| 三件套 | tasklist / ls / stat | electron.exe 0（前/后）；llmb-* 8 → 8；providers.json mtime 未动 |
| 标记不变量 | grep 全量枚举 | 标题级恰 1（《第十五部分》头）；反馈 0 |

## 四、本轮新发现

| 编号 | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| N-31 | P3 | `src/main.js` 工作树 EOL / git blob | **EOL 双口径**：工作树 CRLF（`dd4c676a` = 清单值）vs blob LF（`3336785f`），自 `428118b` 起成立且全程未被察觉（autocrlf 归一吞掉了 numstat/status 全部可见信号）。等链「asar = 工作区 = 入库」的字节级断言第三段不成立；「树内 md5 对清单」核验方法自此对 main.js 失效（会误报）。无用户面影响；修法 A/B 见待办 1 |
| 记账③ | — | 18 项清单覆盖面 | **dom 装置面清单外盲区**：`test/dom/stub-preload-fail / -ipcfail / -savefail` 三枚桩自始不在 18 项清单（两文档全文从未提及），「三哈希未变佐证」只覆盖 dom 装置 5 文件中的 2 个——这三枚若被改动，现有零改动证据链不会报警。当前实测零漂移（工作树 = HEAD，历史上仅 `ff8d871`/`477310e` 两笔）。建议并入固定哈希栏目（21 项）或另立「dom 装置」子清单，与 N-31 处置合并执行 |

无其他新发现；除上述两项外，本轮全部断言与实测相符。

## 五、要求下一轮反馈回答的问题

1. **N-31 处置选择**：A（`.gitattributes` 规范化——源码类须钉 `eol=lf`，仅 `text=auto` 时 autocrlf=true 仍会在 checkout 写回 CRLF；`.ps1` 等按需留例外——+ `git add --renormalize` + 工作树刷新 + 重打包重验 + 18 项重钉：main.js 必变 `dd4c676a`→`3336785f`，exe/asar 全链换新，按 N-27 机制重走交付验证且安装实测不可省）还是 B（零仓库改动：清单口径显式声明为「本机 autocrlf=true 工作树 md5」、树内核验只用 git 语义等价、并更正第十五部分「字节级成立」的表述）？我方推荐 A——本系列核验强度全部压在「字节级」上，B 等于永久保留一个环境依赖的暗角；若取 A，请附重打包与重钉的全量计划。
2. **记账③**：dom 三桩并入固定哈希栏目的方式（扩为 21 项 / 另立子清单），与问题 1 合并执行时的重钉顺序。
3. **Electron 实跑权限（继承，连续第十一轮请求）**：DOM/tray 实跑权限是否对复核方解除，请用户裁决——一句话即可关闭这条已连续结转十轮的悬案。

## 待办清单（第十七轮 · 交回修改）

> 第十六轮清单 2 条全部闭合，无整条结转。N-31 为本轮新立，记账③为覆盖面补全；其余照旧。

```
[必须]
1. N-31 处置：在 A（.gitattributes（源码钉 eol=lf，ps1 留例外）+ renormalize + 工作树
   刷新 + 重打包重验 + 清单重钉）与 B（口径入册 + 第十五部分表述更正）之间明确选择
   并执行；两案均要求处置后「清单 / 工作树 / blob / asar」四口径重新对齐并给全量回执
   （若取 A，dist 产物按 N-27 机制重走交付验证，安装实测不可省）。
[流程]
2. 记账③：dom 三桩（stub-preload-fail / -ipcfail / -savefail）并入哈希对账面；
   与 1 合并执行时一并重钉。
3. 提交：两文档随下轮交付一并入库（反馈 §二十四.七 已在工作树 + 报告第十六部分）；
   numstat 回执照旧（记账①口径维持）；dist/ 不入库、零未跟踪码件（提交前 git status
   复核照旧）；回执中「入库版哈希」自本轮起显式声明字节口径（blob md5 / 工作树 md5）。
4. 「最近一次实跑」栏目照旧维护；Electron 实跑权限待用户裁决（继承第九轮，
   已连续十轮结转，本轮第十一轮请求）。
```

---

# 方法论补记（第十七轮）

1. **「字节级」断言必须逐段声明字节宇宙。** worktree md5、git blob md5、asar 内文件 md5 是三个不同的字节域；`core.autocrlf=true` 使「git status 清洁」≠「字节相同」——归一化把 numstat、status diff 等可见信号全部吞掉，只留下哈希这一个能暴露差异的量具；而「=」等链的三段若有两段同域、一段跨域，跨域那段就会以「=」的面目蒙混过关。本轮 N-31 的三段链「asar = 工作区 = 入库」中，前两段字节同域成立，第三段悄悄换成了 git 语义等价——量具跨域而不声明，等于把较弱断言伪装成较强断言。建议入册：凡「=」链，逐段标注量具（md5 / git 语义 / oid）；不同量具不得共用一个「字节级」限定词；树内核验与清单对账之前，先做一次「blob vs 工作树」差集（本轮成本：18 次 `git show` + 18 次 `md5sum`），差集非空时先解决口径再谈一致性。
2. **白名单不变量需要一次「全集差集」自查。** 18 项清单沿用八轮、双方逐轮 18/18 对表，却从未与「装置文件全集」做过一次差集核对——dom 三桩因此成为永久盲区（记账③），且恰好盲在「零改动证据链」这个最依赖清单闭合性的位置。这与上一轮方法论 #2（清点必须一次全量枚举）同根：**对账清单自身的闭合性，要靠与全集的一次显式差集来证明，而不能靠「清单存在」本身**。清单结构变动时（如本轮并入三桩）应顺带做一次全集复核。
