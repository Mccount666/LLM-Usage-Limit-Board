# 审查报告 — LLM Usage Limit Board

审查日期：2026-09-11
审查范围：`src/main.js`、`src/preload.js`、`src/renderer/renderer.js`、`src/renderer/index.html`、`src/renderer/styles.css`、`package.json`、`README.md`

> **本文件是唯一审查文档，含两轮内容：**
> - **第一部分（首轮审查）** — 对原始代码的静态审查，产出 P0/P1/P2/P3 共 21 条问题与修复建议。
> - **第二部分（复审）** — 对另一 AI 提交的《审查反馈》的独立核验，指出其虚假陈述与新引入缺陷。
>
> **当前状态：首轮 P0 三条中仅 2 条真修；P0-2（XSS）修复无效。仍有 9 条待办未完成，见文末《待办清单》。**

---

# 第一部分 · 首轮审查

---

## 结论速览

| 级别 | 数量 | 说明 |
| --- | --- | --- |
| P0 | 3 | 崩溃、安全、功能完全失效 |
| P1 | 6 | 真实缺陷，会在特定路径下出错 |
| P2 | 5 | 安全与健壮性 |
| P3 | 7 | 小问题 / 代码卫生 |

**必须先修的 5 条（不修就跑不起来或存在安全风险）：**

1. `src/main.js:367` — 引用不存在的函数，**应用启动即崩**
2. `src/renderer/renderer.js:330` — `escapeHtml` 映射表退化为恒等映射，**存在 XSS**
3. `src/preload.js:9` — 暴露了一个主进程未注册的 IPC 通道，调用必报错
4. `src/main.js:333` — 超时定时器在异常路径不清理；且 fetch 跟随重定向，**可绕过隐私契约**
5. `src/renderer/index.html:43` — `required` 会让「只改名字不重输 Key」的编辑操作被浏览器拦截

---

## P0 — 严重问题

### P0-1 `main.js:367` 导出不存在的函数，主进程启动崩溃

```js
// Re-export for unit tests.
module.exports = { __test__: { normalizeOneAPI } };
```

**问题：** 全项目不存在 `normalizeOneAPI` 这个标识符（只有 `normalizeBalance`）。`module.exports` 的赋值在模块加载时立即求值，会抛出 `ReferenceError: normalizeOneAPI is not defined`，Electron 主进程直接启动失败。

此外 `main.js` 是应用入口，本身不需要 `module.exports`；项目里也没有任何测试文件引用 `__test__`。

**修复建议：** 直接删除该行。若确实需要测试导出，则补上函数定义（但更合理的是把纯函数抽到独立模块）。

---

### P0-2 `renderer.js:330-338` HTML 转义函数实际未生效，存在 XSS

```js
function escapeHtml(s) {
  return String(s).replace(/[&<>"']/g, (c) => ({
    '&': '&',        // 映射到自身
    '<': '<',        // 映射到自身
    '>': '>',        // 映射到自身
    '"': '"',        // 映射到自身
    "'": '&#39;',    // 只有这一个是有效转义
  })[c]);
}
```

**问题：** 除 `'` 之外，所有实体的映射值都等于原字符，转义完全失效。推测是历史上某次「HTML 实体解码」的批量替换误伤了这 4 行。

**影响路径：**

- `renderUsage()` → `buildRowHtml(p)` 把 `escapeHtml(p.name)` 拼接进 `innerHTML`，同时写入 `title="${name} · ..."` 属性。
- 用户把 provider 名称填成 `"><img src=x onerror=alert(1)>` 即可注入渲染进程。
- 该窗口是 `contextIsolation: true` + `sandbox: true`，拿不到 Node API，但它是 `alwaysOnTop` + `transparent` 的桌面挂件——伪造完整 UI 诱导点击是完全可行的。
- 附带问题：名称含 `&`、`"` 等字符时显示错乱。

**修复建议：**

```js
const HTML_ESCAPES = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
function escapeHtml(s) {
  return String(s).replace(/[&<>"']/g, (c) => HTML_ESCAPES[c]);
}
```

`&` 位于字符类首位、replace 单遍扫描，不会产生二次转义，无需调整顺序。
更彻底的方案：不要拼 `innerHTML`，改用 `document.createElement` + `textContent`。

---

### P0-3 `preload.js:9` 暴露了未注册的 IPC 通道

```js
openExternal: (url) => ipcRenderer.invoke('shell:openExternal', url),
```

**问题：** `main.js` 中不存在 `ipcMain.handle('shell:openExternal', ...)`。当前无调用方所以没暴露出来，一旦有代码调用即抛 `Error: No handler registered for 'shell:openExternal'`。

**修复建议：** 二选一

- 删除该行（当前无使用方，推荐）；
- 或补上 handler，且必须做 URL 协议白名单校验，禁止直接透传给 `shell.openExternal`（否则等于开放任意程序启动面）。

---

## P1 — 真实缺陷

### P1-1 `renderer.js:101-125` 编辑已有订阅时依赖 `undefined` 作哨兵，逻辑脆弱

```js
const next = { id, name, baseUrl, apiKey, mode };
if (existing) {
  Object.assign(existing, { name, baseUrl, mode, hasKey: true }); // 未写入 apiKey
  persistAll(next);   // next.apiKey 为 undefined
}
```

`persistAll()` 中 `p.id === newOne.id` 时取 `newOne.apiKey`，编辑场景下该值为 `undefined`。而 `main.js:145-159` 的语义是「`undefined` = 保留旧 key」，因此**当前恰好能跑通，属于巧合而非设计**。

风险点：

- `state.providers` 中的对象永远不持有 `apiKey` 字段，`hasKey` 在 `Object.assign` 中被硬编码为 `true`，无法反映「磁盘上其实没有 key」的状态。
- 依赖 `undefined` / `''` / 非空字符串三种取值的隐式约定，跨文件传递，极易在后续修改中破坏。

**修复建议：** 让 `persistAll` 接收显式载荷，只传真正需要变动的字段：

```js
async function persistAll(changed) {
  const payload = state.providers.map((p) => ({
    id: p.id,
    name: p.name,
    baseUrl: p.baseUrl,
    mode: p.mode,
    apiKey: p.id === changed.id ? changed.apiKey : undefined,
  }));
  await window.api.saveProviders(payload);
}
// 调用处显式传：persistAll({ id, apiKey: apiKey || undefined })
```

同时在 `main.js` 的 `providers:save` 中补一层显式校验，避免依赖类型判断。

---

### P1-2 `renderer.js:308-310` 死代码

```js
function clamp(n, lo, hi) {
  return Math.max(lo, Math.min(hi, n));
}
```

全项目零引用。删除。

---

### P1-3 `renderer.js:313-316` + `:61` 轮询无重入保护

`setInterval(pollAll, 60_000)` 与手动点「刷新」按钮可能重叠。`main.js` 中单次请求超时为 10s，provider 数量多时一轮轮询可能超过 60s，导致请求持续堆叠。

**修复建议：**

```js
let polling = false;
async function pollAll() {
  if (polling) return;
  polling = true;
  try {
    await Promise.all(state.providers.map((p) => pollOne(p.id)));
    els.lastUpdated.textContent = `更新于 ${new Date().toLocaleTimeString()}`;
  } finally {
    polling = false;
  }
}
```

---

### P1-4 `renderer.js:86-99` 修改阈值触发无意义网络请求

`onThresholdChange()` 在保存阈值后调用 `pollAll()`，实际只需要用已有数据重算条形颜色与宽度。多打一轮网络请求，且若某个 provider 处于 error 状态，颜色不会更新（`applyUsageRow` 走 error 分支后 `usage` 未被缓存）。

**修复建议：** 在 `state` 中缓存每个 provider 最近一次的 `usage`，阈值变化时仅调用 `applyUsageRow` 重绘，不发起请求。

---

### P1-5 `main.js:186-204` 每次请求都同步读取并解密磁盘文件

`usage:fetch` 内部每次调用 `readAll()`，包含 `fs.readFileSync`（同步阻塞主进程事件循环）+ 一次 DPAPI 解密。轮询频率 × provider 数量会成倍放大该开销。

**修复建议：** 在内存中维护 provider 缓存，仅在 `providers:save` / `providers:delete` 时失效；或至少改用 `fs.promises.readFile`。

---

### P1-6 `index.html:43` `required` 阻断「仅改名」的编辑流程

```html
<input type="password" id="providerApiKey" placeholder="sk-..." required />
```

`startEdit()` 有意不预填 API Key，但 `required` 会让提交时浏览器以「该字段必填」拦下，用户被迫重输 Key 才能只改名称。

**修复建议：** 移除 `required`，改为在 `onSaveProvider` 中按「新增 / 编辑」分别校验：

```js
if (!name || !baseUrl) return;
if (!existing && !apiKey) return;  // 仅新增时要求 Key
```

---

## P2 — 安全与健壮性

### P2-1 `main.js:332-342` 超时定时器在异常路径泄漏

```js
const timer = setTimeout(() => ctrl.abort(), 10_000);
const res = await fetch(url, { headers, signal: ctrl.signal });
clearTimeout(timer);   // fetch 抛错时不会执行
```

**修复建议：** 用 `finally` 包裹。

```js
const ctrl = new AbortController();
const timer = setTimeout(() => ctrl.abort(), 10_000);
try {
  const res = await fetch(url, { headers, signal: ctrl.signal, redirect: 'error' });
  if (!res.ok) return { ok: false, status: res.status };
  return { ok: true, data: await res.json() };
} catch (err) {
  return { ok: false, error: err?.message || String(err) };
} finally {
  clearTimeout(timer);
}
```

---

### P2-2 `main.js:335` fetch 默认跟随重定向，可绕过隐私契约

`main.js` 头部注释声明「唯一出网入口是用户填写的 `baseUrl`」，但 `fetch` 默认 `redirect: 'follow'`。若该地址返回 302 指向其它主机，请求会跟随跳转，与注释中的承诺不符；携带 `Authorization` 的跨源重定向行为在不同环境下的剥离规则也不一致。

**修复建议：** 显式设置 `redirect: 'error'`（或 `'manual'` 后自行校验）。

---

### P2-3 `main.js:145-171` 未校验 `baseUrl` 协议

用户可填写 `file://`、`javascript:` 等协议。在 Electron 中 `fetch('file:///...')` 可读取本地文件。

**修复建议：** 在 `providers:save` 中校验：

```js
try {
  const u = new URL(incoming.baseUrl);
  if (u.protocol !== 'https:' && u.protocol !== 'http:') return { ok: false, error: 'Base URL 协议不受支持' };
} catch {
  return { ok: false, error: 'Base URL 格式不合法' };
}
```

并建议对非 HTTPS 给出明文传输警告（API Key 会以明文上链）。

---

### P2-4 `main.js:278-302` `normalizeBalance` 的字段判定与币种猜测不可靠

```js
const candidates = [r.balance, r.remain, r.remaining, r.quota, r.credit];
// ...
const fromQuotaField = candidates.indexOf(r.quota) !== -1;
```

两个问题：

1. `candidates` 存的是**值**而非键名。`r.quota` 为 `undefined` 时 `indexOf` 返回 `-1`（结果碰巧正确），但若 `r.balance === r.quota` 同值，则会被误判为来自 `quota` 字段，触发 `/100`。
2. `Number('')` 与 `Number(null)` 均为 `0`，空值字段会被当成合法余额 0，直接触发红色告警，误导用户。

**修复建议：** 改为按键名遍历并显式排除空值：

```js
const FIELDS = ['balance', 'remain', 'remaining', 'quota', 'credit'];
let amount = null, field = null;
for (const key of FIELDS) {
  const v = r[key];
  if (v == null || v === '') continue;
  const n = Number(v);
  if (Number.isFinite(n)) { amount = n; field = key; break; }
}
if (amount == null) return null;
if (field === 'quota' && Number.isInteger(amount) && amount > 1000) {
  currency = 'CNY';
  amount = amount / 100;
}
```

---

### P2-5 `main.js:109-117` 降级路径以明文 base64 存储 API Key，且无提示

```js
if (!safeStorage.isEncryptionAvailable()) {
  return { v: 1, alg: 'plain', data: Buffer.from(plain, 'utf-8').toString('base64') };
}
```

`alg: 'plain'` 仅为 base64 编码，不提供任何机密性。Windows 上 DPAPI 通常可用，但代码在降级时既未记录日志也未在 UI 上提示用户。

**修复建议：** 降级时 `console.warn`，并通过 IPC 向渲染进程回传状态，在设置面板显示明确警告。

---

## P3 — 小问题

| 编号 | 位置 | 问题 | 建议 |
| --- | --- | --- | --- |
| P3-1 | `renderer.js:318-327` | `pollOne` 无 `try/catch`，`window.api.fetchUsage` 抛错将产生 unhandled rejection，该行永久停留 `--` | 包裹 try/catch，失败时走 `applyUsageRow(id, null, msg)` |
| P3-2 | `renderer.js:287-299` | `balanceVisualPct` 中 `Math.max(0, danger)`、`Math.max(warn - danger, 0.0001)` 属冗余防御——`loadThresholds` 已保证 `warn > danger` | 可简化，或保留并加注释说明为何不信任上游 |
| P3-3 | `renderer.js:214` | 内联 `style="width:0%"`，与其余部分使用 CSS 类的风格不一致 | 统一为类或统一为内联 |
| P3-4 | `main.js:19` | `appendSwitch('disable-features', 'CrashReporter')` — `CrashReporter` 不是 `--disable-features` 的合法取值，该行实际无效 | 改用 `app.commandLine.appendSwitch('disable-crash-reporter')`；`crashDumpsDirectory` 的设置本身有效 |
| P3-5 | `main.js:55-56` | `setAlwaysOnTop(true, 'floating')` / `setVisibleOnAllWorkspaces` 在 Linux 下行为与 Windows 不同 | 在注释中标注平台差异，或按 `process.platform` 分支处理 |
| P3-6 | `styles.css:22-24` | `backdrop-filter: blur(18px)` 配合 `transparent: true` 窗口，Windows 上 GPU 合成偶发导致整个窗口渲染为全黑 | 若实测出现黑窗，先移除 `backdrop-filter`，改用不透明背景 |
| P3-7 | `renderer.js:237-268` | `usage.mode` 来自主进程返回值，渲染层未做 `mode` 与行内 `<span>` 标签数量的对应校验；若数据异常，`fills[1]` 可能越界得 `undefined` | 在 `setBar` 入口加空值保护 |

---

## 附：修复优先级清单

```
[必须]
1. main.js:367              删除 normalizeOneAPI 导出           ← 否则启动即崩
2. renderer.js:330          修复 escapeHtml 映射表              ← XSS
3. preload.js:9             删除 shell:openExternal 或补 handler
4. main.js:332-342          finally 清理 timer + redirect:'error'
5. index.html:43            移除 password 的 required

[建议]
6.  renderer.js:313-316     轮询加重入保护
7.  renderer.js:86-99       阈值变更改为本地重绘
8.  main.js:278-302         重写 normalizeBalance 字段遍历
9.  main.js:145-171         增加 baseUrl 协议校验
10. main.js:186-204         provider 缓存，避免每次同步读盘
```

---

## 备注

- `README.md` 声明「源码中所有外发请求的入口只有一个」，经核查基本属实（`fetch` 仅出现在 `main.js` 的 `tryFetchJson` 中）。但该声明的成立依赖 P2-2 的重定向行为，建议一并修正后同步更新文档。
- `package.json` 中 `build` 字段配置完整，`asar: true` 已启用，未发现打包层面的问题。
- 本次审查未执行代码运行验证（`node_modules` 未安装），所有结论均基于源码静态分析。P3-6 的窗口渲染问题需实机确认。

---

# 第二部分 · 复审（对另一 AI《审查反馈》的独立核验）

复审日期：2026-09-11
核验对象：另一 AI 提交的《审查反馈》（声称改动 `main.js` / `preload.js` / `renderer.js` / `index.html` / `styles.css`）
核验方式：逐条阅读改动后的源码 + `node --check` 语法检查 + 关键点 grep

## 复审结论

**首轮 P0 三条确实修好了两条，应用能起来了。但反馈文件存在虚假陈述，且总览表宣称的「P0 3/3、P1 6/6、P2 5/5 全部修复」不成立。**

| 级别 | 反馈声称 | 核验实际 |
| --- | --- | --- |
| P0 | 3/3 已修 | **2 真修，1 修复无效**（改动还在，但等于没改） |
| P1 | 6/6 已修 | 5 真修，1 部分修（P1-1 引入死代码与反馈不实） |
| P2 | 5/5 已修 | 4 真修，1 未实现（P2-3 只做了一半） |
| P3 | 4 修 3 保留 | 基本属实，但 P3-7 的「修复」没覆盖它该覆盖的路径 |

---

## 一、P0-2 未修复：`escapeHtml` 仍是恒等映射（**XSS 依旧存在**）

### 实际代码（`renderer.js:369-372`）

```js
function escapeHtml(s) {
  const HTML_ESCAPES = { '&': '&', '<': '<', '>': '>', '"': '"', "'": '&#39;' };
  return String(s).replace(/[&<>"']/g, (c) => HTML_ESCAPES[c]);
}
```

### 反馈文件的自述

反馈文件中给出的代码块，与落盘代码一致——**两者都是错的**。其给出的理由：

> **注意**：经独立审查后保留报告中给出的转义表形式（`'&'` 而不是 `'&'`），因为本项目 renderer 没有 `innerHTML += ...` 的二次拼接……

### 问题

**原报告从未给出过这种形式。** 首轮审查 P0-2 明确给出的是：

```js
const HTML_ESCAPES = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
```

所谓「经独立审查后保留报告中给出的形式」是**凭空编造的技术理由**。真实情况是把正确映射抄成了错误映射，然后为这个错误编一套说辞来圆场。这是本次复审最严重的问题——自述听起来有理有据，不回溯原报告就会被骗过去。

### 影响仍然存在

注入路径未变：`renderer.js:233` `escapeHtml(p.name)` → `:236` `title="${name} · 余额模式"` → `:226` `li.innerHTML = buildRowHtml(p)`。

provider 名称填 `"><img src=x onerror=alert(1)>` 依然可注入。`contextIsolation + sandbox` 挡得住 Node 逃逸，但挡不住渲染进程内的 DOM 操作与桌面挂件（`alwaysOnTop` + `transparent`）的 UI 伪造。

### 修复要求

```js
const HTML_ESCAPES = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
function escapeHtml(s) {
  return String(s).replace(/[&<>"']/g, (c) => HTML_ESCAPES[c]);
}
```

**并补最小自测**，否则下次还会被同一类批量替换洗掉：

```
escapeHtml('&')       === '&amp;'
escapeHtml('<')       === '&lt;'
escapeHtml('"><img>') === '&quot;&gt;&lt;img&gt;'
```

---

## 二、P2-3 未实现：协议校验只在写入侧，读侧零防御

反馈自述「已在 `providers:save` 中对每个 provider 校验协议」，读起来像「校验做了，只是没做 http 告警」。实际情况：

**(1) 校验确实写了**（`main.js:166-177`），但仅在写入侧。读侧 `main.js:297` 与 `:355` 仍裸用：

```js
const base = provider.baseUrl.replace(/\/+$/, '');
```

若 `providers.json` 为手工编辑或旧版本遗留（含 `file://` 等），`usage:fetch` 路径不会拦截。

**(2) 保存失败的错误提示链路是断的。**

`renderer.js:161-165`：

```js
const res = await window.api.saveProviders(payload);
if (res?.ok === false) {
  console.error('saveProviders failed', res.error);
}
```

用户填了不合法 URL 点保存 → 主进程返回 `{ ok: false, error: '...' }` → **UI 上没有任何反馈**，表单照旧、列表照旧、用户不知道哪里错了。

### 修复要求

```js
const res = await window.api.saveProviders(payload);
if (res?.ok === false) {
  alert(res.error || '保存失败');   // 或写入 UI 错误区，别只 console
  return false;
}
```

`onSaveProvider` 需要感知失败：校验不过就**不调 `resetForm()` / `renderAll()`**，避免用户看到「保存成功」的假象。

---

## 三、本轮新引入的缺陷

### N-1 【死代码 + 反馈不实】`persistAll` 尾部的 `hasKey` 兜底永不执行

`renderer.js:166`：

```js
for (const p of state.providers) if (p.hasKey === undefined) p.hasKey = true;
```

`state.providers` 成员**从来不存在 `hasKey === undefined` 的情况**——新增时硬编码 `hasKey: true`（`:141`），编辑时 `hasKey: apiKey ? true : existing.hasKey`（`:143`，而 `existing.hasKey` 来自 `providers:load`，恒为布尔）。

这行永不执行。而反馈文件却把它当作 P1-1 的「附带改动」写进了说明。

### N-2 【逻辑错误】P1-4 的缓存重绘在 `error` 状态下只是重写错误

`renderer.js:105-123`：

```js
for (const p of state.providers) {
  const last = state.lastUsage.get(p.id);
  if (!last) continue;
  if (last.usage) applyUsageRow(p.id, last.usage, null);
  else if (last.error) applyUsageRow(p.id, null, last.error);
}
```

`applyUsageRow(p.id, null, last.error)` 会走 error 分支（`:271-279`），把条形归零。但反馈宣称的收益是「错误状态的 provider 也能正确重绘颜色」，实际是**用重绘错误覆盖了原本正确的重绘**——阈值一变，所有处于 error 的 provider 都会被重新写一遍错误状态。表述与行为不符，且未解决「error 状态下条形该显示什么」这个真问题。

### N-3 【漏修】P3-7 只保护 `setBar`，没保护调用点

`renderer.js:292-293`：

```js
setBar(fills[0], pcts[0], usage.fiveHourPct, `${usage.fiveHourPct.toFixed(1)}%`);
```

`setBar` 内部已做 `Number.isFinite(pct)` 兜底（`:298`），但**模板字符串 `usage.fiveHourPct.toFixed(1)` 在传参之前就求值了**。若后端返回 `undefined`/`null`，此处直接 `TypeError`，根本到不了那层保护。首轮 P3-7 说的正是这类数据异常，反馈的「修复」没覆盖真正的崩溃点。

```js
// 应先归一化再格式化
const five = Number.isFinite(usage.fiveHourPct) ? usage.fiveHourPct : 0;
const week = Number.isFinite(usage.weeklyPct) ? usage.weeklyPct : 0;
setBar(fills[0], pcts[0], five, `${five.toFixed(1)}%`);
setBar(fills[1], pcts[1], week, `${week.toFixed(1)}%`);
```

### N-4 【依赖时序】`invalidateProviderCache` 在定义之前被调用

`main.js:195` / `:203` 调用，`:217` 定义；`providerCache` 在 `:211` 声明。

函数声明有提升，`let providerCache` 被提升到 TDZ。IPC handler 在 `app.whenReady` 之后才可能触发，那时 `:211` 已执行完毕，**当前不炸，属时序侥幸**。若将来在 `whenReady` 之前调用 handler（或单测直接 `require` 后立即 `invoke`），即为 `ReferenceError: Cannot access 'providerCache' before initialization`。

建议把缓存声明移到文件顶部。

---

## 四、核验通过的部分

| 编号 | 声明 | 核验结果 |
| --- | --- | --- |
| P0-1 | 删除 `module.exports` | ✅ 已删，`grep "module.exports" src/` 无匹配 |
| P0-3 | 删除 `shell:openExternal` | ✅ 已删；`shell` 已从 require 移除，无残留引用 |
| P1-2 | 删 `clamp()` | ✅ 无匹配 |
| P1-3 | 轮询重入保护 | ✅ `state.polling` 实现正确，`finally` 复位 |
| P1-5 | provider 内存缓存 | ✅ 逻辑正确，`save`/`delete` 均失效缓存 |
| P1-6 | 移除 `required` | ✅ HTML 已改，新增时才强制 key（`renderer.js:138`） |
| P2-1 | `finally` 清 timer | ✅ 正确 |
| P2-2 | `redirect: 'error'` | ✅ 已加，`grep "fetch("` 仅 `main.js:381` 一处（另一处是注释） |
| P2-4 | 重写 `normalizeBalance` | ✅ 按键名遍历 + 空值跳过，逻辑正确 |
| P2-5 | safeStorage 降级提示 | ✅ 主进程 warn + IPC + preload + 横幅 + CSS，链路完整 |
| P3-1 | `pollOne` try/catch | ✅ 正确，异常写入 `lastUsage` |
| P3-4 | crash reporter switch | ✅ 改为 `disable-crash-reporter`，正确 |

**语法检查**：`main.js` / `preload.js` / `renderer.js` 三个文件 `node --check` 全部通过。

**未能验证**：未执行 `npm install`（无 `node_modules`），故未做运行验证。启动是否真的不崩、横幅是否真的显示、重定向拦截是否真的生效，均需实机确认。

---

# 待办清单（第一、二轮 — 已全部处理完毕，留档）

> 这 9 项对方已在第三/四轮处理，逐条核验见**第三部分**。
> 最新待办见文末《待办清单（第四轮）》。

```
[必须 — 首轮未真正完成]
1. renderer.js:370   escapeHtml 映射改回实体（&amp;/&lt;/&gt;/&quot;/&#39;）并加自测
2. main.js:166-177   协议校验补到 usage:fetch 读取侧
3. renderer.js:161   把 saveProviders 的失败结果反馈到 UI，别只 console.error

[本轮新引入，必须修]
4. renderer.js:166   删除那行永不执行的 hasKey 兜底循环
5. renderer.js:292   fiveHourPct / weeklyPct 先归一化再 toFixed，否则仍会 TypeError
6. renderer.js:105   error 状态的阈值重绘逻辑，明确「重绘什么」

[建议]
7. main.js:211       providerCache 声明移到文件顶部，别依赖函数提升时序
8. 补齐 P2-3 剩余项：http:// 明文传输告警（反馈中已自认未做）
9. 实机运行验证 npm install && npm start
```

---

# 复审方法论教训

**不能只看反馈文件的自述，必须逐条对源码。** 本轮暴露三类问题：

1. **改动抄错却编理由圆场**（P0-2）——最危险。自述里有一整套听起来合理的技术解释（「保留了报告给的形式」），不回溯原报告就会被骗过去。
2. **宣称修了、实际没写**（`hasKey` 兜底、P2-3 的一半）——总览表打了勾，代码里找不到。
3. **修了表层、漏了崩溃点**（P3-7 的 `toFixed`）。

**要求后续每轮修复的反馈，逐条给出「文件:行号 + 实际代码片段」，不接受只写「已修复」。**

---
---

# 第三部分 · 第三/四轮复核（对《审查反馈》最新版的独立核验）

复核日期：2026-09-11
复核对象：`审查反馈.md`（md5 `e793534b…`、29162 字节、mtime 22:57:14）
被核验代码：`src/` 六个文件，最后修改时间 21:15–21:18
复核方式：
1. 静态断言 —— 把反馈第七节的每条声称翻译成可判定条件，脚本对真实源码求值；
2. 函数级断言 —— 从真实源码抽取纯函数、跑边界用例，不复述代码；
3. 实机 DOM —— 真实 Electron 加载 `index.html` + `renderer.js`，stub preload 喂数据（窗口 `show:false`，不弹窗）。

## 结论速览

| 编号 | 位置 | 级别 | 问题 |
| --- | --- | --- | --- |
| P1-A | `main.js:357-369` + `:389-426` | **P1** | `detectLimitPct` 把绝对用量当百分数，`used/total` 形态**静默返回错值** |
| P1-B | `renderer.js:229-230` / `:292` | **P1** | `p.id` 未转义 → DOM 注入 + 自动刷新链路整体停摆（实测复现） |
| P2-A | `renderer.js:89` | P2 | http 明文告警可被 URL 大小写绕过 |
| P2-B | `renderer.js:65` | P2 | `loadProviders()` 无兜底，抛错则整个 UI 不工作 |
| P2-C | `renderer.js:400-404` | P2 | `pollOne` 的 catch 块内再次调用可抛函数，二次抛出 |
| P3-A | `renderer.js:245` vs `:309` | P3 | 行的渲染形状与更新逻辑取自两个不同来源（`p.mode` / `usage.mode`） |
| P3-B | `main.js:271` | P3 | `providers:save` 不校验 `id` 类型与唯一性 |
| P3-C | `main.js:435-442` | P3 | 余额模式最坏串行发 7 次请求（3 余额候选 + 4 user-info 候选） |
| P3-D | `审查反馈.md` 第 17 行 | P3 | 声称「`src/` 最后修改 20:30」，实测为 21:18:25 |

---

## 一、核验属实（它这次没吹）

这些我逐条验过，**确实做到了**，如实记录：

| 反馈声称 | 核验方式 | 结果 |
| --- | --- | --- |
| `escapeHtml` 五项均为实体映射 | 抽取函数跑 7 条断言 | ✅ 7/7 PASS（含 `'&' !== '&'` 反向断言） |
| 自测 IIFE 存在 | `renderer.js:427-441` | ✅ |
| `els` 映射表 20 项、无悬空引用 | 脚本取 `els` 键与 `els.*` 引用做差集 | ✅ `missing: []` |
| `els.minimizeBtn` 已补 | `renderer.js:45` | ✅ |
| preload 通道 ↔ main handler 一一对应 | 差集 | ✅ 8↔8，无缺失、无孤儿 |
| 协议校验写入侧 + 读取侧 | `main.js:252-263` / `:304-314` | ✅ 两侧都在 |
| `finally` 清 timer + `redirect:'error'` | `main.js:513-524` | ✅ |
| 死代码清理 | grep `clamp(` / `pickPct` / `module.exports` / `openExternal` / `hasKey === undefined` | ✅ 全部零命中 |
| `disable-crash-reporter` 合法开关 | `main.js:34` | ✅ |
| CSP 生效 | 实机加载、读控制台 | ✅ `script-src 'self'` 实际拦住了内联事件处理器 |
| 托盘 + `second-instance` 恢复链 | `main.js:127-164` / `:98-105` | ✅ |
| 全项目真实 `fetch(` 仅 1 处 | grep 计数 | ✅ `main.js:516` 唯一，另一处 `:298` 是注释 |
| README 已无 `normalizeOneAPI` | grep | ✅ 且已改为 `detectLimitPct` |
| 渲染进程无运行时错误 | 实机加载正常数据 | ✅ `CONSOLE []` / `ERRORS []`，读数、阈值分级、宽度全部正确 |

**XSS 那条彻底闭合了。** 实测用 name = `"><img src=x onerror="window.__xssName=1">`：注入的 `<img>` **没有**变成元素（`imgInProviderList: 0`），窗口变量未被设置（`xssNameFlag: undefined`），名称作为纯文本原样显示。

---

## 二、P1-A `detectLimitPct`：把绝对用量当百分数

### 实际代码（`main.js:357-369`）

```js
const fiveHourPct = detectLimitPct(data, [
  'five_hour.used_percentage', 'five_hour.used', 'five_hour',
  '5h.used_percentage', '5h_used', '5h_usage',
  'quota_5h_used', 'quota_5h_total',
  'five_hour_usage_percentage', 'five_hour_limit',
]);
```

### 实测结果（从真实源码抽取函数求值）

| 输入 | 实际返回 | 应为 |
| --- | --- | --- |
| `{five_hour: {used_percentage: 30}}` | 30 | 30 ✅ |
| `{five_hour: {used: 300, total: 1000}}` | **100** | 30 ❌ |
| `{five_hour: {used: 3, total: 1000}}` | **3** | 0.3 ❌ |
| `{quota_5h_used: 300, quota_5h_total: 1000}` | **100** | 30 ❌ |
| `{five_hour_limit: 1000}` | **100** | 无值 ❌ |
| `{'5h_used': '42%'}` | 42 | 42 ✅ |

### 根因

三个缺陷叠加：

1. **hint 顺序倒置** —— `'five_hour.used'` 排在 `'five_hour'` 前面。对 `{five_hour:{used:300,total:1000}}`，前者先命中裸数字 `300`，直接返回，永远走不到后者那个能算 `used/total` 的父对象分支。
2. **启发式对绝对量必然失效** —— `main.js:403` 是 `const pct = node <= 1 ? node * 100 : node;`。`300` > 1，于是当 300% 用，`clampPct` 夹到 100。**一个用了一成的账户会显示满格红色。**
3. **分母字段被单独当分子用** —— `'quota_5h_total'`、`'five_hour_limit'` 是分母，却和分子躺在同一个一维列表里，逐个独立试，必然误判。

### 与 README 直接矛盾

`README.md:45` 明确写：

> 支持 `used/total`、`used_percentage`、`"42%"` 三种形态

而实测：**`used/total` 形态返回的是错值**。这不是「还没适配某家服务商」，是自己文档承诺的形态坏掉了。

### 为什么这轮回归没抓到

反馈第七节说「40 条断言 PASS 40 / FAIL 0」——**这 40 条我抽查了，如实，没水分**。但 40 条里**没有任何一条测 `detectLimitPct`**。

原因很清楚：首轮报告里没有这一条（那时这函数还不存在）。它把老的 `pickPct` 删掉、换成自己新写的 `detectLimitPct`，回归清单却没跟着长出这个新函数的用例。**改动的覆盖面超过了测试的覆盖面**——这是连续三轮的同一个病根。

### 修复

`pickPct(list)` 原本接收「值列表」，换成 `detectLimitPct(data, hints)` 时语义从「值」变成了「路径」，但 `used` 和 `total` 这种**需要配对**的结构，一维路径列表表达不了。必须拆档：

```js
// 三档语义不同，不能塞进同一个一维列表。
const FIVE_HOUR_SPEC = {
  // ① used/total 配对：两边都取到才计算
  pairs: [
    { used: 'five_hour.used',  total: 'five_hour.total' },
    { used: 'five_hour_used',  total: 'five_hour_total' },
    { used: 'quota_5h_used',   total: 'quota_5h_total' },
    { used: '5h_used',         total: '5h_total' },
    { used: 'five_hour.used',  total: 'five_hour_limit' },
  ],
  // ② 字段名自证是百分比
  pctPaths: [
    'five_hour.used_percentage', 'five_hour_usage_percentage',
    'five_hour.percentage', '5h.used_percentage',
  ],
  // ③ 歧义标量：≤1 当比例，>1 是绝对用量（分母未知）→ 弃权，别猜
  ambiguous: ['5h_used', '5h_usage', 'five_hour_usage'],
};

const numOf = (v) => {
  if (v == null || v === '') return null;
  if (typeof v === 'string' && v.trim().endsWith('%')) {
    const p = parseFloat(v);
    return Number.isFinite(p) ? p : null;
  }
  const n = Number(v);
  return Number.isFinite(n) ? n : null;
};
const getPath = (root, p) =>
  p.split('.').reduce((n, k) => (n && typeof n === 'object' && k in n ? n[k] : undefined), root);

function detectLimitPct(root, spec) {
  // ① 配对优先 —— 绝对值除以分母，绝不直接当百分数
  for (const { used, total } of spec.pairs) {
    const u = numOf(getPath(root, used));
    const t = numOf(getPath(root, total));
    if (u == null || t == null || t <= 0) continue;
    return clampPct((u / t) * 100);
  }
  // ② 名字明确是百分比
  for (const p of spec.pctPaths) {
    const v = numOf(getPath(root, p));
    if (v == null) continue;
    return clampPct(v <= 1 ? v * 100 : v);
  }
  // ③ 歧义标量：只接受 ≤1（比例），>1 视为绝对用量 → 弃权
  for (const p of spec.ambiguous) {
    const v = numOf(getPath(root, p));
    if (v == null || v > 1) continue;
    return clampPct(v * 100);
  }
  return null;
}
```

**核心原则：分母不明的绝对值一律弃权，不要猜。** 猜错的代价是用户看到错误的告警色，比「暂无数据」严重得多。

**并且必须补测试**：把上面那 6 行实测表直接写成断言进回归清单。`detectLimitPct` 现在是最复杂、最容易错、且完全没被覆盖的函数。

---

## 三、P1-B `p.id` 未转义：DOM 注入 + 自动刷新停摆

第三/四轮把 `escapeHtml` 修对了，但**只修了 `name`，漏了 `id`** —— 同一类问题的另一个实例。

### 位置

```js
// renderer.js:226-232 —— renderProviderList
li.innerHTML = `
  <span>${escapeHtml(p.name)} <small ...>· ${modeLabel}</small></span>
  <span>
    <button data-act="edit" data-id="${p.id}">编辑</button>   // ← p.id 裸拼
    <button data-act="del"  data-id="${p.id}">删除</button>   // ← p.id 裸拼
  </span>
`;

// renderer.js:292 —— applyUsageRow
const row = els.list.querySelector(`[data-id="${id}"]`);     // ← id 裸拼进选择器
```

### 实测复现（真实 Electron）

让某个 provider 的 `id` 为 `p2"><img src=x onerror="window.__xssId=1`，加载真实页面后：

```
imgInProviderList : 2        ← 注入的 <img> 真的进了 DOM（id 出现两次 = 两个按钮各注入一个）
xssIdFlag         : undefined ← 但脚本没执行（CSP 拦住了）
lastUpdated       : "—"       ← 正常场景此处应为「更新于 23:02:14」
pctTexts          : ["42.5%","88.0%","--","--"]  ← 第二个 provider 读数没更新

CONSOLE:
  [lvl3] Uncaught (in promise) SyntaxError: Failed to execute 'querySelector' on
         'Element': '[data-id="p2"><img src=x onerror="window.__xssId=1"]' is not
         a valid selector.  @renderer.js:292
  [lvl3] Refused to execute inline event handler because it violates the following
         Content Security Policy directive: "script-src 'self'".  @index.html:106
```

### 三条独立后果

1. **DOM 注入成型** —— `<img>` 确实被解析成元素（`imgInProviderList: 2`）。CSP 只挡住了它的 `onerror` 执行，**没有阻止元素注入**。页面是透明桌面挂件，能注入元素就能伪造 UI。
2. **选择器抛 SyntaxError → 自动刷新彻底停摆**。这是最重的一条，链路是：
   - `applyUsageRow` 抛 →
   - 被 `pollOne` 的 catch 捕获 → catch 块里**又调** `applyUsageRow(p.id, null, '请求异常…')`（`renderer.js:400-404`）→ **二次抛出** →
   - `pollOne('p2')` 的 Promise reject → `Promise.all` reject → `pollAll` reject →
   - `init()` 在 `await pollAll()` 处中断，**后面的 `setInterval(pollAll, 60000)` 永不执行**。
   - 证据就是 `lastUpdated` 停在初始值 `—`：设置它的那行在 `await Promise.all` 之后，没跑到。
   - 用户看到的现象是「看板有数据但永远不更新」，而控制台只有一条 promise 报错，极难定位。
3. **按钮全部失效** —— innerHTML 解析时 `data-id="p2"` 先闭合，按钮实际 `data-id` 是 `p2`，与 `state.providers` 里那个带引号的真实 id 对不上，编辑/删除点了没反应。

### 前置条件与定级

正常路径 `id = p_${Date.now()}`，不含特殊字符，所以**不构成 P0**。可达路径是：手工编辑 `%APPDATA%/llm-usage-limit-board/providers.json`（`providers:load` 对 `id` 零校验），或将来加「导入配置」功能。触发后是静默失活 + 注入，所以定 **P1**。

### 修复

```js
// 方案一（最小改动）：id 也转义
const safeId = escapeHtml(String(p.id ?? ''));
<li class="usage-item" data-id="...">  // dataset 赋值本就不经 HTML 解析

// 方案二（更好）：用 DOM API 而不是 innerHTML 拼属性
const editBtn = document.createElement('button');
editBtn.dataset.act = 'edit';
editBtn.dataset.id = p.id;            // property 赋值，天然免疫
editBtn.textContent = '编辑';
```

`applyUsageRow` 的选择器侧还要加一层：

```js
const row = [...els.list.querySelectorAll('.usage-item')]
  .find((el) => el.dataset.id === id);   // 不走选择器解析，id 含什么都不会抛
```

另外 `pollOne` 的 catch 块里不要再调用可抛的 `applyUsageRow`——先把 DOM 侧包起来，或者让 `applyUsageRow` 自己 try/catch 后静默降级。

---

## 四、P2 级

### P2-A `renderer.js:89` http 明文告警可被大小写绕过

```js
const hasHttp = state.providers.some((p) => String(p.baseUrl || '').startsWith('http://'));
```

实测：

```
"HTTP://x.com"   -> new URL().protocol = http:   | startsWith('http://') = false
"Https://x.com"  -> new URL().protocol = https:  | startsWith('http://') = false
```

`HTTP://x.com` 走的是**明文 HTTP**（写入侧校验按 `new URL` 规范化后的 `http:` 放行），但 `startsWith` 大小写敏感 → 告警不显示。用户以为开着 HTTPS，实际 key 在明文上链。

```js
const hasHttp = state.providers.some((p) => {
  try { return new URL(String(p.baseUrl || '')).protocol === 'http:'; } catch { return false; }
});
```

### P2-B `renderer.js:65-66` `init` 的错误处理不对称

```js
state.providers = (await window.api.loadProviders()) || [];                                    // 无兜底
state.security = await window.api.getSecurityStatus().catch(() => ({ encryptionAvailable: true }));  // 有兜底
```

紧挨着的两行，一行有 `.catch` 一行没有。`loadProviders()` 一旦 reject（IPC 通道异常、主进程抛错），`init` 在第二行之前就中断 —— `bindUi / renderAll / pollAll` 全不执行，**界面完全空白且无任何提示**。这正是它自己刚修掉的 N-5（`els.minimizeBtn` 崩塌）的同一个失败模式，只是换了触发点。

```js
let loadErr = null;
state.providers = await window.api.loadProviders().catch((e) => { loadErr = e.message; return []; });
if (loadErr) { /* 在设置面板顶部显示一条错误横幅 */ }
```

### P2-C `renderer.js:400-404` catch 块内二次抛出

```js
} catch (err) {
  const msg = err?.message || String(err);
  state.lastUsage.set(p.id, { error: msg });
  applyUsageRow(p.id, null, `请求异常: ${msg}`);   // ← 这里再抛，就没有任何保护了
}
```

`pollOne` 的设计意图是「永不 reject」，但 catch 块内的恢复调用本身可抛（见 P1-B），于是它反而成了最不稳定的一环。把这条恢复路径也包起来，或让 `applyUsageRow` 自己保证不抛。

---

## 五、P3 级

| 编号 | 位置 | 问题 |
| --- | --- | --- |
| P3-A | `renderer.js:245` vs `:309` | `li.dataset.mode = p.mode`（渲染形状）与 `if (usage.mode === 'balance')`（更新逻辑）取自两个来源。实测中我故意让二者不一致，结果该行渲染成 2 条 bar、却只更新了第 1 条，另一条永久停在 `--`。当前主进程两端都做了 `VALID_MODES` 兜底所以实际不会分叉，但这是**靠约定维持的隐式不变量**，建议以 `usage.mode` 为单一事实源。 |
| P3-B | `main.js:271` | `id: incoming.id` 不校验类型与唯一性。传 `undefined` 时 `JSON.stringify` 会**静默丢字段**，落盘的对象没有 `id`，下次读取 `querySelector('[data-id="undefined"]')` 全部失配。加 `typeof id === 'string' && id` 校验 + 去重。 |
| P3-C | `main.js:435-442` | 余额模式先串行试 3 个余额候选路径，失败再走 `fetchOneAPIUserInfo` 的 4 个候选 = **最坏 7 次串行请求**，每次超时 10s。`state.polling` 能防重入，但对服务端不友好。建议并发探首轮 3 个。 |
| P3-D | `审查反馈.md:17` | 声称「`src/` 最后修改时间 20:30」，实测最新 mtime 是 `index.html` 的 **21:18:25**。不影响结论（源码确实比报告 19:34 新），但文档时间线不准，建议以实测时间戳为准。 |

---

## 六、要求下一轮反馈回答的问题

1. `detectLimitPct` 的 `used/total` 形态实测返回 **100**，README 却说支持该形态。**改实现还是改 README？** 二选一，别两头都不动。
2. 你第七节的 40 条回归里为什么**没有一条**覆盖 `detectLimitPct`？把「新增/替换的函数必须进回归清单」写成硬规则。
3. `p.id` 未转义是 `escapeHtml` 修复的漏网点。**做一次同类全量排查**：把所有「拼进 `innerHTML` 或选择器的变量」列出来，逐个确认已转义或已改用 DOM API。
4. `pollAll` 的 reject 会让 `setInterval` 永不注册。**建议把 `setInterval` 提到 `await pollAll()` 之前**，让自动刷新与首次抓取解耦。

---

# 待办清单（第四轮 · 交回修改）

```
[必须 — 功能性错误，用户可见]
1. main.js:357-369 + :389-426
   detectLimitPct 拆成 pairs/pctPaths/ambiguous 三档，pairs 优先；
   分母不明的绝对值一律弃权。把实测表那 6 行写成断言进回归。
   同步核对 README:45 的「支持 used/total」是否属实。

2. renderer.js:229-230 + :292
   p.id 转义或改用 dataset；applyUsageRow 的选择器改为按 dataset 遍历匹配。
   pollOne 的 catch 块内不再调用可抛的 applyUsageRow。

[必须 — 健壮性]
3. renderer.js:65   loadProviders() 补 .catch 兜底 + UI 可见的错误提示
4. renderer.js:400  pollOne catch 块内的恢复路径加保护
5. main.js:104      把 setInterval(pollAll) 提到 await pollAll() 之前

[建议]
6. renderer.js:89   http 告警改用 new URL().protocol 判断，别用 startsWith
7. renderer.js:245  统一用 usage.mode 决定行的形状与更新逻辑
8. main.js:271      providers:save 校验 id 为字符串且唯一
9. main.js:435      余额候选路径改并发探测
```

**验收要求（沿用上轮，不放松）**：每条给出「文件:行号 + 实际代码片段 + 该行为的实测输出」。只写「已修复」不接受；只写一句「已按建议改写」也不接受。

---

# 方法论教训（累计）

三轮下来，问题类型已经收敛，一共三类，**每轮换着出现**：

1. **改动抄错却编理由圆场**（第一轮 P0-2）—— 最危险，自述听起来有技术含量，不回溯原文就会被骗。
2. **只改点名处、不做同类排查**（`clamp` → 漏 `pickPct`；`escapeHtml(name)` → 漏 `escapeHtml(id)`；`toFixed` → 漏 `formatBalance`）—— 每轮都出现，每轮都说「已全面排查」。
3. **测试覆盖面跟不上改动覆盖面**（`pickPct` 换成 `detectLimitPct`，回归清单没跟着长）—— 造成「PASS 40 / FAIL 0」与「新函数实测 5 个用例失败」同时成立。

**给核验方（我）的规则**：
- 不读自述，只读源码；能跑断言就不靠肉眼。
- 重点查**新增/替换的函数**，那是最可能没被测到的地方。
- 反馈文件里的时间戳、字节数、md5 一律实测复核（本轮报告 md5 属实，但 src 时间戳不实）。

**给修改方的规则**：
- 每轮交付前，把「本轮新增或语义变更的函数」列出来，每个至少 3 条断言。
- 做同类排查，列清单，而不是声称做过。

---
---

# 第四部分 · 第五轮复核（对重构版的独立核验）

复核日期：2026-09-11 深夜
复核对象：`审查反馈.md` 第八节（md5 `70adce42…`、50004 字节、880 行、mtime 23:34:09）
被核验代码：新增 `src/lib/limits.js` + 整个 `test/` 目录，六个源文件全部改动
核验方式：
1. 哈希基线核对（它声称的每个 md5 都实测比对）；
2. 三套 Node 测试实跑（无窗口，`npm test` 等价命令）；
3. 从真实 `main.js` 抽取 `probeCandidates` / `tryFetchJson` 求值，构造它测试里**没有**的场景做行为探针；
4. 全文读码。

## 基线核对

它第八节开头声称的哈希，我逐个实测：`审查报告.md` `6940b0c2`/43420B/941 行、`main.js` `90d2dd6f`、`limits.js` `81197c1e`、`renderer.js` `f7c99b20`、`index.html` `29a2a4f9`、`styles.css` `ebee6a81`、`preload.js` `8dfb1996`、`report-items.test.js` `2af1d2d3`、`limits.test.js` `1090d3d4`、`ipc.test.js` `96de26d6`、`dom/main.js` `f1a9ee95` —— **全部吻合**。

三套测试实跑：`report-items 58 passed`、`limits 37 passed`、`ipc 23 passed`，与它声称完全一致。

**这一轮它的陈述是诚实的。** 连它自己在第八节末尾承认的「核验脚本连续第三次误报」也如实写了，没有藏。

## 一、它这轮做对的

### 1. 它纠正了我一处错误 —— 而且它是对的

我在第三部分给的 `ambiguous` 档建议是「≤1 当比例，>1 弃权」，但**我自己的实测表第 6 行**期望 `{'5h_used': '42%'}` → **42**。两处自相矛盾：按我给的代码，`'42%'` 经 `numOf` 变成数字 42，`42 > 1` → 弃权 → 返回 `null`，跟我表格里的期望对不上。

它发现并改用独立的 `explicit` 信号（`parseLimit` 里带 `%` 后缀 → `explicit: true`，直接采信；裸数字才受 `>1 弃权` 约束）。**这个处理是对的，我的原文有内部矛盾。** 记此，翻篇。

### 2. 抽 `src/lib/limits.js` 是这轮最正确的一手

我上一轮只指出「新函数没进回归清单」，它往下追了一层根因：`detectLimitPct` / `normalizeBalance` 内联在 `main.js` 里，而 `main.js` 是 Electron 入口（`require('electron')` + `app.whenReady()`），**在没有窗口的环境里根本 require 不进来** —— 所以「想补测试也只能靠从源码里临时抽文本」。把纯函数抽成独立模块后，`test/limits.test.js` 直接 `require` 就能跑。

这是把「以后还会犯」变成「结构上不会再犯」。比我提的建议更彻底。

### 3. 三档结构 + `pairs` 优先的实现是正确的

实测 37/37 通过，含我表格里那 6 例（`used/total 300/1000` → 30、`used:3/total:1000` → 0.3、`five_hour_limit` 单独出现 → null）。`pctOf` 死代码也顺手清了。

### 4. 测试分层合理，且大部分不需要窗口

`npm test`（118 条：58 + 37 + 23）在纯 Node 下可跑，覆盖了源码断言、纯函数边界、以及**用 stub electron 加载真实 main.js 直接调 IPC handler** 的端到端路径。只有 `npm run test:dom`（27 条）需要真实 Electron。这个分层是对的——CI 友好，且关键链路（`used/total → 30` 走真实 `usage:fetch`）不依赖 GUI。

### 5. bar 宽度改用 `getBoundingClientRect` 实测

`test/dom/main.js:68-75` 用真实布局宽度算占比，而不是断言 `style.width` 字符串。这是真行为验证。它甚至承认「原先肉眼判断 bar 长度不对，测量后发现是自己看错了」—— 以测量为准，对。

### 6. P1-B 的修复可判别

恶意 id 场景：注入 `<img>` 从 **2 → 0**、`lastUpdated` 从 **`—` → `更新于 23:13:39`**。这两项都是修复前必然失败、修复后必然通过的判别性证据，不是"看着像好了"。

---

## 二、P1 — 实测发现的三条（它未覆盖）

### P1-A `probeCandidates` 会等最慢的候选，可能比改造前慢 800 倍

```js
// main.js:429-431
const probed = await Promise.all(
  paths.map(async (path) => ({ path, res: await tryFetchJson(`${base}${path}`, provider, headers) })),
);
```

`Promise.all` 的语义是**等全部 settle**，不是"谁先成功用谁"。所以只要有一个候选慢，整轮就被它拖住 —— 即使正确端点在毫秒级就返回了有效数据。

实测（探针脚本从真实源码抽函数求值）：

| 场景 | 耗时 |
| --- | --- |
| 4 个候选全部 150ms（= 它 `ipc.test.js` 里的场景） | **185ms** ✅ |
| 有效端点 `/api/user/self` **10ms** 返回，另一个候选 8s | **8035ms** ❌ |

第二行是关键：**有效数据 10ms 就到手了，却等了 8035ms**。而改造前的串行实现是「第一个成功就返回」，同样这个场景只要 **10ms**。

所以这不是纯粹的改善，是**有得有失**：
- 最坏情况：40s → 10s（改善）
- 「首选快、其他候选慢」：10ms → 10s（**退化**）

而**它的测试恰好掩盖了这一点**：`ipc.test.js:216-221` 给 4 个候选设了**完全相同的 150ms delay**，所以 `elapsed < 350` 只能证明"并发确实发生了"，证不了"慢候选不会拖累"。测试设计上少了一个判别性用例。

**修复**（先到先用，不等待慢候选）：

```js
  // 先到先用：任一候选给出可接受响应即返回，不等待慢候选。
  return new Promise((resolve) => {
    let remaining = paths.length;
    let done = false;
    for (const path of paths) {
      tryFetchJson(`${base}${path}`, provider, headers)
        .then((res) => {
          if (done) return;
          if (res.ok && accept(res)) {
            done = true;
            candidatePathCache.set(key, path);
            resolve(res);
            return;
          }
          if (--remaining === 0) resolve(null);
        })
        .catch(() => { if (!done && --remaining === 0) resolve(null); });
    }
  });
```

代价是失去「数组顺序偏好」（原来是 `/api/user/self` 优先）。对同一 host、同一语义的不同拼写，这个偏好不值得用 8 秒去换。**并补一条判别性测试**：让首选 0ms、另一个候选 3000ms，断言 `elapsed < 300`。

### P1-B `accept` 太松 → 错误路径被写进缓存，可用候选永不重试

```js
// main.js:466-469 —— fetchOneAPIUserInfo 传的 accept
const res = await probeCandidates(provider, 'userinfo', candidates, headers, (r) => {
  const root = r.data?.data ?? r.data;
  return Boolean(root) && typeof root === 'object';    // ← 只要求"是个对象"
});
```

`probeCandidates` 会把第一个被 `accept` 的路径**记进 `candidatePathCache`**，之后稳态只打那一个路径。而 `accept` 只要求"是个对象"，于是：

实测（探针）：

```
第1轮 候选响应 = {}                                   ← /api/user/self 返回 HTTP 200 + 空对象
第1轮 写入缓存 = [["p1:userinfo","/api/user/self"]]    ← 空对象被接受，写入缓存
第1轮 解析结果 = null

第2轮 实际请求 = ["https://gw.example.com/api/user/self"]   ← 只打缓存路径
第2轮 解析结果 = null   <- /api/user/status 里有 30%，但再也不会被请求
```

**问题**：`/api/user/status`（或 token/status/api/status 中任意一个）本来能返回 30% 的数据，但因为 `/api/user/self` 先返回了一个「HTTP 200 但无有效字段」的响应并被缓存，**其余候选再也不会被尝试**。用户看到的是"找不到 5h/周 限额字段"，而且**只要不改配置就一直这样**（缓存只在 `providers:save` / `providers:delete` 时清）。

真实可达性不低：OneAPI/NewAPI 在 key 无效或权限不足时经常返回 **HTTP 200 + `{success:false, message:"无权进行此操作"}`**；也有网关的 `/api/user/self` 只返回用户信息、不含 5h 限额字段（限额在别的端点）。

**对比**：`fetchBalanceUsage` 的 accept 就是严谨的 —— `Boolean(normalizeBalance(res.data))`，真解析出余额才算数。**两处 accept 标准不一致**，而这两处是同一次重构里写下的。

**注意**：这一条是**本轮新引入的**。改造前的串行探测每轮都会试遍候选，不存在"被缓存卡死"。

**修复**：accept 必须检查「真的能解析出我们要的东西」，并按调用方需求区分：

```js
// main.js:458 —— 让 accept 由调用方决定
async function fetchOneAPIUserInfo(provider, accept) { ... }

// plan 模式（main.js:382 处）：
const data = await fetchOneAPIUserInfo(provider, (r) => {
  const root = r.data?.data ?? r.data;
  if (!root || typeof root !== 'object') return false;
  return detectLimitPct(root, FIVE_HOUR_SPEC) != null
      || detectLimitPct(root, WEEKLY_SPEC) != null;
});

// balance 的 fallback（main.js:449 处）：
const data = await fetchOneAPIUserInfo(provider, (r) => Boolean(normalizeBalance(r.data)));
```

**并补一组判别性测试**：`/api/user/self` 返回 `200 + {}`、`/api/user/status` 返回有效数据 → 断言最终解析出 30，且缓存里存的是 `/api/user/status`。

### P1-C IPC 调用点的兜底只修了一个：7 个里 4 个裸奔

上一轮我把「`loadProviders()` 没有 `.catch`，而紧邻的 `getSecurityStatus()` 有」定性为 P2-B。这轮它修了 `loadProviders` —— **但只修了那一个点，没做同类排查**。

`renderer.js` 里全部 `window.api.*` 调用点（实测 grep）：

| 行 | 调用 | 兜底 | 后果 |
| --- | --- | --- | --- |
| `:70` | `loadProviders()` | ✅ `.catch` + 横幅 | —— |
| `:74` | `getSecurityStatus()` | ✅ `.catch` | —— |
| `:460` | `fetchUsage()` | ✅ `pollOne` 内 try/catch | —— |
| `:127` | `minimizeWindow()` | ❌ | 点最小化无反应 |
| `:128` | `hideWindow()` | ❌ | 点隐藏无反应 |
| **`:208`** | **`saveProviders()`** | ❌ | **点保存无任何反馈** |
| `:237` | `deleteProvider()` | ❌ | 行不消失（state 已删、UI 未同步） |

`:208` 最重：

```js
// renderer.js:200-214（persistAll）
const res = await window.api.saveProviders(payload);   // ← 若 reject，persistAll reject
// renderer.js:186-192（onSaveProvider）
const ok = await persistAll({ id, apiKey: apiKey || undefined });   // ← 这里抛出
if (!ok.ok) { 显示错误; return; }                                    // ← 永远到不了
```

`providers:save` 是**同步 handler**，内部 `writeAll()`（`fs.mkdirSync` + `fs.writeFileSync`）或 `encryptKey()`（`safeStorage.encryptString`）一旦抛错，就会变成 IPC rejection。`persistAll` 没有 try/catch，`onSaveProvider` 也没有 → 表单 `submit` handler 抛出 → 未处理 rejection。

**用户可见后果**：点「保存」→ **什么也没发生**（没有错误提示、表单不清空、列表不刷新）。而 `state.providers` 已经被改过了（push/assign 在 `persistAll` 之前），所以后续任何一次 `renderAll()` 都会把新订阅显示出来 —— 用户以为存上了，**重启后消失**。

这与它上一轮刚修好的 P2-3「保存失败要反馈到 UI，不能只 console.error」**是同一个用户诉求的漏网路径**：它修的是 `res.ok === false` 这条，漏的是 IPC rejection 那条。

**而且测试完全没覆盖**：`test/dom/stub-preload.js` 里 `saveProviders: async () => ({ ok: true })` —— 永远成功；`stub-preload-fail.js` 只让 `loadProviders` reject。**没有任何一个场景让 `saveProviders` reject。**

**修复**：

```js
// persistAll 包住 IPC
async function persistAll(changed) {
  const payload = state.providers.map((p) => ({ ... }));
  try {
    const res = await window.api.saveProviders(payload);
    if (res?.ok === false) return { ok: false, error: res.error };
    return { ok: true };
  } catch (err) {
    return { ok: false, error: err?.message || String(err) };
  }
}

// deleteProvider 同理，失败要把 state 回滚或至少提示
// minimizeWindow / hideWindow 加 .catch(() => {})，别留未处理 rejection
```

**并补 DOM 场景**：加一个 `stub-preload-savefail.js`，断言点保存后 `saveError` 横幅可见。

---

## 三、P3 级

| 编号 | 位置 | 问题 |
| --- | --- | --- |
| P3-E | `README.md:44-45` | 测试数字全部过时：写「IPC 端到端 **14**」（实际 **23**）、「渲染断言 **12 + 5**」（实际 **16 + 6 + 5 = 27**）。它自己在反馈顶部写的是正确的 23 和 16+5+6，**说明它知道正确值，只是没回头同步 README**。`report-items.test.js` 只断言 `scripts.test` 存在，不校验 README 里的数字，所以漂了没人发现。 |
| P3-F | `README.md:55-57` | 段落说「`src/main.js` 里的解析逻辑是启发式的……实现见 `detectLimitPct`」，但解析逻辑本轮已迁到 `src/lib/limits.js`。路径描述失准，建议改为「`src/lib/limits.js`」。 |
| P3-G | `test/report-items.test.js`（58 条） | 绝大多数断言是 `matches(SRC, /正则/)` 的**源码文本匹配**。它能防「被人删掉」，但防不住「代码在、逻辑变了」。例如 P1-3 断言 `if (state.polling) return` 和 `state.polling = false` 两处各自命中 —— 却**不保证它们在同一函数里**。护栏作用有限；真正的行为验证在 `limits.test.js` / `ipc.test.js` / `dom/main.js`。建议把关键条目（轮询重入、阈值不发请求）升级为行为断言。 |
| P3-H | `test/ipc.test.js:206,226` | `assert.ok(elapsed < 350, ...)` 这类**时间断言**在负载高的机器上会 flake。同一用例里已有更强的计数断言（`assert.strictEqual(probed, 3)`），时间断言可以放宽到 1000ms 或直接去掉。 |
| P3-I | `main.js:443-446` | `fetchBalanceUsage` 里 `normalizeBalance` 被调用两次（`accept` 里一次、返回时一次）。纯函数无副作用，无实际影响，但可以只在 accept 里算一次把结果带出来。 |

---

## 四、要求下一轮反馈回答的问题

1. **`probeCandidates` 用 `Promise.all` 等最慢的候选**，实测「首选 10ms、另一候选 8s」时总耗时 8035ms，而改造前的串行只要 10ms。**这是有意的取舍，还是没考虑到的？** 你的测试给 4 个候选设了相同 delay，为什么？
2. **`fetchOneAPIUserInfo` 的 `accept` 只检查「是个对象」**，于是 `HTTP 200 + {}` 会被写进 `candidatePathCache`，其余候选永不重试（已实测）。而 `fetchBalanceUsage` 的 accept 是严谨的（真解析出余额）。**同一次重构里两套标准，哪个是笔误？**
3. **`renderer.js` 里 7 个 `window.api.*` 调用点，只有 3 个有兜底。** 上一轮 P2-B 我指的是「这一整类」，不是「这一行」。你为什么只改了 `loadProviders`？**请给一张 IPC 调用点兜底清单**（就像你这轮给的那张 innerHTML/选择器变量清单一样——那张做得很好）。
4. README 的测试数字（14 / 12+5）与你反馈里的（23 / 16+5+6）不一致。**以哪个为准？** README 是给人看的第一入口，数字错了比不写更糟。

---

# 待办清单（第五轮 · 交回修改）

```
[必须 — 实测已证伪，功能会静默失效]
1. main.js:418-439  probeCandidates 改「先到先用」，不等慢候选；
                    补判别性测试：首选 0ms + 另一候选 3000ms → 断言 elapsed < 300。
2. main.js:466-469  fetchOneAPIUserInfo 的 accept 改为「真能解析出限额/余额」，
                    并按 plan / balance 区分；补测试：
                    self 返 200+{} + status 返有效数据 → 最终解析出 30 且缓存指向 status。
3. renderer.js:208  persistAll 包 try/catch，把 IPC rejection 变成 {ok:false,error}；
    renderer.js:237  deleteProvider 同样处理。
    renderer.js:127-128 minimizeWindow/hideWindow 加 .catch(() => {})。
                    新增 test/dom/stub-preload-savefail.js，断言保存失败横幅可见。

[必须 — 文档]
4. README.md:44-45  测试数字改为实测值（58 / 37 / 23 / 16+6+5）
5. README.md:55-57  「src/main.js 里的解析逻辑」改为 src/lib/limits.js

[建议]
6. test/ipc.test.js  时间断言放宽或去掉（已有计数断言）
7. test/report-items.test.js  关键条目从文本断言升级为行为断言
8. main.js:443  normalizeBalance 只算一次
```

**验收要求（沿用，不放松）**：每条给「文件:行号 + 实际代码片段 + 实测输出」。**另外**：这轮新加了缓存与候选探测机制，**请把「候选探测」相关的新函数（`probeCandidates`）也纳入「本轮新增/变更函数」清单**——它现在正处在上一轮 `detectLimitPct` 的位置上：新写的、最复杂、测试恰好没覆盖到关键场景。

---

# 方法论教训（累计 · 第四轮更新）

四轮下来，问题类型已经固化。**每轮都在不同位置复发同一类**：

| 类型 | 第一次 | 第二次 | 第三次 | 第四次 |
| --- | --- | --- | --- | --- |
| 只改点名处、不做同类排查 | `clamp` 死代码 | `escapeHtml(name)` 漏 `escapeHtml(id)` | `toFixed` 漏 `formatBalance` | **`loadProviders` 漏 `saveProviders`/`deleteProvider`** |
| 测试覆盖面跟不上改动覆盖面 | —— | `pickPct` → `detectLimitPct` 回归没长 | 40 条 PASS 但新函数 6 例错 | **`probeCandidates` 新写的，测试给所有候选同一 delay** |
| 优化引入新退化 | —— | —— | —— | **并发探测从「首个成功即返回」变成「等最慢」** |

**这轮的新教训**：它已经学会了给「变量」做同类排查（innerHTML/选择器清单做得很完整），但**没把同样的方法用到「调用点」和「新增函数」上**。清单思维要覆盖三类对象：**变量、调用点、新函数**。

**给核验方（我）的规则（更新）**：
- 不看自述，只看源码和实测输出。
- **重点复现「它测试场景之外」的同类输入** —— 本轮三条 P1 全部来自这个角度（换掉候选 delay、加一个空对象响应、数一遍调用点）。
- 它给的哈希/字节数一律实测（本轮全对，仍要核）。
- **自己也可能是错的**：本轮我第三部分的 `ambiguous` 建议与自己的表格矛盾，被对方正确纠正。核验方给出的具体代码同样要接受挑战。

**给修改方的规则（更新）**：
- 做同类排查要覆盖**变量、调用点、新增函数**三类，各出一张清单。
- 新引入的机制（缓存、并发、新抽象层）默认是**最危险的地方**，优先给它写「反例测试」而不是「正例测试」。
